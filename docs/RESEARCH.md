# Research provenance

> **Historical note (added 2026-08-31).** Several techniques below turn out to
> have direct precedent in the project author's own 1994 system — IDUR
> (M. Sharpe, N. Ahmed & G. Sutcliffe, "An Intelligent Document Understanding
> & Reproduction System," MVA '94, p. 267,
> [PDF](http://b2.cvl.iis.u-tokyo.ac.jp/mva/proceedings/CommemorativeDVD/1994/papers/1994267.pdf)):
> Hough skew correction via the same Hinds et al. (1990) reference our
> `deskew.hough` cites, RLSA (whose noise sensitivity IDUR already noted),
> text/picture/line-drawing block classification by image features (the
> ancestor of our `imagezones`), and Nagy's X-Y trees. mlws-ocr re-derived
> these independently before the paper resurfaced. The author's directional
> k-NN + SCC segmenter (1995, `layout/knn_scc.py`) grew from the same work.

Every algorithm and approach in mlws-ocr, with its lineage, where it lives
in this codebase, and how our implementation deviates. Rule of the house:
**a new algorithm does not land without an entry here and a docstring
naming its source.**

Status: ✅ implemented · 🔬 implemented, superseded or optional · 🗺 planned

## Image cleanup

| Technique | Lineage | Code | Notes / deviations |
|---|---|---|---|
| ✅ Projection-profile deskew | H.S. Baird, "The Skew Angle of Printed Documents" (1987); W. Postl (1986) | `cleanup/deskew.py` `ProjectionDeskew` | Row-sum variance as the alignment energy; coarse-to-fine search on a downsampled Otsu ink mask instead of Baird's connected-component sampling. |
| ✅ Hough-transform deskew | S.N. Srihari & V. Govindaraju, "Analysis of textual images using the Hough transform" (1989); Hinds, Fisher & D'Amato (1990) | `cleanup/deskew.py` `HoughDeskew` | One reference point per CC (bottom-center ≈ baseline); accumulator scored by Σ(bin²), equivalent to profile variance on points. ~9× faster than raster rotation. |
| ✅ Sauvola adaptive binarization | J. Sauvola & M. Pietikäinen, "Adaptive document image binarization", Pattern Recognition 33 (2000) | `cleanup/binarize.py` `SauvolaBinarize` | Via scikit-image. Known behavior: cuts thinner strokes on sharp clean text than a fixed 0.5 gray threshold (measured; see RESEARCH-NOTES in memory of 2026-08-30 sev-0 study). |
| ✅ Otsu global threshold | N. Otsu, "A threshold selection method from gray-level histograms" (1979) | `cleanup/binarize.py` `OtsuBinarize` | Baseline comparator in the registry. |
| ✅ Illumination flattening by background division | Flat-fielding; morphological background estimation à la S. Sternberg's rolling ball (1983) | `cleanup/illumination.py` | Background = heavy median filter on a downsampled copy, then divide. Median replaces the rolling ball. |
| ✅ Despeckle by component area | Standard document-image practice (see O'Gorman & Kasturi, *Document Image Analysis*, 1995) | `cleanup/despeckle.py` | Area threshold declared at 300 dpi, scaled by (dpi/300)². |
| ✅ Deskew: pixels rotated out of the frame are dropped, not piled on the edge row (2026-09-12) | Baird 1987 (the alignment energy must be computed over the same pixels at every angle) | `cleanup/deskew.py` `_InkProjector.variance`; `tests/test_cleanup_stages.py` | The coordinate projector clipped an out-of-frame row index to the edge row. On two of the sixteen first magazine and newspaper pages a large halftone photograph reaching the bottom of the page put thousands of pixels into one row at the extreme angles, the fake spike won the search at the full 5 degrees (5.5 after the unclipped refinement step), and the rotated page lost its text columns to the image-zone stage: newspaper page 8227_030 read 0.7% char, magazine page 8056_030 9.8%. Dropping the pixels, as `ndimage.rotate` does, reads 0.45 and 0.15 degrees there and changes NOTHING on the 105 pages of the four standard sets (every estimate identical to the fine step, so the four-set rows stand without a re-run). A Baird-style projector over text-sized components only was tried in the same probe and did NOT fix the magazine page (4.65 degrees: halftone dots are text-sized). |

## Layout analysis

| Technique | Lineage | Code | Notes / deviations |
|---|---|---|---|
| ✅ Recursive XY-cut | G. Nagy & S. Seth, "Hierarchical representation of optically scanned documents" (ICPR 1984) | `layout/blocks.py` | Widest-gap-first axis choice; depth-first traversal order used directly as reading order. |
| ✅ Ruling detection by run-length morphology | Document morphology practice; cf. Wong, Casey & Wahl, "Document Analysis System" (IBM JRD 1982); D. Bloomberg's leptonica writings | `layout/rulings.py` | Opening with 1×L / L×1 elements; rules removed (dilated ×2 — ×1 left stubs) before line finding. Tolerance pass (default on): closing bridges hairline breaks and dilation PERPENDICULAR to the rule direction covers waviness — a 1–2 px rule wandering ±1 px never yields the straight run a strict opening demands. Found via a 400 dpi outlier page that lost its column rules entirely (−8 char); fix took it from 61.0→69.9% and turned the paired 300-vs-400 study into a consistent 4B win (mean 81.2→83.5). Still skew-sensitive ≳1° — noted gap. |
| ✅ Projection-profile line segmentation + baseline estimate | Classic; see survey: R. Cattoni et al., "Geometric layout analysis techniques" (1998) | `layout/lines.py` | Baseline = last row ≥ 25% of peak ink scanning upward (descender-robust heuristic). |
| ✅ Image/halftone zone removal | Text/image segmentation: Wong, Casey & Wahl (IBM JRD 1982); D. Bloomberg, "Multiresolution morphological analysis of document images" (1991); L. Fletcher & R. Kasturi, "A robust algorithm for text string separation from mixed text/graphics images" (PAMI 1988) for the line-art rule | `layout/imagezones.py` | Giant well-filled components + dense coarse-scale regions, union'd, grown, removed from ink before layout. Found via UNLV magazines: photo silhouettes bridge every whitespace channel, XY-cut collapses to one mega-block, and glyphs get union-grouped into photo components (138 groups on a page with ~500 words; 1116 after the stage). Zones must clear a minimum area (min_zone_frac): bold headline glyphs trip the density detector locally and were getting eaten (measured on newsprint). LINE-ART DETECTORS (2026-09-01, from the broad-30 deletion decomposition): (1) large HOLLOW components — big in BOTH dimensions (≥120px each at 300dpi) but sparsely filled, the Fletcher-Kasturi size rule the well-filled test misses (display glyphs and letter-spaced logos are big in one dimension only, so they stay); (2) ZONE ABSORPTION — a component ≥4× median CC dim within 12px of a detected zone is a remnant of the same graphic. Root case (visual session on UNLV 8509): a mailbag illustration's dense half was zoned while its line-art envelope spill stayed, welded two opening paragraphs into one unsplittable "line", and 507 chars of body text vanished into the garbage gate. After: 8509 deletions 507→33 chars; broad-30 char 77.5→78.4 (word flat 52.4, recall +0.8); news-10 75.2 / mag-6 59.8 — both far above their stale baselines, no absorption collateral. Regression fixture: tests/test_imagezones.py::test_line_art_removed_text_kept. |
| ✅ Tall-line adaptive re-split | Profile line finding with adaptive thresholds (cf. projection-profile line segmentation surveys; smoothing per Wong-Casey-Wahl) | `layout/lines.py` (profile impl) | Second deletion-decomposition fix (2026-09-01): on wide blocks the fixed noise floor (0.002×block width) sits BELOW inter-line valleys — descender/ascender overlap plus photocopy speckle keeps 1–2% of a wide row inked — so multiple text lines fuse into one segment whose vertically-stacked glyph groups recognize as i/l/! garbage and get suppressed (UNLV 8718: a 3-line paragraph, valleys 7–38 ink vs floor 3.4). Fix: any line taller than 1.8× the page median height is re-profiled with a threshold adaptive to its own peak (12%), smoothed over 5 rows (raw ascender rows sputter around any fixed cut and shed veto fragments — first attempt failed exactly there), tiny fragments merged into neighbors; a genuinely tall single line (display type, signature scribble) has no deep interior valley and passes unchanged. Results: 8718 deletions 193→0, 8753 169→0, 8509 residual 33→0; broad-30 77.5→78.4 (line art)→78.7 char over the two segmentation legs. |
| ✅ Per-glyph rejection retired as default | Reject-option classification (Chow's rule, 1970: reject when error probability exceeds the rejection cost) — revisited as the classifier improved | `decode/beam.py` `reject_mads=None` | Third deletion-decomposition fix (2026-09-01): the confusion report showed ~600 '?' outputs standing in for common letters. Re-sweep of the adaptive reject threshold (median+k·MAD), which measured FLAT (k 3–10) when introduced, turned strictly monotonic-DOWN with the harvest-strengthened prototypes: dev-8 85.7→88.2 char / 65.2→68.3 word from k=5 to off; broad-30 confirmed +1.6 char/+2.0 word; synthetic +2.7/+3.9/+3.6 char, word sev1 72.3→85.0. Chow's logic inverted: when the classifier was weak, '?' beat its guesses; now every '?' is a guaranteed error while beam+LM's best guess is usually right. Junk-line defense belongs to the garbage-suppression gate (line-level, lexicon+confidence-based), not per-glyph rejection. Mechanism kept as an opt-in parameter; per-char confidences still reported for downstream consumers. |
| ✅ Robust line x-height (caps-line fix) | Bimodal 2-means clustering (same pattern as the gap_band spacing fix); page-level typographic anchoring | `decode/beam.py` `_line_x_height` | Fourth confusion-cluster fix (2026-09-01): case-flip context analysis showed 225 of 519 upper→lower flips inside ALL-CAPS words and most of the 207 word-start flips on caps-heavy lines (letterheads, org names, address lines). Root: per-line x-height = median glyph height, which IS the cap height on such lines, so every capital paid the -1.2 "too-tall lowercase" prior and whole words flipped. Fix: cluster glyph ASCENTS (baseline to top — descenders don't pollute) 2-means per line; bimodal → low mode is the true x-height; unimodal AND ≥1.25× the page's lowercase anchor (median over lines of median ascent — body lines dominate) → caps line, use the anchor. dev-8 +1.3 char/+2.2 word; broad-30 80.3→81.0 char, 54.6→55.9 word; synthetic word +0.9/+1.4/+2.0. Remaining case tail: i→I/b→B word-start flips (pixel-identical twins, Capitalized pattern legal) and sentence-initial caps — needs a sentence-case prior, not size evidence. |
| ✅ Sentence-case repair pass | Orthographic post-correction (case restoration/truecasing lineage, e.g. Lita et al., "tRuEcasIng", ACL 2003) applied ONLY where pixels are silent | `decode/beam.py` `_sentence_case_pass` | Word-level case repair for first letters whose case twins pixels cannot separate (c,o,s,u,v,w,x,z size twins + i/I,b/B,p/P tall pairs): flip UP at a sentence start, flip DOWN a lone Capitalized corpus-frequent word mid-sentence. Three measured hazards shaped the guards: (1) up-flips are DOCUMENT-CALIBRATED — applied only when the document's own unambiguous sentence starts are ≥3:1 capitalized (all-lowercase documents and fixtures must not have style imposed); (2) sentence state across an unpunctuated line break is UNKNOWN, not mid-sentence — no flips there ("Call me Ishmael" at a verse-style line start was flipping down); (3) neighbor-capitalization veto protects proper-noun runs ("San Antonio"), and down-flips require very-frequent lowercase forms (>-10). Net: broad-30 +0.1 char/+0.4 word (81.1/56.3); synthetic word −0.3 avg residual — the irreducible cost is lone ambiguous-initial proper nouns with frequent lowercase twins ("Ishmael"→"ishmael", "Brown"→"brown" when a neighbor misreads); accepted as the real-page/synthetic trade and recorded. |
| ✅ Dehyphenation pass | Standard OCR post-processing (line-break hyphen removal, cf. any dehyphenation heuristic in OCR literature); lexicon-gated | `decode/beam.py` `_dehyphenate_pass` | From the dash-insertion context study (2026-09-01): ground truth reads "indi-\nvidual" as "individual". Two variants: (a) cross-line join (line-final "xxx-", next line of the SAME block starts lowercase, joined core endorsed; the same-block guard correctly refuses paragraph-final period-misread-as-dash + new sentence); (b) token-level (the wrapped halves often arrive already merged as "indi-vidual": strip the internal hyphen when the joined core is a word and neither the hyphenated form nor both halves are — "self-service"/"Michigan-Dearborn" keep theirs). Aggregate-NEUTRAL on broad-30 (cases too rare: precision +0.1, else flat) — kept as a correctness improvement; the rest of the dash cluster is garbled letterhead-region debris (the open letterhead recognition problem), not a dash-specific mechanism. |
| ✅ Word-level case coherence | Word-case consistency as a unit constraint (print words are all-lower / Capitalized / ALL-CAPS); post-hoc repair over pixel-ambiguous letters only | `decode/beam.py` `_word_case_coherence` | Letterhead diagnosis on 8571 (2026-09-01) showed the survivors after the day's fixes were case-jittered caps words: "MiCHIGAN", "ADDREss", "DETRoiT" — the beam's per-transition case_change_penalty (1.5/flip) loses to pixel deltas over a long word. When ≥70% of a word's letters agree on a case, the pixel-ambiguous minority (c,o,s,u,v,w,x,z,i,b,p twins) joins them; first letter exempt from down-flips; unambiguous shapes never touched. broad-30 81.1→81.2 char, 56.3→57.1 word; synthetic sev2 word +0.5, rest unchanged. |
| ✅ Punctuation position prior | Baseline-relative mark disambiguation (same geometry family as the descender prior) | `decode/beam.py` `punct_position_prior` | Corpus report: '.'→'-' ×69, '.'→',' ×51 — at 3×4 pixels the SHAPES of tiny marks cannot be told apart, but their VERTICAL POSITION can: '.' sits on the baseline, ',' hangs below, '-' and quotes float above. Among tiny-punct candidates the position-consistent mark gains 1.2 and the inconsistent ones pay it. broad-30 81.2→81.4 char, 57.1→57.9 word, recall +0.9; synthetic unchanged. |
| ✅ Mixed-alphanumeric repair | Twin-substitution post-correction (extends the digit-mode twin machinery both directions) | `decode/beam.py` `_mixed_alnum_repair` | Corpus report showed strays both ways: "0f" for "Of", "482D2" for "48202". A digit inside an alpha word flips to its letter twin ONLY when the lexicon endorses the result; a letter inside a number flips to its digit twin ONLY when flanked by digits on both sides (leading letter runs are product codes: "CD23021" stays); number+unit tokens (9am, 1st, 35mm) exempt. The unit study caught a would-have-shipped bug — "9am"→"gam" via a dictionary word — now a regression test (tests/test_decode.py::test_mixed_alnum_repair). broad-30 81.4→81.5 char, 57.9→58.3 word, recall/precision +0.4 each. |
| ✅ Dot/mark component-count prior | Multi-component class evidence from the grouping stage (the same i-dot grouping O'Gorman & Kasturi describe); mirrors our descender and tiny-punct priors | `decode/beam.py` `dot_prior` | The vertical-stroke confusion family (l↔i, i→I, i→d, t→l/1 — ~190 errors) survived the GED gate re-sweep because its distinguishing evidence is not in the skeleton at all: it is the PART COUNT the components stage already recorded when it merged a dot with its stem, and the decoder never consulted it. Measured on dev-8 top-1s: 'i' groups are 97% two-part and 't/l/1/T' ~100% one-part, while 22% of 'I' readings and 62% of 'j' readings contradict their class. A soft ±1.0 prior (photocopy breaks can split any stroke, so pixels and the LM keep veto power) favours dotted classes (i, j, !, marks-above accents) in two-part groups and undotted ones in one-part groups. dev-8 90.1→90.3 char / 71.8→72.3 word; broad-30 word 58.3→58.8, recall +0.3, precision +0.3. LESSON: when a rerank sweep measures flat, suspect missing evidence upstream rather than mis-tuned gates. |
| 🔬 Space-insertion repair by lexicon (NEGATIVE — vocabulary, not logic) | Fragment-join repair extended to long+short splits | `decode/beam.py` (`_join_decoded`, extension REVERTED) | Corpus space study (broad-30): 458 inserted spaces = 313 mid-word + 154 at punct/digit boundaries; deletions (232) are almost all tabular ("3|x", "5|3.00"). Mid-word splits come from the DEFINITE-gap path, which splits unconditionally — only the uncertain band is lexicon-gated. An extension admitting long-head + short-non-word-tail runs fired ZERO times and measured bit-identical on broad-30. Root cause, measured directly: the joined forms needed ("trademarks", "deadlines", "schoolers", "counseling") are ABSENT from the lexicon (frequency −18 = unknown), while the joins that must be AVOIDED ("fora", "asa" from "for a", "as a") ARE endorsed — the corpus (5 Victorian novels + dict-words) is anti-correlated with modern business vocabulary, so any lexicon-keyed join rule fires on exactly the wrong cases. Extension reverted. This promotes MODERN CORPUS COVERAGE from a nice-to-have to the blocking item for this error class. |
| ✅ Lexicon inflection expansion | Regular English morphology applied to a base-form word list (classic lexicon conditioning; the list is a Webster-derived base-form dictionary) | `scripts/build_langmodel.py` `inflections()` | Root cause found while chasing the space-insertion cluster: `/usr/share/dict/words` holds BASE FORMS ONLY — "trademark" present, "trademarks" absent; a 3000-entry sample found **100% missing their -s plural**. Because the decoder gates every split, join and substitution on lexicon endorsement, essentially every plural noun and third-person verb in a real document was unendorsed unless the corpus happened to contain it. Regular rules only (-s/-es/-ies/-ed/-ing; irregulars left to corpus evidence): lexicon 236k → 814k entries. broad-30 word 58.8→59.3, recall +0.3, precision +0.5 (char flat). Second round (2026-09-04): the inflections applied to the base-form list only, so corpus words gained no forms — 'airline' and 'onboard' were in, 'airlines' and 'onboarding' (the missing-word report's top entries on the modern letters) out; inflecting the corpus words too adds 26k forms (818k → 844k). Measured dev-8 flat, broad-30 flat (recall −0.1), modern +0.3 word / +0.2 recall / +0.2 precision; adopted. Trap recorded: `build_langmodel.py` DEFAULTS to `data/corpus` (a multilingual scratch directory) — the live English lexicon is built from `data/corpus_en_plus`; a rebuild from the default silently lost 'airline'. |
| ✅ Modern public-domain corpus | US federal government works, public domain by 17 U.S.C. §105 (Congressional bills + Federal Register) | `scripts/fetch_modern_corpus.sh`, `data/corpus_en_modern/` (gitignored) | The corpus behind both the lexicon frequencies and the char LM was five Victorian novels (667k words). Ordinary business vocabulary was either UNKNOWN or pinned at the flat dictionary floor, where it cannot win a decode contest: "email" −18→−8.3, "fax" −18→−10.4, "program" −15→−7.1, "software" −15→−10.2, "membership" −15→−9.7. Federal sources chosen over Wikipedia/EDGAR so a public repo carries no licensing entanglement; 12MB added, corpus 667k→2.4M words. Measured: dev-8 90.2→90.7 char / 72.2→74.7 word; broad-30 81.9→82.4 char / 59.3→60.8 word (first time over 60), recall +0.9, precision +0.9. Largest single leg since the harvest flywheel, and it works by letting mechanisms that ALREADY existed act on words they previously had no evidence for. |
| ✅ Char-GRU retrained on the enlarged corpus | Our own numpy char-GRU (see the language-model entry); retrained, not redesigned | `scripts/train_charlm.py --corpus data/corpus_en_plus` → `data/gru_en.npz` | The character model had learned its statistics from the same five Victorian novels as the old lexicon. Retraining on novels + modern federal text (2.4M words) improved the model on its own terms — validation perplexity 3.27 → 2.59 — and compounded with the lexicon leg rather than overlapping it: dev-8 90.7→91.1 char / 74.7→75.2 word; broad-30 82.4→83.1 char / 60.8→62.1 word, recall +1.4, precision +1.8. Synthetic held (96.3/96.8/95.9 char; word 85.9/87.9/85.4, sev1/2 +1.5/+1.4). Combined corpus leg (inflections + modern text + retrained GRU): broad-30 **81.9→83.1 char, 58.8→62.1 word, recall +2.6, precision +3.2**. |
| ✅ Broken-character merge lattice (associator) | Tesseract's legacy "chop and associate" — fragment association over a segmentation search; the inverse of our existing split lattice | `glyph/components.py` (`merge_*`), `recognize/stage.py`, `decode/beam.py` (`max_merge_variants`) | Found by the paired legacy comparison (`scripts/compare_legacy.py`), which showed the gap is CONSISTENCY, not peak quality: legacy's worst page of 30 is 80.8% char, ours was 29.9%, and on 9 pages we were already within 5 points. Diagnosing the worst page (UNLV 8718, an American Express letter in a fine old-style serif) at pixel level: the scan is clean and legible, but the thin top/bottom hairlines of round letters have dropped out in print, so every 'o' arrives as TWO disconnected arcs — "company" read as "c(21III)any", a 25-char line yielding 32 groups. We had a split lattice for touching characters and nothing to put broken pieces back together. Mirror mechanism added: components proposes unions of sub-glyph fragments, recognize scores the union crop, decode enumerates merge variants alongside split variants (adjacent merges conflict and are excluded). TWO CORRECTIONS during the work, both measured: (1) the size reference must be median glyph HEIGHT, not width — on a page where letters break apart the median WIDTH is itself halved, so a width-based rule refuses exactly the merges it exists to propose (fired 0 times before the fix, 464 after); (2) merge_char_bonus = 0.0, NOT the mirror of split_char_bonus — a split ADDS a character and must offset the extra glyph+LM terms it accumulates, whereas a merge REMOVES one and its shorter path is already favoured, so a bonus double-counts (swept on dev-8: 0.0 → 91.1 char, 2.2 → 90.8). RESULTS: page 8718 char 29.9→57.7; broad-30 83.1→83.9 char, 62.1→62.8 word, recall +0.9, precision +0.7; clean pages unchanged (8582 97.8, 20 merge hypotheses vs 464 on the broken page — the rule is selective); synthetic held (sev2 word +0.5). LESSON: the gap to a mature classical engine is a catalogue of failure-mode mechanisms, and each one shows the same signature — flat on clean pages, large recovery where the system was catastrophic. |
| 📊 Legacy-gap error decomposition | Paired error-type analysis against a common ground truth (standard OCR evaluation practice; cf. the ISRI/UNLV accuracy reports) | `scripts/compare_legacy_errors.py` | Both engines aligned to the same truth, every error bucketed. RESULT (2026-09-01): excluding the three catastrophic pages we score **86.8 vs legacy 95.5 — still 8.7 points behind on ordinary pages**; the catastrophic tail explains only ~¼ of the 11.4-point gap, the rest is SYSTEMATIC. Composition of the systematic excess (27 pages, 39,952 truth chars): shape substitutions +1397 (40%; ours 1667 vs legacy 270 — SIX times as many, the classifier itself), character deletions +716 (20%), case flips +391 (11%; legacy makes **10** case errors in 40k chars, we make 401), digit substitutions +373 (11%), punctuation +337 (10%), spaces +257 (7%). We are BETTER than legacy on spurious insertions (−27) — the suppression/scrub work paid off. CONSEQUENCES for the roadmap: (1) the classifier is the largest single reason legacy wins on clean pages, promoting "a classifier that can absorb the 120k harvest" to the top of the gap program (feature-based and self-trained, so inside the project's constraints); (2) case handling has a bounded, near-zero reference to chase; (3) deletions and digit/punct priors are the cheap remainder. DELETION ATTRIBUTION (same day, 30 pages): of 21 deleted runs ≥10 chars, only ONE was suppressed — the rest were never segmented, so the excess deletions are a LAYOUT failure, not the garbage gate. Families: display/letter-spaced text, letterhead blocks, small print, signature/closing lines, and the deferred price table. |
| ✅ Adaptation pins assert shape, not case | Reject-free adaptive classification; the pin comes from document clustering, the case from line geometry | `decode/beam.py` `pin_case_geometry` | The case share of the legacy gap traced to a mechanism, not a prior: case twins share a SHAPE, so the adapt stage clusters C with c, the majority vote pins every member lowercase, and the 2.5 pin bonus outweighs the 2.0 height swing — the existing comment claiming the height prior "retains veto power" was arithmetically false. Geometry itself was healthy (measured on dev-8: lowercase size-twins sit at 0.99–1.10 of x-height, capitals at 1.33–1.60). Fix: a pin asserts shape and, for the pure SIZE twins only, confident geometry directs its case (>1.35 upper, <1.15 lower, cluster vote in between). THREE ATTEMPTS, the first two measured and rejected: (a) fully case-neutral pins traded 25 fixed errors for 33 new ones — case-blind pins had been silently repairing pixel-driven uppercase misreads; (b) geometry over ALL twins TRIPLED lower→UPPER errors, because ascender lowercase (b, d, l, t) legitimately reaches ~1.4 x-height and got pinned as capitals. Restricting to size twins: dev-8 91.1→91.3 char / 75.1→76.5 word, Capitalized→lowercase 68→41 with the opposite direction flat (37→40); broad-30 83.9→84.1 char, 62.8→63.7 word; synthetic +0.2/+0.4/0.0 word. |
| 📊 Classifier ceiling study (offline, on the stored harvest) | Held-out evaluation of condensed vs. exhaustive nearest-neighbour and parametric alternatives (Hart 1968 condensation; Duda–Hart–Stork ch. 4) | `scripts/classifier_ceiling.py` | QUESTION (2026-09-01): the shape-substitution share of the legacy gap is 40% and the 1-NN pool uses ~3.6k of ~120k harvested real glyphs — is the rest recoverable? PROTOCOL: page-disjoint split of each harvest file (contiguous last 20%, 24k test glyphs, 57 classes), synthetic exemplars recovered from prototypes.npz and added to every training set, scored on top-1 / top-3 / truth-in-top-14 (the decoder's candidate list). Caveat stated in the output: test labels are self-labels, so agreement favours the incumbent. RESULT — top-1 on held-out real glyphs: incumbent cap-80 1-NN **91.9**; cap 250 95.1; cap 1000 96.2; 1-NN over all 120k **98.7**; 5-NN vote 99.1; harvest-only 1-NN 98.8; k-means 20/class 97.0, 60/class 97.5; regularized QDA 98.7; numpy MLP 95-256-C 99.0. Top-14 recall is ≥99.8 everywhere (the decoder already sees the truth; it is top-1 that moves). Macro (per-class mean) stays 82–84 for every method: the rare-class floor is label noise and coverage, not the model. So ~7 top-1 points sit in the harvest that the incumbent cannot reach — the 40% share IS recoverable in principle. |
| ✅ Per-class k-means condensation of the exemplar pool | P.E. Hart, "The condensed nearest neighbor rule" (IEEE Trans. IT 1968) realized with k-means; the same design as Tesseract's legacy classifier, which clusters training samples into per-class prototypes (Smith, ICDAR 2007) | `recognize/condense.py`, `scripts/build_prototypes.py --condense N`, `scripts/build_kmeans_prototypes.py` | THE PIPELINE TEST OF THE CEILING. Plain 1-NN over all 144k exemplars (synthetic + whole harvest) measured WORSE in the pipeline despite +7 offline: dev-8 91.3→90.1 char / 76.5→74.7 word. Confusion report names the mechanism — COVERAGE IMBALANCE: the harvest holds no digits at all and few capitals (O: 6, S: 77 against e: 15,429), so once lowercase grows dense every real digit finds a real letter first ('1'→'l' 3→17, '9'→'e' 0→8, '9'→'g' 0→6, '5'/'3'→'S', ','→'s' 0→7). Equal per-class prototype budgets restore the balance while keeping the real glyph modes: 60/class (6,562 prototypes, 4× smaller than the live pool) — broad-30 84.1→**84.5** char / 63.7→**65.0** word, recall +0.4 / precision +0.5; synthetic 96.5/96.9/96.1→**97.5/97.7/97.1** char, word 86.4/88.3/85.9→**91.3/90.3/87.9**; dev-8 −0.4 char / +0.2 word (91.3→90.9 / 76.5→76.7). Two of three sets up, the headline and the clean-page set strongly, so adopted. Budget bracket on broad-30: 40/class 84.4/65.2, **60/class 84.5/65.0**, 120/class 84.3/64.4 — flat between 40 and 60, falling by 120 (the harvest's lowercase mass starts to crowd again). The chunked matmul matcher (`nearest.py`, |q|²−2q·x+|x|² in float64, partial sort) landed alongside: identical rankings to the broadcast form (ties at 1e-13) and 20× faster, and it is what makes a 100k pool affordable at all. NEXT: coverage, not pool size — a format-endorsed DIGIT harvest (`harvest_glyphs.py --digits`) so digits get real exemplars too. |
| 📊 Deletion attribution, corrected; reading-order share measured | Zone-ordered scoring (ISRI practice) as a control | `scripts/eval_unlv.py --zone-order`, scratch layout renders | The earlier claim that 20 of 21 long deleted runs were "never segmented" was WRONG for the largest ones. Rendering the layout of 8514 (the 159-char run "EXECUTIVE DIRECTOR Estelle Murphy, MPA …") shows every block and line segmented and the text present in our output — after the body, where the ground truth lists the sidebar first; edit-distance alignment books a reorder as a deletion plus an insertion. Others were suppressed misreads the probe's word-match missed ("Business Liaison/Consultant" → 'Doe!ness L!a!eon/ConsuMana', "Denny Weddle" → 'D e my Se d d I e', letter-spaced "D~E~D~I~C~A~T~E~D" → 'o c o 1 c a T …'). Address lines were suppressed whole: 'PAssAIc, Na 07055', 'ltasoa, IL 60143'. MEASURED reading-order share: broad-30 with words re-sorted into ground-truth zones = 85.5 char vs 84.1 — **1.4 char points** of the 11.2 gap is order convention on letters (word accuracy drops 63.7→61.8 under the re-sort because zone sorting splits lines that cross zones). Real deletion families after correction: (1) suppressed address/data lines — fixed by format endorsement (below); (2) letter-spaced display text read as letter salad — a recognition problem (small caps) plus grouping; (3) logo lines inside image zones ("Wayne State University"); (4) the deferred price table. |
| ✅ Format endorsement for numeric tokens | Forms-OCR field validation (numeric fields validated by pattern, cf. Casey & Lecolinet 1996 survey; ISRI/UNLV forms evaluations) | `decode/formats.py` `numeric_endorsed`, used by `decode/output.py` and `scripts/harvest_glyphs.py --digits` | A rigid whole-token format (ZIP, ZIP+4, phone parts, years, dates, dollar/decimal amounts, thousands separators) is the digit analogue of a lexicon hit: '0'→'O' breaks the pattern and self-filters. Two consumers. (a) Suppression exemption: a line holding a format-endorsed token is data, not a signature scribble, even when the words beside it misread ('PAssAIc, Na 07055' was 16 deletions; kept, it is 2 substitutions). (b) Digit HARVEST: the lexicon word gate admitted no digits at all (harvest histogram: zero digit exemplars), which is the coverage hole behind the all-harvest dilution. Numeric tokens carry word confidence ≈0.05 (the confidence is lexicon-driven), so the digit harvest runs with `--min-conf 0` — the format is the endorsement. Yield ≈20 digits per business letter. MEASURED (suppression exemption alone): dev-8 91.3/76.5→91.3/76.8, recall +0.2, precision +0.3; broad-30 on the km60 model 84.5/65.0→84.5/65.1; synthetic unchanged (no numerics). Small, as expected for two address lines in thirty pages; kept because it is the right rule and the harvest side is the real payload. |
| ✅ Punctuation position prior, widened; comma/apostrophe position twins | Typographic-position disambiguation of small marks (classic OCR practice; cf. the baseline-relative features in Tesseract's legacy classifier) | `decode/beam.py` `punct_small_frac`, `punct_position_prior` | PROBE (dev-8, ground-truth-aligned glyph geometry): correct '.' stand 0.19–0.36 x-heights tall with bottoms within 0.09 of the baseline; correct ',' stand **0.46–0.62** tall, bottoms 0.22–0.38 below; ':' ≈1.0 tall, 2 parts, on the baseline; ';' 1.14 tall, 2 parts, hanging 0.27; apostrophes 0.46–0.75 tall with bottoms 0.65–0.93 ABOVE the baseline. The position prior was gated at h < 0.4 x-height, so it fired for periods and hyphens only — the top punctuation confusions (','→'.', "'"→',', ':'→';') were all outside its reach. Three changes: (1) band widened to 0.85 x-height (letters are ≥1 by construction); (2) hang threshold 0.10→0.15, the midpoint between the measured populations; (3) a colon/semicolon rule for two-part marks near x-height. And a SECOND mechanism the probe exposed: for real apostrophes the classifier offers only ',' ("I,ll", "Burgess,s" — no "'" in the candidate list; synthetic apostrophes do not look like real ones) — so a comma shape floating above the baseline now injects "'" as a POSITION TWIN with the comma's score, the same move as case twins. Measured on the condensed model: dev-8 90.9/76.7→91.1/77.3; broad-30 84.5/65.1→**84.8/65.8**, recall 76.3→76.9, precision 74.0→74.6; synthetic unchanged. Residual: eroded commas (h 0.3, bottom 0.09) are geometrically periods — unfixable by position. FOLLOW-UP (same evening): "you'll" decoded as "you-ll" — hyphen and apostrophe were both 'floating' and tied on position. Measured populations separate them: hyphen bottoms 0.37–0.50 x-heights above the baseline and wider than tall; apostrophes 0.65–0.93 and taller than wide. A floating mark that is high OR tall now favours the apostrophe/quote and disfavours the hyphen, and the apostrophe twin is injected from either ',' or '-'. dev-8 91.7/77.9→91.8/78.0; broad-30 86.4/67.8→86.5/67.9. |
| ✅ Digit harvest through format endorsement; live model condensed | Self-labeled real exemplars for the classes the lexicon gate could never admit (the numeric analogue of Ho & Nagy's self-training) | `scripts/harvest_glyphs.py --digits --min-conf 0`, `data/harvest_digits.npz`, `scripts/build_prototypes.py --condense 60` (live build) | 120 non-evaluation business letters yielded 964 real digit exemplars (10 classes; 9/1/0 most common — years, ZIPs, phones). Condensed together with the letter harvest into the live model (6,600 prototypes, k-means++ seeding with best-of-3 restarts after a single random seeding measured ±0.3 char / ±1 word of pure build noise on the synthetic suite). FIRST EFFECT was a FLIP, not a fix: the dev-8 confusion report went from digits-read-as-letters ('0'→'O' 18, '1'→'l' 11, '9'→'D' 7) to letters-read-as-digits ('l'→'1' 13, 'I'→'1' 12, 'o'→'0' 10, 's'→'5' 10), total errors 786→762 but word accuracy down (dev-8 77.3→76.4, broad-30 65.8→65.0 before the ++ rebuild). The probe below found the mechanism in the decoder, not the classifier. |
| ✅ Digit mode as a deferred decision | Late commitment (this project's own design principle: emit alternatives, decide with the most evidence); lexicon-gated mode selection | `decode/beam.py` `_beam_word` / `_beam_word_mode` | PROBE (dev-8, GT-aligned): in every letter→digit error the glyph's rank-1 candidate AND its adaptation pin were the correct letter ('so'→'50', 'is'→'15', 'tool'→'t001', 'allows'→'a110w5', "I'll"→"1'11"). The digit came from DIGIT MODE: the graded trigger counts a digit at rank 2-3 as 0.6 evidence, and with real digit prototypes a digit twin sits at rank 2-3 of most l/I/o/s glyphs, so ordinary short words crossed the 0.5 line, muted the LM and took the +1.2 digit boost. A first attempt — an 'alpha mode' mirror penalty for digits in words — measured exactly zero effect (the words never reached alpha scoring; they were already in digit mode). FIX: top-1 digits decide digit mode outright; graded evidence only earns a digit-mode decode when the WORD-mode reading is not a lexicon word; a single glyph never enters digit mode on graded evidence alone (a lone 'I' vs '1'); and a standalone 'l' token becomes 'I' in the repair pass (the only one-letter English words are 'a' and 'I'; adaptation clusters I with l and pins the majority). MEASURED (condensed digit model): dev-8 91.1/76.4→**91.7/77.9**, recall 84.0→85.4, precision 82.1→83.3 — the best dev-8 of the project; confusions 791→746, 'o'→'0' 11→3, 's'→'5' 11→3, 'I'→'1' 13→4; broad-30 84.8/66.2→**85.1/67.4**, recall 77.4, precision 76.0; synthetic 97.3/89.8→97.4/90.8, 97.3/88.3→97.6/90.3, 96.6/85.9→96.8/87.4; legal-8 (other domain, same decoder) old pool 85.4/64.0 → new live model 86.1/66.7. Session total (2026-09-01 evening): broad-30 84.1/63.7→85.1/67.4. |
| ✅ Row alignment of column blocks (unruled tables) | T. Kieninger & A. Dengel, "A paper-to-HTML table converting system" (T-Recs, DAS 1998) — unruled tables recognized from word/line geometry and read out by row | `layout/rows.py`, `decode/output.py` `align_columns` | The deferred price-table page (8588) rendered with its layout showed the mechanism exactly: the table is TWO blocks (a names column, an item+price column) whose lines share baselines; output emitted column after column while the ground truth reads rows, so edit distance booked half the page as deletions and insertions. Groups of horizontally disjoint, vertically overlapping blocks with ≥3 lines each, ≥60% of one block's baselines matched within 0.5 line-heights in the other, are re-emitted as rows (baseline clustering, left-to-right within a row). GUARD against the obvious failure (two columns of running text set on one leading grid ALSO share baselines): the cell-length prior — a block whose median words-per-line exceeds 4 is text, never a column of cells; plus newspaper/magazine doc_type opts out. MEASURED: page 8588 char 50.6→**82.2**, word 34.8→46.0; broad-30 85.1→**86.2** char / 67.4→67.7 word (the whole gain is that page — a one-page catastrophe was a quarter of the legacy-gap tail); dev-8 unchanged; synthetic unchanged; magazine-6 zone-ordered identical with and without (guard holds). Remaining on the page: cell-internal spacing ('3 x 5'→'3x5'), '(LINED)' misread, small-caps 'HOF PLAQUE'. |
| ✅ Column-first XY-cut for tall gutters (single-column doc types) | Nagy & Seth's X-Y tree (1984) with a column-preference rule; reading-order conventions of the ISRI zoning | `layout/blocks.py` `column_first_frac` | The 8514 sidebar diagnosis: the widest-gap rule cut ROWS first wherever a horizontal gap spanned the page, interleaving the sidebar's sections (BOARD OF TRUSTEES, OFFICERS, MEMBERS…) with the body's paragraphs, while the ground truth reads the sidebar as one column. Rule: inside a region at least half the page tall, any full-height gutter is cut before any row gap. MEASURED: 8514 char 69.0→**74.7**, word 42.5→44.4; dev-8 unchanged; BUT magazines −2.6 char (48.6→46.0 on mag-6 unordered) — their gutters are genuine columns whose order the widest-gap rule already gets right, and forcing columns first inside sub-regions broke it. So the rule is a single-column-document PRIOR, applied with the existing letter/legal/book gutter widening only; magazines/newspapers keep the classic rule. broad-30 86.2→**86.4** char / 67.7→67.8 word (the reading-order share was measured at 1.4 points; this recovers a seventh of it on one page family). |
| ✅ Word-case coherence: body-majority rule, possessive exemption | Same orthographic convention as before (all-lower / Capitalized / ALL-CAPS), applied to the word BODY | `decode/beam.py` `_word_case_coherence` | Case-context probe after the evening's fixes (broad-30): UPPER→lower 179 (wordstart 79, capsword 64), lower→UPPER 171 (wordstart 80, **midword 67**). Midword examples: "SaVe", "yoU'll", "Sus[s]man" — a Capitalized word's legitimate initial was voting AGAINST its own lowercase body, so a 2:1 body never reached the 70% majority. The lowercase majority is now judged on the letters after the first (≥3 of them, ≥60%); and the possessive s after an apostrophe is exempt from up-flips ("TADC's" had been becoming "TADC'S" inside a caps word). dev-8 91.8/78.0→91.8/78.1; broad-30 86.5/67.9→86.5/68.0; synthetic unchanged. Small, kept as the correct rule. |
| 📊 Harvest round 3 under the improved pipeline (capitals coverage) | Self-training iteration (Ho & Nagy 2000) — the flywheel's third turn, now consumed by condensation instead of a capped 1-NN pool | `data/harvest_en_r3.npz` (100 non-eval letters), `scripts/build_prototypes.py --harvest` for variants | Motivation: the classifier-ceiling work moved the consumption mechanism, and the pin-case fix changed the labels (round 2 had labeled most capitals lowercase: S 77, C 47, O 6). Round 3 yielded 61,547 exemplars from 100 pages with capitals 1,747→2,112 (S 207, C 126, P 92). Two variants measured against the live model under identical code: (a) r3 REPLACING round 2 — dev-8 91.8/78.1→92.3/79.5, but synthetic sev2 word 87.4→85.0 and broad-30 word 68.0→67.7; (b) r3 ADDED to round 2 — dev-8 92.1/78.6, synthetic 97.6/91.3 · 97.8/91.3 · 96.6/87.4 (all ≥ live), broad-30 86.5/68.0 with recall 77.4→**78.1** and precision unchanged. Adopted (b): the union is the live model (`--condense 60` over harvest_en, harvest_en_r3, harvest_legal, harvest_digits). Verdict on the flywheel stands — headline char is flat on the third turn — but coverage still buys recall and the clean-page sets, which is what the condensation mechanism was built to let through. |
| ✅ Thin-wide veto for image zones | Text/graphics discrimination by component aspect (Fletcher & Kasturi 1988 use elongation to separate rules from text; the same cue separates an underlined line from art) | `layout/imagezones.py` `max_aspect` | Zone crops from the deletion families showed page 8571's zones 1 and 2 were UNDERLINED TEXT LINES ("Gifts are tax deductible…", "your receipt for income tax purposes."): the underline welds every glyph into one 880×32 px component, which passes the giant-well-filled rule and is erased as art. A giant component longer than 6× its thickness is a rule or an underlined line, never art — skipped, left to rulings and text. MEASURED: dev-8 92.1/78.6→**92.5/79.5**, recall 86.2→87.1; broad-30 86.5/68.0→86.5/**68.3**; magazine-6 zone-ordered 54.9→55.4 char; synthetic unchanged. (Page 8571's own char accuracy does not move because the recovered lines sit out of ground-truth order and edit distance books them as insertions regardless of content — the recall and the other sets carry the evidence.) |
| 🔬 Stroke-preserving rule removal — NEGATIVE, opt-in | Line removal with intersection recovery (Yu & Jain, "A generic system for form dropout", PAMI 1996) | `layout/rulings.py` `preserve_strokes` (default False) | After the veto, the underlined line decoded as "nift- Ara tax M.d.j.fihla": the rule band (rule dilated by 2) erased the glyph bottoms resting on the underline. Restoring band pixels that continue text ink within a few rows above/below made the line read "Gifts arm tax Hedlletible as provided by law. This is" — and measured dev-8 +0.2 word, broad-30 −0.2 word, **legal-8 86.3→85.8 char / 66.2→65.6 word**: pleading pages carry long rules beside line-numbered margins, and the restored stubs decode as junk. Kept as an opt-in for underlined business text; default off. The right fix is probably to restore only where ink exists on BOTH sides (true crossings) plus glyph bottoms only when the run above is glyph-sized — not done. |
| ✅ Self-trained MLP as a second opinion on the candidate list | Multilayer perceptron over hand-designed features (Rumelhart–Hinton–Williams 1986; LeCun et al.'s early zip-code work used the same shape of model on extracted features) — self-trained on our own exemplars, inside the no-pre-trained-nets constraint; combined with the prototype matcher by additive re-costing (classifier combination, Kittler et al. 1998) | `recognize/mlp.py`, `scripts/train_mlp.py`, `recognize/stage.py` `mlp_path` / `mlp_weight` / `mlp_inject` | The ceiling study's best offline method (99.0 top-1 vs 97.5 for condensation) brought into the pipeline WITHOUT replacing the prototype distances: cost' = distance + w·(nll_mlp(c) − min nll over the list), so the MLP's favourite among the candidates keeps its prototype distance (the scale graphic detection and adaptation calibrate against is untouched) and every other candidate pays its disagreement in nats; the MLP's top-3 classes are inserted when the prototypes missed them. One hidden layer of 256, numpy Adam, cosine schedule, inverse-sqrt class weights; trained in **12 seconds** on 197k exemplars (synthetic renders + all harvests, `build_prototypes.py --cap 1e9 --inlier 100` → `train_mlp.py`), held-out 5% top-1 97.7 (self-labels). MEASURED (w=1): synthetic 97.6/91.3 · 97.8/91.3 · 96.6/87.4 → **98.2/93.7 · 98.6/95.1 · 97.4/90.8**; dev-8 92.5/79.5→92.4/80.2, precision 84.2→84.9; broad-30 86.5/68.3→**86.8/68.3**, recall 78.2→78.9, precision 76.1→76.8; legal-8 86.3/66.2→86.4/66.3. WEIGHT SWEEP: w=0.5 dev-8 92.4/80.4; w=1 as above; **w=2** broad-30 **87.0/68.6**, dev-8 92.7/80.8, legal-8 86.5/66.5, synthetic 98.2/93.7 · 98.4/94.7 · 97.4/90.8; w=4 broad-30 86.9/68.5, dev-8 92.7/81.1, legal 86.6/66.7, synthetic sev0 slips to 98.1/93.2 while sev2 rises to 97.7/91.3. Default w=2 (the headline optimum). Adopted (`data/mlp.npz`, gitignored like the other models; README lists the build). The 'overnight bigger network' item is now a 12-second job; what is left overnight is data, not training time. Day total (2026-09-01): broad-30 77.3/52.4 → **87.0/68.6**; legacy reference 95.3/90.5. |
| 📊 Legacy-gap decomposition, end of day (2026-09-01) | Same paired error-type analysis as the morning's | `scripts/compare_legacy_errors.py` (run with `TESSDATA_PREFIX` pointing at a legacy-capable `eng.traineddata`; Homebrew's ships LSTM-only) | All 30 pages, 44,668 truth chars: ours **87.0** vs legacy 95.3; excess errors 3,861 (morning, 27 clean pages: ~3,470 excess at 86.8 vs 95.5 — the catastrophic tail is now inside the count and the total still fell). Shares of the remaining gap: shape substitutions **43%** (2,036 vs 368 — still 5.5× legacy), character deletions 25% (1,411 vs 459), case 9% (350 vs 14), punctuation 7% (490 vs 227), digits 6% (351 vs 136), spaces 8%, insertions 4%. What moved today: digits +373→+215, punctuation +337→+263, case +391→+336 (on a harder page set), deletions per clean page down (the table and underline families). What did NOT move: shape substitutions (+1,397→+1,668 with the three hard pages included), even though the classifier's offline top-1 on real glyphs went 91.9→~99. CONCLUSION: the residual shape errors are not single-glyph classification errors — they sit where glyphs are broken, merged or eroded on degraded pages, i.e. in segmentation and in features that are not robust to breaks. Next levers: (1) a TRUTH-labeled real glyph set from GT-aligned pages (the harvest is self-labeled, so it cannot show the classifier its own systematic mistakes); (2) merge/split lattice quality on degraded pages; (3) reading order (1.4 points) and display-type wordmarks for the deletion share. |
| 📊 Shape-residual labeling (GT-aligned glyph probe) | Error taxonomy by glyph condition, in the spirit of the ISRI reports' failure analyses | scratch `shape_probe.py` (crop grids in the session scratchpad) | Every letter→letter substitution on aligned lines classified by the glyph that reached the classifier. dev-8 (clean): 152 errors — whole 59, broken 8, merged 8, bloated 7, eroded 1, unaligned 69. broad-30: whole 463, broken 47, bloated 22, merged 16, eroded 14, **unaligned 1,299** — 'unaligned' means the line used split/merge variants or dash scrubs, so 70% of degraded-page shape errors sit on lines where segmentation was already active. CROP GRIDS overturn the 'degraded glyph' story for the aligned part: the whole-glyph errors are mostly LEGIBLE — heavy sans capitals from letterhead faces (F→r, Y→o, N→c, A→s, P→F, D→G: a bold grotesque not in our 23-face stock), 'fi'/'ffi'/'fl' LIGATURES (single components with no class: 'f'→'i', 'f'→'h'), italic serif merges ('rve', 'rvi'), and filled/eroded bowls in body text (e→o 7, a→e 5, e→a 5, c→s 5, s→e 4). Consequences: (1) the outline channel below targets the bowl family and the unseen-bold family; (2) ligature classes are a missing piece with a clear signature; (3) the 'unaligned' 70% says the segmentation lattice, not the classifier, is the biggest single lever left on degraded pages — the same conclusion the decomposition reached from the other side. |
| ✅ Outline-segment features, many-to-one matched (Tesseract §5 re-derived) | R. Smith, "An Overview of the Tesseract OCR Engine", ICDAR 2007, §5.1–5.2 and §6; source read: `src/classify/intfx.cpp` (feature extraction: normalization by centroid and second moments to a 256-unit space, features as fixed-length outline pieces of 64/5 units with position and direction), `intmatcher.cpp` (evidence per feature–proto pair from a Gaussian-like table of point-to-line distance and angle difference; per-config feature evidence = best proto; per-proto evidence = best L matches for a proto of length L; `NormalizeSums` divides by features + proto lengths), `intproto.h` (protos as (A,B,C,angle) lines with configs). PDF fetched to `docs/papers/refs/` (not committed). | `recognize/outline.py`, `scripts/build_outline_protos.py`, `recognize/stage.py` `outline_path` / `outline_weight` / `outline_top`; test `tests/test_outline.py` reproduces Smith's Fig. 6 (a cut 'h' still rates 'h' above 'b', 'n', 'k') | Readable numpy re-derivation: skimage contours → moment normalization → fixed-length oriented pieces (features) and polygon-approximated segments (prototypes, one configuration per clean font render, 3,080 configurations over 110 classes from 28 fonts — clean renders only, as Tesseract trained, §5.3). Evidence e = exp(−(d/σd)²−(Δθ/σθ)²) with d the distance to the SEGMENT (Tesseract bounds its infinite-line distance with the proto pruner's position buckets); ratings vectorized over all configurations of a class with `reduceat`. σ study on the cut-'h' case: tight widths (10, 0.45 rad) rated everything ~0.2 and let 'b' win; (35, 0.7) rates the cut 'h' 0.75 as 'h' vs 0.71 as 'b', a clean cross-font 'h' 0.81 vs 0.73. Used as a THIRD opinion in the same additive form as the MLP: cost' = cost + w·(oc − min oc over the top candidates); scale measured on a real page — prototype candidate gaps ~20 units vs outline gaps ~0.07, so w=100 ≈ half a vote. On real glyphs its top-1 agrees with the prototype top-1 79% of the time. MEASURED: synthetic 98.2/93.7 · 98.4/94.7 · 97.4/90.8 → **98.3/94.2 · 98.6/95.6 · 97.5/91.3** (w=100; w=200 loses sev2); dev-8 92.7/80.8 → 92.8/80.5 (w=100), 92.5/79.7 (w=200). Cost ~48 ms/glyph at 14 candidates, capped to the top 6. WEIGHT SWEEP on the pipeline: w=50 dev-8 **92.8/81.2** (recall 88.0, precision 85.3), broad-30 **87.1/68.7**, recall 78.9, precision 77.1 — every column up; w=100 broad-30 87.1/68.6, dev-8 92.8/80.5; w=200 dev-8 92.5/79.7. Synthetic at w=50: 98.3/94.2 · 98.5/95.1 · 97.4/90.8. Adopted as default at w=50 (`data/outline_protos.npz`, ~2 s per page at six candidates). Day-2 scoreboard: broad-30 87.1/68.7 against legacy 95.3/90.5. |
| 🔬 Ligature classes (fi, fl, ff, ffi) — NEGATIVE for now | Ligature modelling as in Tesseract's unicharset (which carries fi/fl classes and expands them) | `factory/stock.py` `LIGATURES` (defined, not in CHARSET), `factory/synth.py` `glyph_available`, NFKC expansion in `decode/beam.py` `_core` and `decode/output.py` | Motivated by the shape-residual crops (single-component 'fi'/'ffi' decoding as 'i'/'h' with the 'f' deleted). Built as classes from the 28-face stock (fonts lacking the glyph render a fallback box, detected by comparing the ligature's ink width with its NFKC expansion; fi/fl available in ~all faces, ff/ffi in about half): 114 classes, variant prototypes + MLP. MEASURED on dev-8: 20 glyphs decoded as ligatures, of which THREE were ligatures — the rest signature scribbles, a blackletter initial, merged fragments ('Dese', '00'): the widest classes became a junk magnet; dev-8 92.7/80.8→92.5/80.3, synthetic sev0 98.2/93.7→98.0/92.7. Not adopted; the expansion plumbing stays so a gated version (ligature admitted only when the expanded word is lexicon-endorsed, or only from the outline channel's rating) can be tried cheaply. |
| 🔬 Adaptation-pin strength sweep — NEGATIVE (pins stay as they are) | Adaptive classifier confidence gating (cf. Tesseract's adaptive classifier and its 'good word' criteria, Smith 2007 §7) | `adapt/cluster_refit.py` `member_gate` (new, default off), `min_purity`, `decode/beam.py` `pin_bonus` | A truth-labeled probe on three non-evaluation pages (3,600 glyphs, 71 pipeline errors) found 73% of the residual errors carried an adaptation pin to the wrong class, while the recognizer's raw top-14 held the truth for 73% of them. Three ways of trusting pins less were measured on dev-8 (baseline 92.8/81.2): pin only members whose OWN candidate list contains the voted class → 92.8/81.1 (flat); pin bonus 2.5→1.5 → 92.5/80.5; cluster purity 0.7→0.85 → 92.6/80.8. Pins are present in most residual errors because pinning is what repairs the majority; every way of weakening them loses more than it saves. The residual truth sits at ranks 7–14 of the candidate list, so the lever is a better ORDERING of that list, not a weaker pin. |
| 🔬 One-base dot association — NEGATIVE; the 'i' errors are touching pairs | Diacritic/base association by overlap (Smith 2007 §3.1) | `glyph/components.py` `_group_overlapping` (reverted to the single overlap rule) | The provenance-based shape probe on broad-30 (1,885 aligned shape errors: 73% whole single components, 21% broken, 2% merged-by-aspect) put truth 'i' at the top of the confusion list — read as d, A, s, n, e, N over 120 times — with accented letters among the candidates, which suggested the i-dot welding its stem to the next letter through the union-find. Restricting small marks to attach to ONE base (most overlap) was measured three ways: 'any overlap' glued punctuation under letter overhangs (dev-8 92.8/81.2→92.3/79.0); with the half-width requirement, separating small marks from each other broke colons, quotes and broken small letters (same numbers); with small–small unions restored, dev-8 92.8/80.8 and **broad-30 87.1/68.7→86.9/68.0**. Reverted. The crop grid then showed the true mechanism: the 'i' errors are TOUCHING PAIRS in a bold serif face — 'ti', 'li', 'fi', 'ri', 'is', 'in', 'wi' as single connected components that the split lattice never cut (their width is only ~1.2–1.5× the line's median, under the suspect threshold), so a two-letter blob decodes as one letter. This is the chopper (Smith §4.1), not association; see the split-threshold work that follows. |
| ✅ Touching-pair split suspects: threshold 1.5→1.3 | Chopping of joined characters (Smith 2007 §4.1: chops proposed generously, undone when confidence does not improve) | `glyph/components.py` `split_width_factor` | The provenance probe's top confusion was a bold serif face's touching pairs ('ti', 'li', 'fi', 'ri', 'is', 'in', 'wi') read as one letter: at 1.2–1.5× the line's median width they never became split suspects. A suspect only ADDS a hypothesis — the decoder still chooses, with the split bonus — so the threshold can be generous. Sweep: dev-8 1.5 → 92.8/81.2, **1.3 → 92.8/81.3** (recall 88.3, precision 85.5), 1.15 → 92.7/80.9; broad-30 1.5 → 87.1/68.7, **1.3 → 87.3/69.3**, recall 78.9→79.7, precision 77.1→77.8. A second, targeted proposal (`split_under_dot`): a dot-sized part over one side of a body wider than 1.1× median marks an 'i' or 'j' touching its neighbour, and the cut is looked for at the dot's edge — alone broad-30 87.4/69.3; together with the threshold **87.4/69.5**, recall 80.0, precision 78.1 (dev-8 92.8/81.1–81.3, synthetic unchanged). Both adopted. |
| ✅ Chopper: ranked cut options, three-piece splits, fixed-pitch guard | Chopping joined characters with several candidate chops tried in priority order (Smith 2007 §4.1: up to three chop pairs, undone when confidence does not improve; our cuts are ink-profile minima rather than concave outline vertices) | `glyph/components.py` `_cut_candidates`, `split_cuts`, `split_triple`; `recognize/stage.py` scores every piece of every option; `decode/beam.py` `_decode_word` enumerates one option per suspect, best-first, under `max_split_variants` | Before: one cut per suspect at the global ink minimum, two pieces. In a touching pair the kiss is often the second-lowest column (the lowest sits inside a bowl or under a serif), and three touching letters ('rti') cannot be read at all. Now up to three local minima (at least a piece apart) each give a two-piece option, and the best cut's still-wide side gets a second cut for a three-piece option; the split bonus counts the extra characters actually produced. MEASURED (all with threshold 1.3): three cuts + triples → dev-8 92.9/81.2, broad-30 87.2/69.8 (recall +0.7), legal-8 85.9/65.5; ablation: one cut + triples → **broad-30 87.5/69.9**, recall 80.6, precision 78.5, **dev-8 93.0/81.7**, recall 88.8, precision 85.9 (the best dev-8 and broad-30 yet); three cuts without triples → broad-30 87.2/69.4. So the THREE-PIECE option carries the gain and extra cut positions add nothing; defaults `split_cuts=1`, `split_triple=True`. The legal-8 loss (86.5→85.9) traced NOT to the chopper but to the earlier 1.5→1.3 suspect threshold: typewriter pages are fixed-pitch, every wide letter is 1.3× the median, and legal had not been re-measured after that change. Fixed-pitch detection (page median of per-line component width CV: legal 0.17 vs letters 0.29, threshold 0.22 — Smith §3.3 separates fixed-pitch text the same way, by pitch) keeps the 1.5 threshold on such pages. On the legal EVALUATION pages, though, the width statistic read 0.22–0.47 and fired on two of eight — raw component widths mix punctuation and broken pieces — so the `doc_type` hint (legal filings are typewriter pages) is the surer trigger, and either may fire. Legal-8 isolation: dot-guided cut off → 85.8/65.1 (no change), triples off → 85.9/65.0 (no change), fixed-pitch threshold via doc_type → **86.1/65.5**. dev-8 93.0/81.7 and broad-30 87.5/69.8 unchanged by the guard. The last 0.4 of legal's drift from 86.5 predates the chopper: the outline channel, adopted on letter and synthetic evidence, was never measured on legal until now — without it legal-8 reads 86.3/66.5, with it 86.1/65.5. A 0.2 char / 1.0 word domain trade for +0.1/+0.4 on dev-8 and the synthetic gains; kept, recorded, and a candidate for a doc_type-aware weight. |
| ✅ Per-blob confidence-driven chopping — neutral at first, adopted after the ensemble sharpened | Smith 2007 §4.1: "while the result from a word is unsatisfactory, Tesseract attempts to improve the result by chopping the blob with worst confidence"; "any chop that fails to improve the confidence of the result is undone" | `recognize/stage.py` `_chop_on_confidence` (`chop_on_confidence`, `chop_distance_factor`, `chop_min_width_frac`) | Truth-set analysis of the 41% of errors whose truth lies OUTSIDE the candidate list: whole-read glyphs of truth 'i' (293), 't' (99), 'l' (85), 'r' (62), 'f' (50) decoded as d/a/n/h/u/m — two narrow letters touching are no wider than an 'm', so no width threshold can flag them (the 1.3 rule already caught the wide pairs). Second pass in the recognizer: a group with no split hypothesis whose top-1 distance exceeds 1.3× the page median and whose width is at least 0.9× the line median gets one cut at the ink minimum; its pieces are scored by every channel and the decoder chooses. UNGATED, the pass cut clean glyphs apart on the synthetic suite (sev0 char 98.8→98.1); with Smith's second clause — the hypothesis survives only if BOTH pieces match better than the whole did — synthetic is fully restored. MEASURED (gated): dev-8 93.0/81.7→92.9/81.6, broad-30 87.5/69.9→87.6/69.8 (recall +0.3), legal-8 86.1/65.5→85.9/64.8; ungated far worse everywhere (broad-30 86.6/68.5). Why it cannot work as a per-blob rule: a touching 'li' matches 'u' or 'h' WELL, so its distance never exceeds the page median — the blobs that need chopping are exactly the confident ones. Smith's trigger is at the WORD level ("while the result from a word is unsatisfactory"), which needs the lexicon verdict; that version is the chop stage that follows. Kept as an opt-in. RE-MEASURED 2026-09-04, after font-stock widening and the outline channel: modern 66.2→66.8 word (+0.7 recall/precision; 'Depatment:' → 'Department:' on every payslip — the touching 'rt' in Helvetica is a 3-px bridge the width rule cannot see), broad-30 88.3/72.7→88.5/73.1, legal-8 86.9/68.5→87.0/68.8, dev-8 94.1/84.8→93.9/84.7. Three sets up, one −0.1: ON by default. Lesson: a chopper is only as good as the distances that trigger it — the same rule that was inert against the old prototypes trips correctly once the channels separate 'rt' from 'd'. |
| 🔬 Word-level chop stage (Tesseract's trigger, faithfully) — INERT, opt-in | Smith 2007 §4.1: chop the worst blob while the WORD is unsatisfactory; undo chops that do not improve confidence | `recognize/chop.py` (slot `chop`, impl `unendorsed`; not in the default pipeline), `recognize/stage.py` `score_crops` | Between the two decode passes: every word the lexicon did not endorse, or endorsed below decode confidence 0.15 (the lexicon pass endorses near-misses — on one page 'mIl', 'Wea', 'SCOtch', 'Spar' all under 0.12 while right words sit at p50 0.28), has its worst-matched whole-read groups cut at the ink minimum, the pieces scored through every channel, and the hypothesis kept only if both pieces beat the whole. RESULT: dev-8, broad-30 and legal-8 IDENTICAL to the reference to the decimal (broad-30 87.5/69.9). Counts across dev-8 with the gate OFF: 296 unsatisfactory words, 397 hypotheses, **9 chosen by the decoder**. The trigger is now right and the cut position is not the blocker: the PIECES are — a cut piece carries a cut edge the whole-glyph prototypes never saw, its candidate list is wrong, and the split reading loses the beam to the whole. Consistent with the truth set (cut pieces err 13.9% vs 2.7%). The lever is piece-aware scoring: outline evidence over the un-cut sides only, or prototypes rendered with cut edges. Stage kept as an opt-in so that work has its harness. |
| 🔬 Piece-aware scoring — NEGATIVE, opt-in: cut-edge masking and all-class outline rating for chopped pieces | Partial-shape matching — the outline features' original purpose (Smith §5.1: unmatched features and prototypes cost only their own share) applied to a deliberately incomplete glyph | `recognize/outline.py` `outline_features(cut_edges=…)`, `OutlineMatcher.costs_all`; `recognize/stage.py` `_piece_outline` (`piece_outline`, `piece_inject`) | A chopped piece has a straight cut edge no prototype has, and its whole-glyph 95-vector candidates are unreliable (cut pieces err 13.9%; the word-level chop stage saw 9 of 397 hypotheses accepted). Now every split piece drops the outline features within two pixels of its cut side(s) and is rated by the outline channel against ALL classes; the outline's top three join its candidate list and the additive re-cost applies. Cost ~350 ms per piece (pieces only). Synthetic unchanged. MEASURED: dev-8 93.0/81.7→92.6/80.7, broad-30 87.5/69.9→87.1/69.2, legal-8 86.1/65.5→86.3/65.0. Rating a small piece against all 110 classes produces confident wrong classes (a fragment of 'm' rates as 'n' or 'r' better than anything), and injecting them at the outline weight wrecks pieces that the prototypes had right. Off by default. The chopper series' honest sum: three-piece splits (+0.4 word broad-30) are the only positive; per-blob trigger, word-level trigger and piece-aware scoring all measured inert or negative. Cut pieces remain the weakest population, and the next attempt should change what a piece IS (cut at concave outline vertices so the edge falls in a natural neck, or prototypes rendered from cut synthetic pairs) rather than how a piece is scored. |
| 🔬 Concave-vertex cut placement — NEGATIVE on real scans, opt-in | Smith 2007 §4.1: chop points are concave vertices of the polygonal outline approximation, paired with a concave vertex opposite | `glyph/components.py` `_concave_cuts` (`cut_method`, `cut_tol`), ink minimum as fallback | The outer outline (largest enclosed area) is polygon-approximated; a vertex is concave when its turn opposes the outline's winding; pairs of concave vertices one above the other within a quarter of the blob height mark a NECK, scored by neck length plus horizontal offset, and the cut goes through the neck's midpoint. On real width-flagged suspects (6 pages, 375 blobs) a concave pair exists for 83%, and the chosen column differs from the ink minimum by 7 px median (17 px at p90) — the two rules genuinely disagree. Synthetic: tolerance 1.5 px hurt the degraded severities (sev1 word 96.6→94.7: noise vertices form spurious pairs); tolerance 3.5 recovers it (98.9/95.6 · 99.1/96.6 · 98.1/91.7) but does not beat the ink minimum (98.8/95.6 · 99.3/96.6 · 98.3/92.2). REAL PAGES, both tolerances: dev-8 93.0/81.7→91.6/78.7 (1.5) and 91.9/79.3 (3.5); **broad-30 87.5/69.9→86.0/66.2 and 86.0/66.4**; legal-8 86.1/65.5→84.4/62.9 and 84.9/63.0. Decisively worse. Smith's chop points come from outlines of clean 300 dpi originals traced by Tesseract's own edge tracker; on our bitonal photocopy scans the polygonal outline is ragged and a 'neck' between two concave vertices is far more often a pair of noise pits than the kiss between letters — the ink-minimum column, which integrates the whole column, is the robust statistic here. Default back to "ink"; the rule stays as `cut_method="concave"` for cleaner sources. This closes the chopper series: three-piece splits positive; per-blob and word-level triggers inert; piece-aware scoring and concave placement negative. The remaining piece-quality idea is prototypes rendered from cut synthetic pairs, so cut pieces have neighbours; everything else on the chopper has now been measured. |
| 📊 Modern test set: first numbers, and a rasterization lesson | Test-set construction from born-digital documents; print simulation (dot gain) as in document-degradation modelling (Baird 1992; Kanungo et al. 2000 model the scanner, not the printer — the printer mattered here) | `scripts/make_modern_set.py`, `data/modern/` | FIRST RUN (35 govinfo PDF pages rasterized straight to 300 dpi + 24 templated invoices/payslips/letters in Helvetica Neue, Avenir, Arial Narrow, Helvetica; clean): ours 57.5 char / 28.7 word / 55.6 recall / 51.7 precision vs legacy Tesseract 67.0 / 61.9 / 85.7 / 92.9. Modern letters read at 95–97 char in all four faces — the modern-font fear is not borne out on sans letters. The congressional BILLS were catastrophic (recall 12–22%): their face (a Century-like high-contrast serif) has hairlines thinner than one 300-dpi pixel, which poppler anti-aliases to light grey or nothing, so 'and for other purposes.' (20 glyphs) binarizes to 78 components at Sauvola k=0.2, 110 at a plain 0.5 threshold, 114 unantialiased — every letter shredded before any stage of ours ran. Not a split-lattice fault (all split toggles ±1 point), not a stroke-normalization fault (top-1 garbage with it off too), not fixable by the threshold alone (k 0.05–0.1: 30–45 char). Real print does not do this: toner dot gain spreads ink about a 600-dpi pixel. PRINT MODEL adopted for the PDF pages: render at 600 dpi, one-pixel grey minimum filter, area-downsample to 300 → 19 components for those 20 glyphs, median stroke 2 px, like a real scan of the page. The bill page then reads 18.2→68.4 char, recall 22.5→54.4, body text intact; residue is letter-spaced headings and marginalia. TWO LESSONS: (1) a born-digital raster is not a scan — a test set needs a printer model as much as a scanner model; (2) legacy Tesseract read the SHREDDED pages at 86% recall, so fragment association (Smith §4.2) is a real gap of ours regardless of the set's fidelity — the merge lattice joins adjacent pairs only. Real-page numbers on the rebuilt set follow. |
| ✅ Fragment associator: merge runs and k-best segmentation search | Smith 2007 §4.2: the associator searches the graph of combinations of maximally chopped blobs (A*, best-first); here a k-best dynamic program over the piece sequence | `glyph/components.py` `merge_max_pieces` (runs of 2–3 pieces as one hypothesis), `recognize/stage.py` (scores every run), `decode/beam.py` `_merge_paths` | Before: one hypothesis per piece ('this plus its right neighbour'), and the decoder tried SUBSETS of those under a cap of eight — a word shredded into ten fragments could never be reassembled, and the modern bills (hairline serif) and UNLV's broken-hairline page are exactly that. Now every run of 2..3 consecutive pieces whose union is one character wide (≤1.05× median height), whose gaps are small and which contains a sub-glyph piece is scored; the decoder finds the k best segmentations of the word into singles and runs by summed best-candidate cost, keeps the all-singles path first (ranking is for truncation, the beam decides), and combines them with the split variants as before. MEASURED: broad-30 87.5/69.9→**87.7/70.1**, recall 80.6→80.9, precision 78.5→78.7; dev-8 93.0/81.7 unchanged; legal-8 86.1/65.5→86.3/65.7; synthetic unchanged. Adopted. On the SHREDDED bill render (35 pieces for 20 glyphs) it does not help (recall 22% either way) although twelve merge hypotheses exist on the test line: a letter in three pieces merged two at a time still scores badly against whole-glyph prototypes, and fragments that overlap horizontally (negative gaps) were never grouped — the same piece-scoring wall the chopper series hit, now from the merge side. Legacy reads that render at 86% recall, so the wall is real; the print model makes it rare in the modern set. |
| ✅ Outline-channel confidence gate (2.4x faster, accuracy up on the headline) | Cascade evaluation: spend the expensive opinion only where the cheap one is unsure (the same gate the GED rerank already used) | `recognize/stage.py` `outline_margin` | The outline channel costs ~30 ms per glyph and dominated runtime: a three-column Federal Register page took ~5 minutes, and a 59-page set two hours per severity. But the prototype channel is already sure for most glyphs: on a real page, 80% have (d2-d1)/d1 > 0.3. Rating only the rest measured, on real pages, NOT worse but slightly better: broad-30 87.7/70.1 → **87.7/70.4** (recall 80.9→81.0, precision 78.7→78.9), legal-8 86.3/65.7 → **86.5/66.2**; dev-8 93.0/81.7 → 92.9/81.3 and synthetic sev0/sev1 word −0.5 (the tuning set and the clean renders lose a little). Margin 0.6 (76% skipped) measured identical to 0.3 on all three real sets, so the faster gate wins. Whole-suite timing: synthetic evaluation 94 s → 40 s. Default 0.3. Plausible mechanism for the gain: on confident glyphs the outline opinion could only add noise, and its cost re-ranking occasionally displaced a correct top-1. |
| 🔬 Self-trained glyph CNN as a fourth opinion — NEGATIVE in the pipeline, off by default | Convolutional classification of glyph images (LeCun et al. 1989 on hand-designed inputs; here trained from our own renders and our own truth-labeled scans, ~30k parameters of numpy, no framework) | `recognize/cnn.py`, `scripts/train_cnn.py`, `recognize/stage.py` `cnn_path` / `cnn_weight` / `cnn_inject` | Motivation: convolution is local, so a break costs a few filter responses rather than every feature — the property the outline channel has without its fragility on ragged outlines. Trained in 11 min on 151k crops (24.6k renders through the degradation stack + 134.6k truth-labeled real crops); **95.4% top-1 on a PAGE-DISJOINT holdout** (a random 5% split flatters it: crops from the same page are near-duplicates). On the glyphs the pipeline reads right it agrees 99.1%; on the glyphs it reads wrong it was 62% right — but those crops were in its training set, so that number is memorization-inflated. IN THE PIPELINE: with class injection (its top-3 added to the candidate list, as the MLP does) it was catastrophic at EVERY weight — dev-8 92.9/81.3 → 86.7/71.6 at w=2, 88.4/73.0 at w=0.1 — because a CNN reading an image proposes classes the prototype channel rejected for good reasons (junk crops, split pieces), and they enter near the best cost; the MLP's injections are safe only because it reads the same 95 features and proposes the same neighbourhood. As a pure re-coster (`cnn_inject=0`): dev-8 93.0/81.6 (+0.1/+0.3), synthetic flat, but **broad-30 87.7/70.4 → 87.6/70.0 and legal-8 86.5/66.2 → 86.3/65.2**. Not adopted. THE LESSON IS ABOUT THE ENSEMBLE, NOT THE MODEL: a fourth opinion on the same crop cannot help when the residual errors are decided elsewhere — the truth set says 68% of them carry a wrong adaptation pin, 52% sit in a lexicon-endorsed word, and the truth is outside the candidate list for 41%, i.e. the glyph was mis-segmented before any classifier saw it. Kept selectable for the day the segmentation frontier moves. |
| 📊 Modern set, measured: we beat legacy on characters, lose on recall | Paired evaluation on today's documents (`data/modern`, print model per the entry above) | `scripts/eval_unlv.py data/modern/sev0`, `scripts/eval_tesseract.py` (now prints per page, so both engines parse alike) | 59 clean pages: ours **73.8 char** vs legacy 67.4, but word 47.6 vs 62.2, recall 70.1 vs 85.7, precision 69.4 vs 93.1. BY FAMILY (ours / legacy, char and recall): modern LETTERS 95.7/87.8 vs 99.7/98.5 — modern sans is not the problem; congressional BILLS 73.0/62.2 vs 76.2/69.2; FEDERAL REGISTER 63.8/79.5 vs 67.7/94.3; INVOICES **78.5**/63.2 vs 41.9/95.7; PAYSLIPS **64.8**/68.0 vs 34.7/97.8. The inversion on invoices and payslips is the whole story of this set: legacy reads nearly every word (96–98% recall) and emits them in an order that destroys edit distance, while we order sparse column layouts well and MISS a third of the words. So on business forms our reading order beats legacy's and our recall is far behind. |
| ✅ Short numeric cells are data; margin line numbers are apparatus | Table-cell vs. page-apparatus discrimination (line-number columns are a standard special case in legal-document OCR) | `decode/output.py` `keep_short_numeric`, `_line_number_column`, `line_number_doc_types` | Diagnosing the invoice recall gap: every quantity cell was being deleted. A one-character line ('9') trivially repeats 100% of itself, so the garbage rule's shape test (one character supplying ≥40% of the line) fires on every short cell. FIRST FIX, a blanket 4-character floor on that test: modern recall +1.1 but −0.2 char on all three scan sets (short junk on photocopies is real: 'u5', 'x', 'c'). SECOND, narrower: exempt only lines of ≤3 characters that are ALL DIGITS — modern recall 70.1→71.4, broad-30 unchanged at 87.7/70.4, but legal-8 86.5→84.6 char, because a pleading numbers every line down the margin and UNLV's ground truth omits those numerals. THIRD: detect the numbering column structurally (≥6 single-token numeric lines in one 40 px x band, ≥70% ascending) and suppress it. That fixed legal (86.7/66.7, its best) but cost the modern set 2.6 recall — because a congressional bill's line numbers ARE in its ground truth. The conflict is a consumer convention, not a fact about documents, so it is a parameter: `line_number_doc_types="legal"`. FINAL, all three at their best: modern 74.2 char / **71.4 recall**, legal-8 **86.7/66.7**, broad-30 87.7/70.4, dev-8 92.8/81.3. |
| ✅ Font-stock widening (adopted): six modern sans faces, all channels rebuilt | Re-running the widening experiment under the CURRENT consumers (per-class condensation + MLP + outline), as the roadmap predicted | `scripts/build_prototypes.py --add-fonts` | Every earlier widening failed under the capped 1-NN pool (a pure append of six faces cost letters −1.3 char). Repeated now with six modern sans faces that the modern set does NOT use (Trebuchet MS ×3, Geneva, Avenir Next ×2 — Helvetica Neue, Avenir, Helvetica and Arial Narrow stay held out): modern 73.8→**75.5** char, recall 70.1→**72.8**; dev-8 92.9→**93.4** char, recall +0.6; at 90 prototypes per class instead of 60, modern **75.8 char / 73.9 recall / 71.3 precision** (its best) — the first widening in the project's history to help. BUT broad-30 word −0.6 / precision −0.9 and **legal-8 86.5→85.1 char (−3.4 word)**, only partly recovered by the larger budget (85.4/64.2). Font ROUTING is not the cause: legal pages still vote mono with higher confidence and their distances IMPROVE (10.25→8.54). The cost is elsewhere — a denser pool compresses the distance scale that the graphic gate, the softmax spread and the adapt calibration are tuned against. THE TRADE WAS AN ARTEFACT OF A MISMATCHED ENSEMBLE. Those measurements swapped the prototype file while leaving the MLP and the outline bank built from the OLD stock: three channels disagreeing about what a face looks like. Rebuilding all three together (`build_prototypes --condense 90`, `pool_all` → `train_mlp`, `build_outline_protos`) removes the loss entirely and every real set improves: modern 74.2/47.3/71.4/69.3 → **76.0/50.0/74.6/71.7**; broad-30 87.7/70.4 → **88.2/71.0** (recall +0.5); legal-8 86.7/66.7 → **87.0/67.0** (recall +1.8); dev-8 92.8/81.3 → **93.6/82.1**. Synthetic, whose face is held out, loses ~1 word point at the two lighter severities and gains 0.5 at the heaviest. Adopted: the six faces are in `factory/stock.py` and the live build is 90 prototypes per class. LESSON: the three recognition channels are one model — rebuild them together or measure a chimera. |
| ✅ Sparse column layouts: numbers split at separators, and the 2-means gap band on short lines | Word segmentation from gap statistics (the classic bimodal-gap method) and its failure on lines with a single word gap; numeric-format endorsement as lexicon | `decode/beam.py` `numeric_join` (an uncertain gap the variant search reads both ways, scored with `numeric_endorsed`), `digit_mode_separators`, and the gap-band sanity check in `_segment_line` | Word-by-word diagnosis of an invoice after the stock widening left 18 missing words in four families: MONEY split at the thousands comma ('$7,165.00' → '$7,1' + '65.00'); DATE slashes read as '1' ('09/01/2024' → '0910112024', because digit mode boosted '1' and not '/'); WORD FRAGMENTATION ('Project management' → 'Proj act management'); and '@', which is not a class at all. (1) A FORCED join across a wide gap after a digit-separator-digit pattern recovered the money on the invoice (82.2→88.1 char, ~74→81.7 recall) but cost 0.4 word on every scan set, and a width guard on the separator recovered only 0.1 of that. Rewritten as the pipeline's own idiom — the gap becomes UNCERTAIN and the variant search reads it both ways, with a whole-shape numeric format counting as lexicon quality — it keeps the invoice gain. (2) Number separators (/ - . , : $ %) share the digit-mode boost. (3) THE BIG ONE: on 'Project management' the gaps are 0–4 px with one 14 px word gap; 2-means with a single outlier splits the letter gaps among themselves and calls 3 px a word space on a 21 px x-height line. A band whose 'word space' is narrower than the minimum plausible one (space_lo × x-height) is spurious and falls back to the x-height ratios. Invoice recall 81.7→**88.2**, payslip 67.1→**76.7** on the spot-check pages. FULL SETS, all three changes (separator boost; numeric join as an uncertain gap, context-gated to data lines — '$'/'%' or two digit-separator-digit triplets — because ungated it cost dev-8 0.5 word even when deferred; gap-band sanity check): dev-8 93.6/82.1/88.9/85.8 → **94.1/84.7/89.3/88.3**; legal-8 87.0/67.0/82.4/79.3 → 86.9/**68.5**/81.8/**80.3**; broad-30 88.2/71.0/81.5/78.7 → **88.3/72.5**/80.7/**80.9**; modern 76.0/50.0/74.6/71.7 → **77.9/62.2/78.0/82.3** — word accuracy +12 and precision +10.6 on today's documents, because money amounts stopped shattering into fragments. Synthetic unchanged. The recall dip on broad-30 (81.5→80.7) against precision +2.2 is the gap-band fallback merging a few true word pairs on scans; word accuracy, which counts both, is up 1.5. |
| 🔬 '@' as a class — NEGATIVE (gate it, don't add it) | Charset design: every added class competes with its lookalikes on every glyph | `factory/stock.py` (not in CHARSET), `factory/synth.py` `glyph_available` (now rejects fallback boxes for '@' too) | E-mail addresses read as 'billing0example.com' because '@' was never a class. Added (13 of 29 stock faces draw it), all three channels rebuilt (111 classes): the address reads, and dev-8 loses 0.9 word (94.1/84.7→94.1/83.8), legal-8 −0.5 char, and the sample invoice 4.3 RECALL (88.2→83.9) — a large round glyph with an inner loop is a plausible neighbour for a, e, 0, O and ©-like junk on every page, and it wins some of those contests, while there is one address per document to gain. Same lesson as the ligatures: a rare class must be admitted by CONTEXT (inside an e-mail pattern: token contains letters, a '.', and the candidate sits between them), not offered everywhere. Not adopted. |
| ✅ Digit-run continuation and the e→3 twin | Same numeric-continuation idea; confusion twins for digit mode (as l→1, o→0 already) | `decode/beam.py` `numeric_join` (digit\|digit gap inside a token that already holds a separator), `DIGIT_TWINS` e→3 | Two residues on the payslip: '06/15/2025' split into '06/1' + '5/2025' — a narrow '1' leaves a 10 px kerning gap that lands just above the band's 9.8 px word threshold, and the join rule only fired after a separator; and every '3' in Avenir read as 'e' ('$116.36' → '$116.e6', '36.25' → 'e6.25'), a geometric-sans shape with no twin in the table. Fixes: a digit\|digit wide gap inside a token that already carries a numeric separator continues the token (data lines only); 'e' joins the digit twins so digit mode can yield to '3'. Spot checks: payslip recall 76.7→**87.7**, invoice 88.2→**90.3**. FULL SETS: dev-8 and legal-8 unchanged to the decimal, broad-30 88.3/72.5→88.3/72.6 (precision 80.9→81.0), modern 77.9/62.2/78.0/82.3→**78.0/62.8/78.5/82.7** (old scorer). Small in aggregate because only the 16 business pages carry dates and amounts in these faces; on those pages it is the difference between a readable payslip and not. |
| ✅ Scorer folds typographic punctuation | Evaluation hygiene: score conventions the same for both engines (ISRI's accuracy tools likewise normalize before alignment) | `scripts/eval_unlv.py` `normalize` (shared by `eval_tesseract.py`, `harvest_truth.py`) | The modern set's truth comes from PDF text layers and carries curly quotes (‘‘ ’’ “ ” ’), en/em dashes and non-breaking spaces; UNLV truth is ASCII. Neither engine has classes for the typographic forms, so every one was scored as a substitution against both. Folded to ASCII before alignment — quote pairs first, then single characters. UNLV numbers are unchanged (no such characters in that truth, verified); modern numbers rise slightly for both engines: legacy 67.4/62.2/85.7/93.1 → 67.7/63.1/86.8/94.3; ours: see the follow-up entry once rescored. |
| 📊 Federal Register family: the gap is small type and footnotes, not columns | Diagnosis by word-set difference on one page | scratch `missing.py` | On FR page 9 (three columns, 8-pt body) we emit 1,198 words for 1,254 in the truth with all three columns segmented (79/67/70 lines), so nothing is lost as a block; recall 79.5 against legacy's 94.3 is (a) misreads of 8-pt body type and 6-pt footnotes at 300 dpi (x-height ≈ 12 px; the suppressed lines are footnote gibberish: 'precipation in overvinorisma'), (b) URLs and legal citations ('234.3(a)(17)(vii)', 'https://www.federalreserve.gov/…'), and (c) the truth itself: the PDF text layer includes the vertical marginal slug ('VerDate Sep<11>2014 … E:\\FR\\FM\\15MRR1.SGM') that no OCR of the page image can read, plus '§' which is not a class. Small type at 300 dpi is the same frontier the newspaper sets showed (RESEARCH, resolution study); the honest lever is 400 dpi input or the print-and-scan loop, not decoding. |
| ✅ Modern-set truth cleaned: production slug and glued line numbers | Ground-truth hygiene (ISRI zoning practice excludes page apparatus the OCR is not asked to read) | `scripts/make_modern_set.py` `clean_truth` (unit-tested) | The PDF text layer of every govinfo page carries a 4-pt VERTICAL production slug along the margin ('VerDate Sep<11>2014 03:20 Jan 14, 2023 Jkt 039200 PO 00000 Frm 00002 Fmt 6652 Sfmt 6201 E:\\BILLS\\H21.IH') — in the text, unreadable in any 300-dpi image — and bills' right-margin line numbers come out glued to the last word of each line ('jurisdic-17'), which on the page are two tokens. Both engines had been scored against that. Cleaned (slug lines dropped, trailing 1–2-digit numbers un-glued): one bill page 81.1/59.0/76.7 → 90.7/67.1/88.1 for us with no pipeline change; **legacy on the modern set 67.7/63.1/86.8/94.3 → 72.0/67.5/92.0/95.7**. Ours: **82.8/66.8/84.4/84.9** (from 78.0/62.8/78.5/82.7 under the dirty truth and old scorer). Against legacy on the cleaned set we lead by 10.8 char, trail word accuracy by 0.7, recall by 7.6 and precision by 10.8 — the modern gap is now mostly PRECISION (spurious tokens), no longer recall. A test set is only as honest as its truth; this is the second fidelity defect found in this one (the first was the print model), and both moved the numbers by more than most algorithm changes do. |
| 📊 Modern set after the sparse-layout work: where the remaining errors are | Bag-of-words difference aggregated per family (`scripts/missing_words.py` over every page) | scratch aggregation | BUSINESS PAGES (24 invoices/payslips/letters, cleaned truth): char 86.9 / recall 89.4 / precision 88.6. The 308 spurious tokens: 211 word-like misreads — almost all in the two HELD-OUT faces (Avenir: 'priya'→'priva', 'overtime'→'advedime', 'training'→'lraining'; Helvetica Neue: 'department'→'depatment') — 45 numeric, 42 fragments, 10 mixed. Top missing: 'priya' 9, '(8.25%)' 8, 'billing@example.com' 8, 'qty' 8, 'airlines' 8 (split 'air'+'lines'), 'department:' 8, '(415)' 8, '99.95%' 8, 'onboarding,' 8 (split 'onboard'+'ing'). Mechanisms, ranked: held-out-face glyph shapes (coverage, by design of the test); the '%' glyph (3 parts, a split suspect) losing to its own split into two boosted digits and ')' losing to a boosted '0' — fixed below; word splits where the variant search prefers a lexicon word plus a fragment because the whole ('onboarding', 'airlines') is not in the lexicon although its stem is; '@'; a bold 'Qty' header. BILLS (23 pages): char 88.3 / recall 80.6 / precision 82.1. Top missing 'of' 23, 'shall' 9, 'the' 9 against spurious 'd', 'o', 'sham', 're' 20, 's' 10 — the italic 'Be it enacted' lines whose letters touch ('of' arrives as one blob, 'll' reads as 'm'): the chopper's territory, not a class or lexicon problem; plus the page-foot slug '•HR 21 IH' (the bullet is not a class) and the ‘‘(i) item markers whose doubled quote reads as 'u'. |
| 📊 Bills: the missing 'f' of 'first' is italic touching letters, not a ligature | Diagnosis by group geometry | `scripts/missing_words.py` + a group-gap probe | 'first' → 'irst' twice on one bill page looked like the fi-ligature failure (RESEARCH, ligature classes). It is not: the first glyph of each 'irst' is a whole 's'- or 'b'-shaped group with no ligature candidate, and the line's groups are 52–70 px wide with 52 px word gaps — the italic 'Be it enacted…' lines are set in a face whose letters TOUCH, so each word arrives as one or two blobs, the split lattice cuts them, and the 'f' (whose hook overhangs the 'i') is lost in the cut. Chopper territory (the closed series), not a class gap. The gated ligature classes, rebuilt for this, therefore measured on the scan sets only as a safety check: dev-8 94.1/84.7→94.2/84.4, legal-8 86.9/68.5→86.4/67.8, broad-30 88.3/72.6→88.0/72.2. Negative again, gate or no gate; reverted, plumbing kept. |
| 📊 Truth-labeled real glyph set and channel attribution at scale | Ground-truth-aligned error analysis (ISRI practice), now per glyph | `scripts/harvest_truth.py` → `data/truth_en.npz` (134,838 glyphs from 120 non-evaluation letters, 3,035 line matches), `scripts/classifier_truth_eval.py` | Each decoded line matched to its truth line and aligned character by character through the new per-character provenance. RESULT: pipeline right on 96.8% of aligned glyphs (4,324 wrong). On ALL glyphs: prototype top-1 95.1, MLP 96.6, outline-among-top-6 95.4, truth within prototype top-14 98.5. On the WRONG subset: prototype 34.3, MLP 36.3, **outline 41.1**, truth within top-14 only **59%** — two errors in five have the truth outside the candidate list entirely. Attribution: 67.7% carry an adaptation pin to the wrong class (but every way of weakening pins measured worse — pins repair far more than they break), 52% sit in a word the lexicon endorsed, and only 6.2% are cases where the recognizer's top-1 was right and got overridden. BY READ KIND: whole glyphs err 2.7%, split pieces **13.9%** (4,678 pieces, ~650 errors — 3.5% of glyphs, 15% of errors), merges 42.7% (386). Top truth→decoded: ','→'.' 76, 'i'→'d' 73, 'i'→'a' 67, 'a'→'e' 61, "'"→'-' 56, 'i'→'l' 49, 'c'→'e' 47, '0'→'o' 44, '$'→'9' 34, '"'→'n' 32. CONSEQUENCES: (1) the 'i' family is the touching-pair chopper (fixed above, after this harvest ran); (2) split pieces need their own treatment — a cut piece carries a cut edge the whole-glyph prototypes never saw; (3) the truth labels cover punctuation, digits and capitals that the self-labeled harvest's lexicon gate never admits, so they are training data for every channel (`build_prototypes.py --truth`). |
| 📊 Truth-labeled exemplars as training data (prototypes + MLP) | Supervised training on aligned ground truth vs. self-training (the classic contrast; cf. Nagy's 'no shape training' line and its limits) | `scripts/build_prototypes.py --truth`, `data/prototypes_t.npz`, `data/mlp_t.npz`; `recognize/stage.py` `_family_mask` ("truth" tag = routing wildcard) | 134k truth-labeled glyphs (charset only; the ground truth's '~' unreadable marker and stray symbols dropped) merged with the synthetic renders and the self-labeled harvests, condensed to 60/class; MLP retrained on 325k exemplars (20 s). MEASURED with both truth models: dev-8 92.8/81.2→**93.3**/80.3 (recall 88.4, precision 85.5→84.2); broad-30 87.4/69.5→**87.8**/69.5, recall 80.0→80.7, precision 78.1→77.4; legal-8 86.5/66.5→85.5/65.2; synthetic 98.9/95.1 · 99.3/96.6 · 98.2/91.7 (sev0 word −0.5). Characters up on letters, words and precision down, another domain down: the truth glyphs are all business letters, and denser real exemplars lower every distance, including the junk lines the graphic gate used to catch. ABLATION: truth PROTOTYPES alone → dev-8 93.3/80.2 (precision 84.6), legal-8 85.8/64.0 — they carry both the character gain and the word/precision loss; truth MLP alone → dev-8 92.7/80.9, legal-8 86.8/66.8 — neutral to slightly positive. MECHANISM (dev-8 emission counts): with truth prototypes the graphic-suspect lines fall 37→24 and the suppressed lines 28→20, emitted words 1,628→1,649 against 1,583 true — denser real exemplars give junk lines (scribbles, rules) closer matches too, and the RELATIVE graphic gate (line median distance > 1.7× page median) no longer fires. Tightening the gate (1.7→1.5→1.35) with the truth prototypes: dev-8 93.1/80.0 → 93.0/80.0, legal 85.9/64.5 → 85.9/64.8 — precision does not come back and characters slip, so the gate is not the whole mechanism; the remaining word loss is unexplained (candidate-distance calibration is the suspect: denser pools compress distances and shift the pixel/LM balance). VERDICT: truth prototypes NOT adopted (character gain on letters, word and cross-domain loss); truth MLP neutral, not adopted. The truth set's value so far is measurement; its split-piece subset was tested next: adding only the 4,678 truth-labeled split pieces and 386 merges as exemplars (`--truth-kinds split,merge`, prototypes + MLP) — dev-8 92.3/80.2, legal-8 86.6/66.4, broad-30 87.5/69.0 (char +0.1, word −0.5): NEGATIVE. Cut pieces do not become a learnable population by adding a few thousand of them to a 60-prototype budget; the 13.9% error rate of split pieces is a chopper problem (where to cut, and whether), not an exemplar problem. |
| 🔬 Lexicon coverage hole (bug, fixed) | — | `scripts/build_langmodel.py` | Exposed by the larger corpus: dictionary words were excluded whenever they appeared in the corpus at all, but the weighted lexicon keeps only the top 30k — so a word occurring in the corpus yet outside that cut fell out of BOTH paths and vanished. Invisible with five novels (fewer than 30k distinct words); with 2.4M words "trademarks" disappeared entirely. Exclusion is now against the words actually kept. |
| ✅ Digit-heavy suppression exemption | Data-line recognition (numeric strings are unvalidatable by lexicon, by construction) | `decode/output.py` | The deletion study showed price-table rows ("3 x 5 (LINED) 3.00"), phone numbers and receipt codes being garbage-suppressed: no lexicon can endorse them and their confidences run low, so they met every gate. Lines whose alnum content is ≥40% digits (≥4 alnum) are now exempt from suppression including the graphic-suspect branch — junk that decodes digit-heavy is rare. dev-8 +0.6 char/+1.3 word (90.1/71.8, first dev char >90); broad-30 81.6 char, recall +0.1. |
| ✅ Dash-run scrub | Output sanitation; complements the punctuation position prior | `decode/output.py` | Follow-up dash study (2026-09-01): the position prior fixed '.'→'-' (69→~5), but '-' INSERTIONS rose 133→192 — signature scribbles and underline fragments now decode as dash runs (rejection retired) and dodge suppression via accidental short words ("no", "of"). Printed text never contains '-{3,}': such runs are deleted from tokens at output, emptied tokens dropped, hyphens and '--' untouched. broad-30 81.6→81.9 char; synthetic unchanged. |
| 🔬 GED gate re-sweep (negative) | Riesen-Bunke GED rerank gate calibration, revisited after the classifier legs | `recognize/stage.py` | The fresh confusion report ranked a vertical-stroke family (~190: l↔i, t→l/1, h→r, i→d, u→i) — exactly GED's territory — so the gates were re-swept on dev-8: margin 0.25→0.40, roughness 0.72→0.85, top 6→8, alone and combined. ALL FLAT (90.1/71.8 → 90.2/72.0 at best, noise-level). The gates are not the binding constraint: these confusions either never present the right candidate to rerank or lose their distinguishing evidence upstream (i-dots merging, t-crossbars eroding at small bitonal sizes). Next lever for this family is evidence-side (component/dot handling, feature sensitivity), not rerank tuning. Defaults unchanged. |
| 🔬 knn_scc edge-distance variant | Author's 2026 refinement of his 1995 algorithm | `layout/knn_scc.py` `distance_mode="edge"` | Link length = minimum distance between the two boxes' edge-center points (sector classification stays centroid-based). Measured: ties centroid mode on the fixture (IoU 1.00) and letters; on photo-heavy news it INVERTS the big-component bias rather than removing it — a large remnant's edges are near everything, so it merges INTO text (12 mixed blocks, 54.5% vs centroid's 47/77.4%). Kept as an option; the two biases trade places by domain. Same session: spread-based prune thresholds (mean+kσ, MAD) measured collapsing newspapers (heavy-tailed lengths from photo fragments) — default reverted to the 1995 ratio rule; paper amended with the cross-domain caveat. |
| 🔬 knn_scc per-axis pruning (granularity study) | Docstrum's separate within-line/between-line spacing estimates (O'Gorman, PAMI 1993) motivated splitting the prune threshold by link axis | `layout/knn_scc.py` `prune_scope="per_axis"` / `"per_axis_nn"` | Question (user-raised TWICE, paper Fig. 5): why does a letter's body weld into ONE block instead of per-paragraph blocks? TRUE RESOLUTION (2026-09-01, via the segmentation lab): it DOES weld at the default — the intermediate "11 blocks at the default" claim recorded here earlier was an ARTIFACT of a diagnostic script feeding the binarizer raw 0–255 gray instead of load_gray()'s [0,1]; Sauvola binarizes differently and the CC/link statistics shift. Correct-scale measurements on bus.3B 8760_001: default 1.5×mean (cutoff 137px) = 5 blocks, body welded (its paragraph gaps ≈90–135px sit just under the cutoff); 1.2×mean (110px) = 10 blocks, the classical letter decomposition; per_axis_nn = 47 blocks, mostly paragraphs but sparse lines shatter into word boxes (the earlier "103-block word confetti" figure was also mis-scaled). GRANULARITY DIAL conclusion stands with corrected level labels: 1.5×mean = region, ~1.2×mean = paragraph, nearest-link = below paragraph. Default unchanged; Fig. 5 rebuilt as a two-panel (a) default/(b) 1.2×mean comparison with the retraction noted. LESSONS (process): (1) regenerate figures after any default change; (2) ad-hoc scripts MUST load pages through core.imgio.load_gray — mis-scaled gray silently changes every downstream stage; (3) the lab exists precisely so segmentation claims are checked against the algorithm's live inner state. |
| ✅ knn_scc pooled-k link collection (author's refinement) | **Proposed by M. Sharpe (2026)** for his 1995 algorithm, from the lab's single-line evidence: per-sector quotas force every char into long north/south links however far the next line is | `layout/knn_scc.py` `k_total` (default None = 1995 spec) | Per-sector picks are pooled and only the k_total shortest links survive per node — a mid-line char keeps left/right/2nd-left instead of forced verticals. Two sub-refinements found by iterating in the lab: (1) DISPLAY-SIZE chars are exempt from pooling (they sit far from everything; a pooled top-k measured against body text starves headline links — first attempt shattered a news headline); (2) the "large" reference must be the median GLYPH size with specks excluded (newsprint's raw median CC is a 5px dot; 2×5px classified every char as large, exempted the whole page, and welded columns to 1 block). MEASURED: on the Fig-5 letter the LAYOUT quality is the best of any configuration — 19 blocks matching the human decomposition (every bullet, paragraph and letterhead field separate, vs 5 with the body welded at the spec default); synthetic fixture IoU 1.00 at every setting; news parity (columns each one clean block both ways; the headline shatters both ways — pre-existing display issue). END-TO-END OCR on dev-8 with knn_scc blocks: spec 89.2/70.6 vs pooled kt=5 88.6/70.7 — char −0.6 because finer blocks fragment reading order. VERDICT: the granularity lesson again — the OCR pipeline wants regions (spec default unchanged), but for layout-analysis consumers the pooled variant is the better segmenter. A second dial: k_total sets granularity structurally (at kt=3 the threshold stops mattering — connectivity, not pruning, is the constraint). USER CONFIRMED the refinement (2026-09-01) and supplied the IDUR lineage for the bullet question: strictly by whitespace each bullet is its own block, and COMPOSING them into a list is a logical operation — IDUR (1994) detected bullets geometrically and used a DEFINITE-CLAUSE GRAMMAR to recognize a bullet list as one-or-more bullets. That layering (whitespace-honest geometry below a grammar-based logical composer) is the design direction for a future logical-layout stage. Paper §4 gained the pooled-k section + Figure 8. |
| 🔬 knn_scc component conditioning (author's suggestions, optional) | **Proposed by M. Sharpe (2026)**: filter the CC list before graph building — tiny specks, oversized image components, components nested inside others, optional overlap-merge; kinship: Fletcher-Kasturi size filtering, Docstrum preprocessing | `layout/knn_scc.py` `cc_min_px` / `cc_max_factor` / `cc_drop_nested` / `cc_merge_overlap` (all off by default) | Nested rule refined during design: containment alone would delete BOXED TEXT (the Fig-5 letterhead address lives inside a drawn box), so a container must be SOLID (fill ≥0.35) or oversized — hollow frames keep their contents. MEASURED, full pipeline (despeckle+imagezones upstream): near-noop on letters; speck filter HARMFUL on news (44→4 blocks) — the thousands of short speck links were holding the length mean down, and without them the 1.5×mean cutoff climbs above the column gutter: the pruning statistics are COUPLED to the node population (future refinement: compute threshold stats over glyph-sized links only). MEASURED, STANDALONE (raw binary, no upstream cleanup): big+nested filters do imagezones' job on an illustration page (1 welded block → photo/text separated); letters clean; newsprint remains beyond size filters (a halftone FIELD's mid-size blobs survive speck removal — density detection is irreducible there). Overlap-merge: mild and safe on Manhattan layouts (21 unions, blocks unchanged). VERDICT: valuable for the algorithm's standalone story, redundant-to-harmful inside the full pipeline — all four ship as documented options, defaults off. ADDENDUM (author's point, same day): a component dropped for being BIG is not noise — it is a photo/illustration/boxed region the filter has effectively DETECTED. Dropped-big boxes now rejoin the final segmentation as `image_blocks` (merged among themselves only — never with text blocks, the measured photo-weld hazard — and interleaved in reading order). Standalone mailbag page: illustration returns as its own image block alongside the text blocks, a complete accounting of the page; the lab draws image blocks in orange. |
| ✅ Whitespace maximal empty rectangles (experimental) | H.S. Baird, "Background structure in document images" (1994); T.M. Breuel, "Two geometric algorithms for layout analysis" (2002) | `layout/whitespace.py` (`blocks.whitespace`; default remains `xycut`) | Branch-and-bound maximal empty rects over RLSA-smeared line segments (glyph-level obstacles fragment spanning headlines — measured); gutters validated by SIDE SUPPORT (ink hugging both flanks ≥0.30 of gutter height; real gutter measured 0.31/0.44, false margins/word-gaps ≤0.15 — an unvalidated version produced 22 false gutters on one newspaper page); banded column-major reading order (spanning blocks close bands). Status: passes the synthetic multicolumn suite, parity with xycut on newspapers (42.1 vs 41.3 char); the recall its better segmentation gains (+9 measured pre-validation) is not yet retained through validation. Trim-to-supported-extent measured WORSE (recall 20.5). A visual-debugging session (2026-08-31) then mapped the full failure space on one newspaper page and banked four findings for a redesign: (1) RIVERS AT GUTTER SCALE — obstacle smear must exceed word gaps (15–25px body, 50–70px display) yet stay under gutter width (~30px): no single length works, so spans need their own long smear; (2) the long smear welds body lines across narrow gutters, so span candidates must also be TALL — and the height median must be computed over line-sized obstacles only (tiny fragments drove the median to 4px and neutered the gate); (3) gutter validation must be flank-ASYMMETRIC — the next column's left edges align under any alignment mode, but the left column's right edges are ragged unless justified; (4) removing span-band obstacles from column bookkeeping deleted body text wholesale when a false span band overlapped columns (aggregate 42→25 char). All variants reverted to the parity implementation; the redesign should be built against SYNTHETIC NEWSPAPER FIXTURES (known gutters/spans/rivers as unit tests) rather than live-page iteration. |
| ✅ Table cell grid from rule intersections | Ruling-based table recognition; R. Zanibbi, D. Blostein & J. Cordy, "A survey of table recognition" (IJDAR 2004) | `layout/tables.py`, `decode/output.py` | Union-find over touching rules groups a frame; clustered rule coordinates give row/column levels; cells are the inter-level rectangles (≥2×2 required — lone separators are not tables). Output stage places decoded words into cells by box center → meta.tables_text grid. Side discovery: language detection on a near-empty page (9 isolated words) confidently picked Italian — detection now requires ≥60 chars of pseudo-text and otherwise falls back to a default language. |
| ✅ XY-cut newspaper/magazine prior: half gutter, tall-gutter rule, narrower row gap for newspapers (2026-09-12) | Nagy & Seth 1984 (XY-cut); the known weakness — a spanning headline blocks the column cut until a row gap frees it — and the tab-stop answer to it in R. Smith, "Hybrid page layout analysis via tab-stop detection" (ICDAR 2009) | `layout/blocks.py` `XYCutBlocks` (`min_column_h_300dpi`, doc_type branch) | First numbers on UNLV mag.3B and news.3B (eight pages each, seed 1, neural): 59.2/44.2 and 62.3/52.1 char/word against legacy Tesseract's 87.4/85.0 and 96.4/93.6. Two of the sixteen pages were lost to the deskew edge pile-up (cleanup row); with that fixed, 66.5/48.6 and 73.1/61.7. Two two-column newspaper pages still read 28 char: their gutter is 0.06", under the letter's 36-px minimum, and the second headline line sat 20–26 px above the byline (the letter's row break needs 30), so the body block kept the headline, whose letters crossed the gutter and blocked the column cut; the columns were then read line by line, fused. Variants on both sets: gutter 18 px alone — news 84.5/74.1, mag 68.2/50.8; plus a tall-gutter rule (a vertical cut needs a region at least 150 px tall, because a display headline's WORD gaps are wider than a newspaper's gutter and had cut 'Joyful patrons of all races' into four blocks) — news 84.6/74.3, mag 68.3/50.7; plus row gap 18 px — news **92.8/83.1** (the two pages 92.9/82.4 and 94.0/84.9), mag 65.8/50.2 (magazine leading is heavier; blocks shred). The whitespace-rectangle blocks stage on the same pages: news 56.1/43.4 with a crash, mag 58.9/38.9 — worse than XY-cut either way (its crash, a cut span past the mask in `components._cut_column`, is clamped). Adopted as a `doc_type` prior in the pattern of the letter/legal one: newspaper gets all three, magazine the gutter and the tall-gutter rule. Classic on the same pages: news 92.1/79.0, mag 65.3/40.6. Zone-ordered (`--zone-order`, recognition without reading order): news 92.9/82.7 — the order is now right — and mag 74.9/53.4 against 68.3/50.7 plain, so a third of the magazine gap is still reading order (a page at recall 92.7 reads 65.7 char plain, 91.6 zoned). The four standard sets carry doc_type letter, legal or none and are untouched by construction. |
| 🔬 Tall-gutter rule as a GENERAL default — NEGATIVE, stays a newspaper/magazine prior (2026-09-12) | As above | `blocks.min_column_h_300dpi=150` on every document type, classic profile | dev-8 95.2/89.2 → 95.4/89.6, broad-30 91.8/82.0 → 92.0/81.9, legal-8 91.7/81.2 → **89.9/76.3**, modern 89.6/80.8 → **84.2/59.5**. The rule forbids a vertical cut through any region under 150 px tall, and that is exactly what a pleading's line-number column beside a short block, and an invoice's or payslip's cell row, need: their columns are one or two lines tall by construction. A letter's headline word gaps were never the problem there. Kept at 0 by default; newspapers and magazines, whose short regions are display headlines, set it through the `doc_type` branch. |

## Glyph features (`glyph/features.py`)

| Feature | Lineage | Notes / deviations |
|---|---|---|
| ✅ Zoning densities (8×8) | Classic hand-print OCR feature; surveyed in Ø.D. Trier, A.K. Jain & T. Taxt, "Feature extraction methods for character recognition — a survey", Pattern Recognition 29 (1996) | Our single strongest feature family (measured +4–5 pts top-1 over 4×4). |
| ✅ Hu invariant moments | M.-K. Hu, "Visual pattern recognition by moment invariants" (1962) | Log-compressed to tame dynamic range. |
| ✅ Crossing counts | Classic; Trier et al. (1996) | 4 rows + 4 columns of ink-run counts. |
| ✅ Skeleton endpoints/junctions | Thinning: T.Y. Zhang & C.Y. Suen (1984) via scikit-image `skeletonize` | Counts only in the feature vector; the full structural representation lives in `glyph/skeleton.py` (attributed graphs: endpoint/junction nodes with positions and degrees, stroke edges with length/angle/straightness, explicit loop count; rings like 'o' become a self-loop node). |
| ✅ Gated GED reranking | K. Riesen & H. Bunke, "Approximate graph edit distance computation by means of bipartite graph matching" (IVC 2009); structural OCR lineage per Kahan-Pavlidis-Baird | `recognize/ged.py`, `recognize/stage.py`, `scripts/build_skeletons.py` | Bipartite-assignment GED over skeleton graphs as a SECOND OPINION on the feature matcher's top-6, active only behind two gates, both measured necessary: (1) roughness < 0.72 — flip-noise corrupts skeletons (ungated: −5 on heavy isolated glyphs; fixes 7, breaks 12); (2) feature top-1/2 margin < 25% — blur corrupts skeletons while SMOOTHING roughness, and ungated GED overrode confident correct calls (synthetic sev2 −13 word). Three further lessons completed it: (3) bank-uncovered candidates need the NEUTRAL mean GED — rescoring only covered classes penalized them relatively, and accent classes with no skeletons flooded top-1 ("the"→"tnü"); (4) the bank must be OPERATOR-CONSISTENT — clean 48px prototypes mismatch Sauvola-thinned 32px pipeline crops (an 'e' whose bar vanished needs a matching degraded prototype), so the bank carries 48px/32px/blurred-32px variants; (5) scale swept to 10 (25 taxed synthetic). FINAL: broad 30-page letters 75.7 char/44.2 word (+1.1/+2.1), dev-8 84.2/55.5, synthetic at parity — a clean net win, largest in the clean-to-light regime where customer grayscale PDFs live. Language detection gained a challenger margin (default holds unless beaten by 0.08) after noisy top-1 text flipped borderline en/de. |
| ✅ Persistence-graded hole counts | Simplified from persistent homology (H. Edelsbrunner, J. Harer, *Computational Topology*, 2010) | Holes counted after closing at radii {0,1,2}: a hairline-broken 'o' keeps its hole at r≥1. Poor man's persistence — no filtration diagram. |
| ✅ Moment-based deslanting | Slant normalization from handwriting recognition; cf. Bozinovic & Srihari (1989) | Shear by μ11/μ02, clipped to ±0.6. Lets one prototype cover roman + italic (+3 pts shape-class top-1). |
| ✅ Stroke-width normalization | Cf. Tesseract's character normalization (R. Smith, "An overview of the Tesseract OCR engine", ICDAR 2007) | Erode/dilate toward width = 0.16·max(h,w), estimated from the distance transform median. |
| ✅ Side profiles (left/right first-ink distance) | Profile features; Trier et al. (1996) | Separates b/d, p/q. |

## Recognition, decoding, language

| Technique | Lineage | Code | Notes / deviations |
|---|---|---|---|
| ✅ Nearest-prototype (1-NN, z-scored) | T. Cover & P. Hart, "Nearest neighbor pattern classification" (1967) | `recognize/nearest.py` | Std floored at 5% of mean std — a raw ε amplified near-constant features into the dominant distance term (measured catastrophic). Candidate depth matters far more than expected: top-k 5→8→10 gained +2 then +4 char on real scans (the truth for unseen fonts often sits at rank 6–10, where the beam+LM+adaptation stack can still find it); 12 plateaus. Beam width does NOT follow: 16/24 measured slightly worse than 8 even with 10 candidates — wider beams surface LM-favored wrong paths. |
| ✅ Synthetic training data via font rendering + degradation | H.S. Baird, "Document image defect models" (1992); T. Kanungo et al., "A statistical, nonparametric methodology for document degradation model validation" (PAMI 2000) | `factory/synth.py` | Kanungo edge-distance-decaying flips + blur + skew + illumination field + mirrored-page bleed-through. Every sample keeps its θ. Bitonal variant added 2026-08-31 (blur → hard threshold, the fax/bitonal-scanner pipeline per Baird's defect model): prototype variants at threshold 0.4/0.55 improved BOTH real scans (+2 char) and synthetic (+2–6 word) — a general augmentation, not just domain matching. |
| ✅ Over-segmentation + LM-scored variant choice | Segmentation survey: R.G. Casey & E. Lecolinet, "A survey of methods and strategies in character segmentation" (PAMI 1996); document decoding: G. Kopec & P. Chou, "Document image decoding" (PAMI 1994) | `glyph/components.py`, `decode/beam.py` | Wide groups (>1.5× median) get an ink-minimum cut hypothesis; decoder enumerates ≤8 variants/word. Full DID-style lattice not implemented. |
| ✅ Character n-gram beam decoding | C.E. Shannon (1948) n-grams; beam search standard practice | `decode/beam.py` | Trigram over prefix context; add-one smoothing. |
| ✅ Corpus-frequency lexicon + correction | OCR post-correction practice; cf. K. Kukich, "Techniques for automatically correcting words in text" (ACM Surveys 1992) | `lang/model.py`, `decode/beam.py` | Tiered: corpus top-30k weighted + dict-words (≥3 chars) at a floor. Flat word lists measurably hurt (junk 2-letter entries). |
| ✅ Dictionary-endorsed word segmentation | Word-boundary DP practice (cf. Kukich 1992 §segmentation) | `decode/beam.py` `_best_gap_variant` | Key invariant found the hard way: beam score can never be the primary key for split choice — every extra word resets the n-gram context, so shredding always wins locally. Lexicon endorsement is primary; scores break ties. |
| ✅ Bimodal gap clustering for spaces | Standard bimodality assumption on inter-word vs inter-letter gaps (layout surveys, op. cit.) | `decode/beam.py` `gap_band` | Per-line 1-D 2-means; x-height ratio only as fallback (median-of-heights x-height is ascender-inflated — measured twice). |
| ✅ Case disambiguation from line geometry | Typographic-context practice in classic OCR | `decode/beam.py` `_height_prior` | Plus orthographic prior: capital-after-lowercase penalty. Dotted i/j are ascender-tall — missing this cost ~10 char pts (bug of 2026-08-30). |
| ✅ Shape-confusion twin injection | Classic OCR confusion-pair handling (n/h, c/e); same mechanism as case-twin injection | `decode/beam.py` CONFUSION_PAIRS | When one of a confusion pair is a candidate, the other joins at penalty 1.6 so language context arbitrates. Motivating diagnosis: a typewriter letter where 'h' ranked below top-14 while 'n' led — "the" (19 page occurrences) decoded as "LEe"/"CLe", unreachable by any LM because 'h' was never offered. Worst page 75.3→79.5 char; letters set +1.5 char/+4.0 word. Penalty calibrated two-objective (1.0 cost degraded synthetic −3.9 word; 1.6 keeps both). Digit-mode fix in same leg: a letter pin must not override digits ("1993"→"l993" via an 'l' pin). e↔o added next leg after a sans-font page decoded "president"→"prosidonl" throughout ('e' bar fills/vanishes): letters +1.6 char/+5.5 word AND synthetic up — the strongest single pair so far. e↔s and 9↔s followed ("Drivs", "5ss5"=5995): letters +0.3 char/+1.3 word, word acc crossing 50%. Same leg fixed a plain coverage hole: '&', '$', '%', '/', '#' were absent from the class set entirely — "&" decoded as 'a' forever in a corpus full of business text. |
| ✅ Digit-mode decoding (rank-graded, categorical twins) | Numeric-field handling practice in forms OCR | `decode/beam.py` `_beam_word` | Digit evidence graded over candidate ranks (rank-1 = 1.0, ranks 2–3 = 0.6); in digit mode the letter LM is muted, lexicon substitution off, digit candidates boosted, letter pins do not apply, and DIGIT_TWINS (l/I→1, o→0, s→5, z→2, B→8, g→9…) are injected. Boost arithmetic alone lost when adaptation left short sharp candidate lists ('l' beat '1' by 1.5 nats on 5 entries — unit-reproduced), so the rule is categorical: a top-scoring letter with a digit twin in contention yields to the twin. "l993"→"1993", "357l"→"3571", "92l23"→"92123". Letters +1.5 word; ~0.4 synthetic char tax accepted. |
| ✅ doc_type layout priors | Original plan scope (optional hint, never required) | `cli.py --doc-type`, `layout/blocks.py` | Hint flows via page.meta. Single-column prior (letter/book/legal): column-split gap requirement rises 2.5×. Measured +0.4 char on letters — and +6.0 char / +9.6 word on legal pleadings, where double-spaced monospace text is full of vertical whitespace RIVERS (aligned character cells stack spaces into full-height channels) that shredded pages into ~187 single-line blocks. Auto river-defense added: a page shredding past 80 blocks retries the cut at the wide gap — legal unhinted recovers 67.2→70.5 char (hint still best at 73.2). |
| ✅ Corpus confusion methodology | ISRI-style error analysis at character-alignment level | `scripts/confusion_report.py` | Edit-distance backtrace over the full 30-page sample aggregates substitutions/insertions/deletions — the scaled version of the worst-page play; statistics nominate fixes. First run surfaced: ~550 case flips, p→D ×144, punctuation→letters ×280, and net ~700 spacing errors as the largest op category. |
| ✅ Baseline-descender prior | Baseline-relative typographic context (classic zone analysis: ascender/x-height/descender bands) | `decode/beam.py` | A glyph whose box crosses the line baseline is a descender letter (p/y/g/q, not P/Y/D/9), and vice versa — geometry always computed (lines stage baselines) but never consulted at decode. With the tiny-glyph punctuation prior (a mark under 40% of x-height prefers .,'- over letters), broad 30-page letters +1.4 char / +2.7 word and synthetic sev-1 +3.4 word. |
| ✅ Case-pattern grammar | Orthographic convention (words are all-lower, Capitalized, or ALL-CAPS); cf. truecasing literature (Lita et al. 2003) | `decode/beam.py` | Any case transition inside a word pays a penalty except upper→lower right after the first letter. Found via error decomposition on UNLV: case-folding alone moved word acc 16.6→28.6% on the worst page — random per-glyph flips inside caps words were the top word killer. |
| ✅ Garbage-line suppression (shape-gated) | Reject-class practice; cf. handwriting/signature discrimination in letter processing | `decode/output.py` | A line is suppressed only when THREE tests agree: zero lexicon words, mean confidence <0.15, AND degenerate shape (one character ≥40% of the line — "IIIIIIxIIIII"). The original two-test version deleted misread-but-real lines on hard fonts ("ADril 5, 19s31" = April 5, 1991; addresses; "Sincerely"), silently costing whole lines of recall; the shape gate returned them (+1.7 char on letters). |
| 🔬 Graphic-line suppression (distance-based, partial) | Non-text discrimination via recognition confidence | `decode/beam.py`, `decode/output.py`, `adapt/cluster_refit.py` | Attempted: flag lines whose pass-1 median top-1 distance ≫ page median (logo shapes match no prototype), suppress unless a ≥4-char endorsed word survives; graphic-suspect lines excluded from adaptation. KEY DISCOVERY en route: adaptation PINS logos — repeated identical strokes are the most self-consistent shapes on a page and rescored to distance ~0.0 (measured), the purest error-amplification case; the pass-1-only rule and adaptation exclusion are kept as guards. But the relative test fails exactly where needed: stylized-font pages inflate the page median so logo lines (1.2–1.5×) hide below real-line ratios elsewhere (1.3–1.4×). Open: needs absolute distance calibration or shape-level non-text features (density/periodicity), not a page-relative ratio. |
| ✅ Dotted-rule filtering | Perforation/dotted-ruling handling; complements solid-rule morphology (Wong-Casey-Wahl) | `glyph/components.py`, `decode/output.py` | A "line" of ≥20 dot-sized groups (≤8px median) is a perforation or dotted rule, not text — one receipt tear-off line decoded its dots as 313 junk words, costing a page −54% word accuracy; solid-line morphological opening cannot see dotted rules. Groups dropped at the components stage; a single-char-flood backstop (≥80% one-char tokens) added to output suppression. One fix moved the ENTIRE 30-page letters sample +1.6 char / +5.1 word. Found via broad-sample honesty check: the 8-page dev set had been ~10 points rosier than a 30-page sample — tuning-set optimism made measurable. |
| ✅ Fragment-join repair (group-level re-decode) | Mirror of dictionary word segmentation (Kukich 1992) | `decode/beam.py` `_join_decoded` | Letter-spaced caps make letter gaps as wide as word gaps (unimodal-large distribution defeats gap_band), shredding "National" into "Nat i ous l". Text-level concatenation is insufficient — the fragments are usually also misread — so candidate runs of ≤3-char fragments are RE-DECODED as one word over their combined glyph groups, letting the lexicon repair the reading; the merge is kept only if the result is an endorsed word and most fragments weren't words on their own ("on to" never becomes "onto"). |
| ✅ Adaptive rejection (median + k·MAD) | Robust statistics: F. Hampel's MAD outlier rule | `decode/beam.py` | Replaced 3×median (multiplicative rule was tightest exactly on clean pages). Reject exempts adapt-pinned glyphs. |
| ✅ Per-document adaptation (cluster → vote → refit) | Tesseract's adaptive classifier (R. Smith 2007); T.K. Ho & G. Nagy, "OCR with no shape training" (ICPR 2000); decipherment view of OCR | `adapt/cluster_refit.py` | Average-linkage clustering (measured 100% purity at cut ≤8 on synthetic pages); purity-gated cluster votes; labeled glyphs pinned as hints; unlabeled glyphs re-scored against document prototypes with cross-calibrated distances. Replacing all candidates amplified errors (measured) — blast-radius control is the design. Negative result (2026-08-30): a second adapt+decode round gains ≤0.5 pts — adaptation converges in one pass; iteration not worth the cost. |
| ✅ Calibration sheet with corner fiducials | Fiducial-marker practice (cf. ArUco, Garrido-Jurado et al. 2014); classic registration | `factory/sheet.py`, `decode_sheet.py` | 3 large + 1 small square; TR/BL disambiguated by requiring det>0 (a reflection fits all 4 marks!). Labels from pure geometry. |
| ✅ Degradation-parameter fitting by distribution matching | Simulation-based calibration; Kanungo et al. (2000) model validation | `factory/fit_theta.py`, `scripts/fit_theta_unlv.py` | Per-feature 1-D Wasserstein on glyph summary stats; Nelder-Mead over (blur, flip rates). Works label-free on harvested real crops. Negative result (2026-08-31): against UNLV bus.3B the fit only moved the distance 3% and the fitted theta gained ~nothing — those TIFFs are ALREADY BITONAL, so the dominant degradation is the scanner's own thresholding of low-res glyphs, which a gray-blur+flip model cannot mimic. build_prototypes now gates theta inclusion on fit quality (final < 0.8×initial). The calibration sheet (gray scan, known device) remains the intended use. |
| ✅ Font-family routing | Font recognition: A. Zramdini & R. Ingold, "Optical font recognition using typographical features" (PAMI 1998); also this plan's "runtime document router" idea | `recognize/stage.py`, `factory/fonts.py` | Exemplars tagged serif/sans/mono by face name; page's dominant family detected by a dominance vote (top ≥1.4× runner-up) among the CONFIDENT HALF of a glyph sample — absolute shares were too diffuse because degraded glyphs vote randomly. Matching then restricted to that family. Prototype-set study (2026-08-31, 8 UNLV pages + 220-word synthetic): 14 accidental-sans fonts = best synthetic, worst real; 26 curated (serif+mono+sans incl. Courier/Times/Georgia) = best real, −5-10 synthetic without routing; routing recovers most of it; 40 fonts (incl. SF UI faces) = noise-level mixed. Locked at 26 curated. Per-BLOCK routing added later (letterhead diagnosis: display-type logo lines decoded as word salad after inheriting the body's family): blocks with ≥12 glyphs vote their own family (+0.3 char/+0.4 word letters; synthetic unchanged). A "display" family (Academy/Herculanum/Luminari — the very faces once excluded as prototype poison) is now stocked FOR ROUTING ONLY: within the routed architecture they help letterheads instead of diluting (letters 79.5→81.0 char, +3.2 word; synthetic unharmed). The same faces unrouted had measurably hurt — the mechanism, not the fonts, was the problem. Composition is now PINNED BY NAME (scripts/build_prototypes.py BODY_NAMES): alphabetical-limit selection reshuffled the stock on every scanner change, and a .ttc-scan experiment that widened body to 30 with new serifs crashed synthetic sev0 92.7→75.4 (reverted). The .ttc scan's keepers: Impact and Copperplate as display faces (routing-quarantined, zero cost, both letter sets at baseline). The deliberate widening experiment then ran: appending 6 classified .ttc faces (AmericanTypewriter/Athelas/Baskerville/Charter/Cochin/Didot) to the pinned base with nothing displaced STILL cost letters −1.3 char/−3.6 word and sev0 −2.2 char — dilution is intrinsic to a bigger 1-NN pool even within routed families. The 23-face body is a genuine measured local optimum; per-document adaptation, not stock width, is how new fonts get covered. |
| ✅ Directional k-NN + SCC block segmentation (experimental) | **Original algorithm by M. Sharpe** (designed pre-project); kinship: L. O'Gorman's Docstrum (PAMI 1993) also clusters connected components by k-NN, via angle/distance histograms | `layout/knn_scc.py` (`blocks.knn_scc`) | Per character: 3 nearest neighbors in each of 8 compass sectors (directed edges); prune long edges; STRONGLY connected components = blocks; overlapping boxes merged. Merit test verdict: REAL MERIT — synthetic multicolumn fixture 3/3 blocks at IoU 1.00 untuned; on a real newspaper page it produced the best block structure of any implementation (coherent headline + three clean columns, no rivers); letters 79.7 char vs 81.2 for the hand-tuned incumbent, zero tuning. Refinement found: the spec's global 1.5×mean prune fragments display type (body spacing dominates the mean) — a HYBRID rule (global + extra links between mutually large-but-not-huge characters) fixes headlines without welding columns (pure relative pruning welds them: 2× an ascender exceeds a news gutter). Open hardening: photo-heavy aggregate pages lose recall (suspect: merge_overlapping absorbing text into photo-remnant boxes); needs the fixture-driven pass every stage gets. |
| ✅ Own character GRU language model | Recurrent char LMs: Mikolov et al. (2010), Graves (2013); GRU cell: Cho et al. (2014); user's proposal, self-trained per the project's founding constraint | `lang/gru.py`, `scripts/train_charlm.py`, `lang/textprep.py` | The project's own trained model: 258k-param single-layer GRU (hidden 256, embed 48, vocab 78) in PURE NUMPY — documented forward pass, longhand BPTT verified by finite-difference gradient checks, numpy Adam. Trains on the boilerplate-stripped Gutenberg corpus in 18 min on a laptop (24 epochs, 45 s each). Held-out perplexity 3.27 vs the trigram's 10.03 (same stream). Chosen over a transformer because beam search wants an O(1) per-hypothesis state: `GruLM` threads a hidden state through each beam, one batched step per glyph yields the full next-char distribution. Trained on RUNNING text (spaces included), so it models cross-word context the word-internal trigram never saw; lexicon/frequency stay with the corpus model; trigram remains the fallback when no weights exist for the locked language. lm_weight recalibrated 0.7→0.5 (sharper log-probs). RESULTS: broad 30-page letters 77.1 char / 47.3 word / 63.2 recall (+1.4/+3.1/+3.6 — largest LM-side gain of the project); dev-8 85.8/59.7; synthetic ≈ parity (pangram vocabulary is atypical for the corpus — accepted). Cost: decode slows measurably (per-word batched GRU steps). |
| ✅ Accented glyph classes (Latin multilingual) | Diacritic handling practice; components-level grouping reuses the i-dot mechanism | `scripts/build_prototypes.py` (105 classes), `decode/beam.py` | Accented letters are whole classes (the overlap grouper already merges mark+base into one glyph group). Three compensations, all measured: marks-above are TALL and ç is a DESCENDER for the height prior; accents outside the locked language's alphabet are penalized (LANG_ACCENTS); and top_k rose 10→14 because accent variants crowd their base letter's neighborhood and evict true competitors from shorter candidate lists (−2 char on English until compensated). Residual multilingual tax on English letters: ~0.7 char pt. |
| ✅ Language detection, multilingual corpora | n-gram language ID: W. Cavnar & J. Trenkle, "N-gram-based text categorization" (1994) | `decode/beam.py` `_detect_language`, `scripts/build_langmodel.py` | Five models (en/de/fr/es/it from Gutenberg corpora; alphabet extended to accented Latin); the page's pixel top-1 text is trigram-scored per language, best wins, locked per document. Three measured pitfalls fixed: (1) unequal corpus sizes make the smallest model flattest so it wins on any noisy text — counts are scaled to a common per-million rate before add-one smoothing; (2) each model's score is calibrated against its own-corpus baseline (likelihood ratio); (3) the pseudo-text must be split at word gaps — whole-line concatenation destroys the ^xx/xx$ boundary trigrams that carry much of a language's signature. Glyph classes are still ASCII; accented prototypes are the remaining piece for full multilingual decoding. |
| ✅ Multi-part sign grouping by nesting ('%') | Connected-component grouping of multi-part glyphs (Smith 2007 §3.1 on diacritic/base association; classic CC-to-character assembly) | `glyph/components.py` `_group_overlapping` (`nested_overlap`, `nested_min_h_factor`) | The stacked rule (x-overlap ≥ 0.5 of the narrower part) merges dots and accents but not the lower circle of '%', which overlaps the slash's foot by 5 of 11 px: '99.95%' read '99.95-40' on every modern letter page. Second rule: a part at least 1.5 dot-sizes tall whose whole y-span lies INSIDE another part's joins at 0.4 overlap — Latin print puts no separate blob beside a letter within its own height except the two-part signs and scanner-broken letters (both want merging); the height floor keeps a kerned period out. Unit-tested on the measured boxes. Whole-set effect is small (the sign is rare: modern word +0.1) but the confusion was systematic. |
| ✅ Vertical bar named by geometry | Position-based punctuation disambiguation (this project's punct-position priors; cf. Tesseract's baseline/x-height normalisation) | `decode/beam.py` `_is_bar` (post-pass) | '\|' is the field separator of modern letterheads ("Tel … \| www…") and read as 'I' on every modern letter. It is NOT a trained class: adding it would only steal from l/I/1. Instead the standalone-letter post-pass names a lone stroke taller than 1.5 x-heights whose foot drops 0.12 x-heights below the baseline — '\|' spans ascender to descender, l/I/1/! sit on the baseline. The recognized-lines' x-height is now published on the line for the geometric post-passes. Unit-tested. |
| ✅ Numeric formats admit a space cut | Format grammars as a lexicon (cf. Tesseract's number-pattern DAWG, `number-dawg`) | `decode/beam.py` `_best_gap_variant`, `decode/formats.py` | The strict admissibility rule let a cut through an uncertain gap only when BOTH parts were dictionary words, so '(415) 555-0190' was glued on every letterhead. A whole-shape numeric format (phone parts, amounts, dates, ZIP, and now percents) endorses a part as a word does. dev-8 recall +0.1 / precision +0.1, modern up on the letters. |
| ✅ Space before a comma or period joins | Typographic rule (no word starts with , or .) used as a segmentation constraint | `decode/beam.py` `_segment_line` (`_tiny_mark`) | Tracked figures on the invoices open a gap wider than the space threshold before the thousands comma: '$1 ,200.00'. A mark under `punct_small_frac` x-heights whose top reading is ',' or '.' joins the word before it whatever the gap. Merely making the gap uncertain LOST: the variant scorer counts "$1" and "200.00" as two endorsements against the whole's one — the join has to be a rule. |
| ✅ Numeric endorsement split from the digit-mode gate | Deferred decision principle (this project's design rule 4) | `decode/beam.py` `_beam_word` (`numeric_format` in word meta), `_decode_word` | A format hit was first made to count as `in_lexicon` so '(8.25%)' could answer the split reading's bonus. That flag ALSO gates the deferred digit-mode re-read ("graded evidence only if the word is not in the lexicon"), and "$35.55" is as valid an amount as "$39.99" — so the re-read that fixes 5/9 was skipped and the modern invoices lost 3.4 word (payslips −0.9) while every other kind was flat. The endorsement now lives only where it belongs: as a +2.0 term among the segmentation variants. Invoices restored; the percent line still reads. General lesson: a boolean that means two things will be wrong for one of them. |
| ✅ Merge charge (−1.0 per removed character) | Segmentation-search cost balance (Tesseract's chop/associate search uses a per-segment cost; Smith 2007 §4.2) | `decode/beam.py` `merge_char_bonus` | The first sweep compared 0 and +2.2 only; the negative side was never tried. At 0, three disconnected letters of 'tailspin' and 'Priya' merged into one confident wrong letter ('ttspin', 'PAya'). −1.0 gained on ALL FOUR sets (dev-8 char +0.2, broad-30 +0.3/+0.2, legal-8 +0.3/+0.5 with precision +0.8, modern +0.1/+0.2); −2.2 over-shreds ('Priva Date'). |
| ✅ Digit mode: a digit spawns no letter twin | Confusion-pair injection (this project) meeting Tesseract-style numeric context (Smith 2007 §4.4 on number patterns) | `decode/beam.py` `_beam_word_mode` (CONFUSION_PAIRS loop) | Every Avenir 9 on the modern invoices read as 5 ('$495.00' → '$455.00') although the classifier ranked '9' first by 47 distance units. Bisecting the word decoder per prior found the term: the ('9','s') confusion pair injects 's' at the 9's score minus 0.3; on an all-figure line the estimated x-height IS the digit height, so the lowercase height prior paid 's' +0.8×scale, 's' outscored its own source, and the categorical digit-twin rule ("a letter with a digit twin in contention yields to the twin") promoted 's'→'5'. In digit mode a digit now spawns no letter twin. Measured: modern 68.0→68.4 word (+0.5 recall/precision; the Avenir invoices' amounts all correct, 'SOTO:' → 'Bill To:'), legal-8 68.8→69.1 (+0.4 recall), dev-8 and broad-30 flat. Method note: the per-glyph bisection probe (decode one glyph with each prior zeroed in turn) found in four runs what reading the code did not. |
| 🔬 Segmentation lattice over ranked cuts — NEGATIVE, opt-in | The plan's "lattice" stage; Tesseract's chop-and-associate search over a segmentation graph (Smith 2007 §4.1–4.2); classic over-segmentation + DP | `glyph/components.py` `lattice_cuts`, `recognize/stage.py` `_lattice_options`, `lattice_paths` | Motivation: bill titles in a large serif (Century-like) set letters touching through hairline serifs — 'Administrator' arrives as 7 blobs for 13 letters, a 106-px five-letter blob read WHOLE as a confident 'e'. The fixed 2/3-piece options cannot reach it; a widest-first recursive chop (tried first, `split_max_pieces`) cuts every 'm' and 'w' in half because a wide letter exceeds the letter-width cap — page result mixed, removed. The lattice: components stores up to k ranked ink-minimum cut columns per suspect; the recognizer scores EVERY span between boundaries (≤ 27 crops for k=6) and ranks whole paths by summed top-1 distance minus the page median (a plain sum favours few pieces however badly they read); the k best paths become the `alts` the decoder already consumes. Page probe on the worst bill: 7 words gained ('payment', 'Illinois', 'Introduced', 'awarding'), 6 lost ('JANUARY' → 'JerrY', 'Services' → 'Fleeces'); bag-of-words neutral. Sets (k=5): dev-8 94.1/84.7 → 93.7/83.9, modern 83.1/68.4 → 82.6/67.7. Why: pieces cut between serif-touching letters carry the neighbour's serif stub, and the distances that rank the paths are calibrated for whole glyphs — the same lesson as piece-aware outline scoring. 'Advstrator' persisted: the true path was in the lattice but its pieces did not read as d-m-i-n-i. What would make it work is a partial-shape classifier (Tesseract's answer) or synthetic training on cut pieces; both deferred. Kept opt-in at `lattice_cuts=0`. |
| ✅ Class-aspect prior, gated off on fixed pitch | Width/aspect expectation per class (Tesseract's blob-width and x-height normalisation feeds its classifier; Smith 2007 §4.3) | `recognize/nearest.py` `class_geometry`, `decode/beam.py` `aspect_prior`/`aspect_tol` | The classifier sees a normalised crop: an 'ail' blob three letters wide reads as a confident 't'. The recognize stage now publishes each class's trained median height/width (de-standardised from the exemplar features) and the decoder can charge a candidate per log-unit its box departs from it beyond a ±40% band. Page probes (weight 1–2): fixes 'resting'→'hosting', breaks 'Tail shin'→'TaiLOR', 'Elm'→'nIm', 'Client'→'Event' — narrow-set faces (Arial Narrow, Avenir) sit outside the band of the training stock, so the prior punishes the correct class. Whole-set measurement at weight 1.0 told a different story from the page probes: dev-8 94.1/84.7→94.2/85.0, broad-30 88.6/73.1→88.7/73.4, modern 83.0/68.0→83.1/68.1, and legal-8 87.0→86.3 char — the typewriter set. Fixed-pitch faces stretch every glyph toward one advance width, so the stock's proportional aspects are wrong there; the components stage now publishes its fixed-pitch verdict on the layout and the decoder drops the prior on such pages: legal-8 restored exactly (87.1/69.1), dev-8 keeps its gain. Adopted at 1.0; 2.0 over-punished narrow faces ('Elm' → 'nIm') on the old features. Re-swept after the feature and calibration fixes (2026-09-07): 2.0 gains dev-8 87.5 → 88.0 word, legal-8 79.2 → 79.5 (precision 87.8 → 88.9), broad-30 80.6 → 81.0, modern −0.1; 3.0 no better — 2.0 adopted. Lesson repeated: three-page probes show the failure modes, only the four-set run shows the sign. |
| ✅ Word and line renders with controllable letter spacing (2026-09-09) | Synthetic word images for sequence recognizers: Jaderberg et al. (2014) random kerning and fonts; Shi, Bai & Yao (2017) CRNN on synthetic words; line renders per Breuel et al. (2013); degradation per Baird (1992) via `synth.degrade` | `factory/words.py` (`render_line`, `touching_pairs`, `render_word_window`, `sample_theta`, `sample_tracking`), `scripts/make_seq_data.py` | Characters are placed one at a time (PIL has no tracking control) so every box is exact; negative tracking pulls neighbours into contact, and an OWNER MAP drawn from each glyph's thresholded mask says which adjacent characters share a connected component after rendering — the touching-pair label itself. Two defects found by looking: drawing the owner map with `fill=index` anti-aliased the index (an edge pixel of character 5 read '2', every pair looked touching); and tracking applied before a space left one pixel between italic words. Measured on Arial: letters meet below −0.10 em (0 pairs at −0.10, 5 of 6 at −0.12), so the tight range is [−0.22, −0.06] em. Blur is a fraction of the type size, not a pixel count: a 0.9-px blur that softens 26-px type erased 12-px type entirely (looked at), so sigma ≤ 6% of the x-height. Windows are degraded at NATIVE size, then normalized to the 32-row strip (x-height 13 px, baseline row 22; `glyph/strip.py`). 250k windows, 43% with a touching pair, 8 min on 10 workers. |
| ✅ Truth-labeled real word strips (2026-09-09) | The truth-labeled glyph harvest (above), at word level | `scripts/harvest_lines.py` | Per decoded line matched to its truth line, one window per word cut at the gap midpoints (as the decoder cuts) and labeled with the TRUTH substring between two word boundaries the alignment matched — whatever the pipeline read inside, so a word decoded 'Depatment' yields its strip labeled 'Department'; the harvest is richest exactly where the pipeline fails. Same contamination guard as every harvest. Pitfall: `align()` yields its operations from the line's END backwards; walked forward it found no boundaries at all (18 words from 3 pages; 677 once sorted). bus.3B 170 non-eval pages and legal.3B 240. |
| 🔬 Word-strip sequence scorer, CRNN + CTC, as a rerank term (2026-09-09, under measurement) | Graves, Fernández, Gomez & Schmidhuber (2006) CTC; Shi, Bai & Yao (2017) CRNN; Breuel et al. (2013) and Smith (2016, Tesseract 4) line normalization by x-height and baseline; Cho et al. (2014) GRU cell in torch's gate convention | `recognize/ctc.py`, `recognize/seq.py` (numpy reference: forward, masked BiGRU BPTT, CTC gradient), `recognize/seq_torch.py` (optional mirror, `[train]` extra), `glyph/strip.py`, `scripts/train_seq.py`, `decode/beam.py` `seq_path`/`seq_weight`/`seq_gate`/`_seq_rescore` | The neural profile's first term, aimed at the 53% of excess errors that are touching pairs: each word's segmentation variants (split × merge, as enumerated by `_decode_word`) are rescored by −log P(text \| word strip) under CTC, relative to the variant's best, so 'rt' and 't' are judged by ONE model over ONE image with no cut placed — the decision four chopper series could not make with whole-glyph distances. Never injects a class or a string by default (the glyph CNN's injection was catastrophic). 32 × W strip, conv 16/32/64/64 with vertical-only pools after the first (column stride 2 px so 'll' keeps a blank frame between), BiGRU 96, 285k parameters. Activations past each strip's width are zeroed after every layer so a strip's posteriors do not depend on batch padding (measured 0.1-nat drift on the last frames before). Trained on 250k synthetic windows + real truth strips ×2, page-disjoint holdout. Throughput on this M4 Pro: torch/MPS 310–620 samples/s (the GRU backward launches a kernel per frame), CPU torch 430, numpy 250 — the plan's 4–8k/s estimate was wrong by an order of magnitude; an epoch over 420k windows is 12 min on MPS, so a 12-epoch model is 2.5 h and numpy is the overnight backend. Numpy/torch forward parity to 1e-4 is a test. |

## Evaluation

| Technique | Lineage | Code |
|---|---|---|
| ✅ Levenshtein char/word accuracy | V. Levenshtein (1966); ISRI accuracy-report conventions (Rice & Nartker) | `scripts/eval_pages.py`, `scripts/eval_unlv.py` |
| ✅ Order-independent word recall/precision | Bag-of-words evaluation practice | `scripts/eval_unlv.py` | Added after magazine measurements: edit distance conflates reading-order mismatch with recognition error (one page: 15.8% char "accuracy" yet 53% of words correctly read). Recall vs word-acc gap = order problem; low recall = recognition/coverage problem. |
| ✅ GT-zone-ordered scoring | ISRI evaluation practice (manual zoning; Rice & Nartker) | `scripts/eval_unlv.py --zone-order` | Output words re-sorted by (.uzn zone, line baseline, x) before scoring — sort must use line baselines, not word centers (word centers interleave within a line; measured). Order convention is worth ~4-5 char pts on mag/news. |
| 🔬 Small-size prototype variants (16 px) | Size-matched exemplar practice | — (tried, reverted) | Negative result (2026-08-31): flat on newsprint. Small bitonal type (~15 px x-height) loses topology in the source scan itself; no exemplar coverage recovers information that binarization destroyed upstream. The news/mag gap needs layout/reading-order and possibly higher-resolution source sets (4B), not more prototypes. |
| ✅ Held-out-font evaluation at graded severities | Standard generalization protocol | `scripts/eval_glyphs.py`, `scripts/eval_pages.py` |
| ✅ UNLV/ISRI corpus measurement | S.V. Rice, F.R. Jenkins & T.A. Nartker, ISRI annual tests of OCR accuracy (1992–96) | `scripts/eval_unlv.py`; sets in data/unlv (bus.3B/3A, mag.3B, news.3B/4B) | Resolution study (2026-08-31, paired same-document news 300 vs 400 dpi; after the wavy-rule fix): 4B wins 5/6 pages, mean +2.3 char — resolution helps consistently; the one former −8.2 outlier was a rule-detection bug, now fixed. Clean originals (bus.3A) vs photocopies (bus.3B): +1.8 char. Rejection-threshold sweep (3–10 MADs): flat — adaptation pins already exempt most would-be rejects. |

| ✅ Real-glyph harvest (corpus-scale self-labeling) | Self-training/adaptive-classifier lineage (Ho & Nagy 2000; Smith 2007) taken corpus-wide; motivated directly by the external benchmark's gap decomposition | `scripts/harvest_glyphs.py`, merge in `scripts/build_prototypes.py` | The pipeline runs over real pages and keeps (features, label, family) for glyphs inside lexicon-endorsed, confident, ≥4-char words on clean lines — 52,571 exemplars from 163 UNLV letters. CONTAMINATION GUARD: the harvester reproduces the evaluation samples' exact shuffles and excludes every evaluated page. Merge guards: per-class cap 80 (250 measured diluting — the pool-size law holds for real data too) and inlier-core filtering vs the class medoid (a mislabeled exemplar is a decoy). RESULTS: the largest word-accuracy leg of the project — broad 30-page word 47.3→52.3 (+5.0, crossing 50%), precision +4.0; dev-8 word +6.2; synthetic char 96.0/91.9/93.5 (+4.2/+1.7/+4.7) — real glyphs generalize better than synthetic degradation. ROUND 2 (flywheel iteration, 2026-09-01): re-harvest with the round-1-improved model grew the pool 52,571→75,395 (+43%). Word-level metrics compound — dev-8 word 59.7→63.2 (+3.5), broad recall +2.4 / precision +4.7, synthetic word sev1 63.1→71.8 / sev2 63.6→79.6 — but headline broad char/word barely moved (77.1→77.3 / 52.3→52.4) and dev-8 char dipped 85.8→83.9: the flywheel now polishes glyphs it already reads while the residual errors live in segmentation/layout, not classification. Diminishing returns reached in 2 rounds; further harvest breadth (legal.3B) queued, further letter re-harvests not. CROSS-DOMAIN MERGE STUDY (2026-09-01, before merging legal.3B): the hypothesis that the class-level medoid/cap discards a minority family's mode motivated three family-aware merge variants — ALL measured worse than the plain pooled cap-80 on broad-30 (pooled 77.3 char): (a) equal per-family budget shares starved the dominant family (76.6, −0.7); (b) full cap-80 per family flooded the pool with real English exemplars until distance calibration shifted and ACCENTED classes started rejecting (French decode test went red — accent classes have no harvested exemplars to balance them); (c) per-family inlier filter + pooled cap gave the best dev-8 of the project (85.1/65.0) but still lost broad (76.6/52.0) and still broke the accents test. Reverted to pooled; lesson: the pooled class medoid IS the balance point between synthetic and harvested mass, and dev-8 optima that lose broad-30 are overfits to the 8 tuning pages. LEGAL.3B HARVEST (2026-09-01): 44,537 exemplars from 120 non-eval pages (contamination guard widened to seed-1[:30]+seed-2[:30] first — legal evals default to 10 seed-1 pages). Through the pooled merge: legal-10 FLAT (79.6/54.3 vs 79.9/54.9 — legal pages were already well-read without their own glyphs), broad-30 flat (77.5/52.2), dev-8 85.6/66.0. Root cause of the flatness: POOL SATURATION — at cap 80/class, 120k harvested exemplars compete for ~4.7k merged slots, so a 44k-exemplar harvest nets ~100 merged additions. Harvest breadth via this merge is exhausted; more real data cannot reach the model without a different consumption mechanism (bigger model, per-domain prototype sets, or self-training beyond 1-NN). The error frontier stays segmentation/layout. |

| ✅ Citation labels take no digit-mode bias | Numeric-context decoding vs legal citation grammar ("§ 234.3(a)(17)(ix)"); the deferred-decision rule | `decode/beam.py` `_beam_word_mode` (`flanked`) | Every Federal Register citation read "(a)" as "(3)" and "807(b)" as "807(6)". The candidate dump showed 'a' top-1 at 19 with 'e' second at 39 and NO '3' in the list: digit mode injected '3' as the digit twin of the rank-2 'e' with the boost attached, and it edged the 'a'. A lone glyph whose neighbours carry '(' and ')' in their top three is a label — letter or digit equally — and is decoded on pixel evidence alone: no digit boost, no twin injection. FR page: every citation label restored, 16 lines changed, none for the worse. Sets: modern 68.7 → 68.8 word, dev-8 / legal-8 / broad-30 exactly flat — the rule fires only where labels are, which is what a rule should do. Also measured: parentheses without the digit-mode boost (`digit_mode_parens=false`) — modern 68.7 → 68.8 word, neutral; the boost stays. |
| ✅ Alternative cuts may sit close: 'the' read 'he' | Chopping with several candidate chop points (Smith 2007 §4.1); this project's ranked cut options | `glyph/components.py` `split_cuts` (2), separation `piece // 2` | broad-30 error census (30 pages, 1,423 missing / 1,314 spurious words): the largest single class was 'the' missing 41 times against 'he' spurious 26, with 'with', 'will', 'this', 'that' behind it — the 't' of a touching 'th' vanishing. Candidate dump: the 35-px 'th' blob reads 'h' at distance 18 (and adaptation pins it 'h'); its ONE split option cut at 22 px — inside the 'h', between stem and leg, where the ink minimum is — and the pieces read 'f' + '1'. The t|h kiss 9 px away had been excluded by the ranked-cut separation, which equalled the minimum piece width: right for simultaneous cuts, wrong for alternatives. Alternatives now need only half a piece apart and two are offered: census page 8726 reads 14 of 14 'the' (was 4), zero 'he'. Sets (with the word-split gate below): dev-8 94.2/85.0 → 94.3/85.3, broad-30 88.7/73.4 → 88.6/74.3 (recall +0.4, precision +0.8), legal-8 87.1/69.1 → 87.0/69.8, modern 83.3/68.8 → 83.5/70.0 (recall +0.7, precision +1.3). The census pointed straight at it: one hour from the frequency table to the fix, against days of guessing before. Three cuts (2026-09-06): the remaining 'he' page (8531, a thicker t-h bridge) reads 7 of 14 'the' with three cuts against 5, but the sets say no — dev-8 85.2 → 84.6 word, legal-8 flat, broad-30 75.3 → 75.2, modern 70.5 → 70.3; more alternatives feed the split bonus more wrong pieces. Two stays. |
| ✅ Word split gated on gap width | Missing-space repair vs compound names; gap statistics of inter-letter vs inter-word spacing | `decode/beam.py` `split_min_gap` (0.18 x-height) | The lexicon-driven split of an unknown 7+ letter word into two endorsed words cut 'Mathcad' into 'Math cad' 15 times on one page (and 'TurboCAD'). A lost space leaves a gap wider than a letter pair's (~0.1 x-height); a compound name does not. The split now requires the gap at the cut to reach 0.18 x-height: page 8753 keeps 'Mathcad' (7 of 19 read whole; the rest are other errors), no stray 'Math'. |
| ✅ Everything inside an art zone is art | Layout practice: text and graphics are exclusive regions (Wahl, Wong & Casey 1982 block classification; Tesseract's image-region masking) | `layout/imagezones.py` (inside-box absorption after zone boxes are final) | Census: the biggest spurious class on broad-30 was 331 tokens of one or two characters, and the two worst pages shed 41 and 27 lone letters — from a letterhead emblem and a church logo (an open book), not from text. The zone detector had caught each emblem's thick strokes but absorbed only neighbours larger than 4× the median glyph, so the emblem's glyph-sized crumbs (edge speckle, the gaps between strokes) stayed 'text' and read as 'x', 'u', 'J', '-u.--'. Now every component whose box lies inside a zone box (grown by the absorb gap) joins the zone: page 8519 lone-letter lines 10 → 4, spurious words 325 → 313 against 298 truth; the two survivors are page-edge scraps outside any zone. First version absorbed inside EVERY zone box and ate the typed 'Sincerely' under a signature zone on dev-8 (fill 0.08 of its box; the emblem fills 0.28): now only zones filling ≥ 0.15 of their box absorb. Sets (with the pin fix below): broad-30 88.6/74.3 → 88.9/75.0 (precision +0.6), dev-8 recall +0.2 / word −0.2, legal-8 and modern exactly flat. |
| ✅ Adaptation pins follow position for the comma/apostrophe pair | Per-document adaptation (Smith 2007 adaptive classifier) meeting this project's position priors; the case-twin pin geometry already there | `decode/beam.py` (pin block, `_baseline`) | Census: "you'll" → "you,ll" 6 times. The apostrophe glyph (13×18 px, 0.76 x-heights) had ',' at rank 1 with "'" second AND a comma pin: adaptation clusters the two marks (one shape, two heights) and pins the cluster's majority — commas — onto every member, and the pin bonus outweighed the position prior. The pin now re-targets by position as the case pins do by height: a pinned ',' whose box floats above the baseline becomes an apostrophe pin, a pinned "'" that hangs becomes a comma pin (thresholds shared with the position prior). Census page 8542 restored; unit-tested both ways. Measured together with the zone absorption above (the pair is inseparable in the runs): broad-30 word +0.7. |
| ✅ Quotes and brackets are language-model transparent | Character LMs over a restricted alphabet; punctuation as out-of-vocabulary (cf. Tesseract's separate punctuation DAWG, kept apart from the word DAWG for the same reason) | `decode/beam.py` `WRAPPERS`, `wrapper_lm_logp` (−4.0); merged/split pseudo-glyphs now carry `_baseline` and `parts`; floating short glyphs penalize alphanumerics; `''`/`""` fold to `"` | Bills census: 70 of 629 missed words were quoted citation labels (`‘‘(i)’’`, folded to `"(i)` by the scorer), read "u(i)" or "(((ii)"; broad-30 has 23 more. Four causes, found one at a time with the per-glyph score table: (1) the curly opening quote is two 9×18 tick blobs that merge into one glyph, and the merged pseudo-glyph carried no baseline, so no position prior ever judged it; (2) it carried no part count either, so the multi-part '"' class paid the dot prior; (3) letters do not float — a short glyph a quarter x-height above the baseline now penalizes alphanumerics; (4) decisive: the trigram alphabet is letters + apostrophe, so a quote at word start scored the floor (−13.8 vs −4.2 for 'u') and poisoned the next transition to the floor too, and the GRU was worse (−23.4). Quotes and brackets now cost a flat −4.0 and are stripped from the trigram context. Bill page hr500-p005: 5 of 5 quoted labels correct (was 0). Sets: modern 83.5/70.0 → 83.6/70.5 (recall +0.5, precision +0.5), broad-30 88.9/75.0 → 89.0/75.3, dev-8 word +0.1, legal-8 char −0.1. Lesson: a language model over a restricted alphabet is not neutral about what it cannot spell — it is maximally hostile, and the hostility hides until a letter competes with the punctuation. |
| ✅ Deslant at 3x with block-OR; stroke target from the glyph, not the canvas | Shear normalization by second moments (classic slant correction, cf. Bozinovic & Srihari 1989); stroke-width normalization (Tesseract's outline normalization does the same job differently) | `glyph/features.py` `_deslant`, `extract_features` | Bills census: the italic enacting clause on every first page read 'Remote' for 'Senate', every glyph 70–100 units from its nearest prototype, with the roman on the same page at 20. Widening the stock with the open TeX Gyre italics (Schola is the bills' Century face) did NOT help — and Times New Roman Italic's own 'e' at 28 px, a face in the stock, read as '-'. Tracing the extractor: (1) the order-0 shear on the binary mask dropped a quarter of a hairline's ink (52 → 38 px) wherever the nearest-neighbour sample fell beside a 1-px stroke; a linear resample thresholded at 0.5 lost the same; shearing at 3x and returning by block-OR keeps every pixel (a 24-px leaning bar comes back vertical at 24 rows). (2) The stroke normalizer's target width is a fraction of the glyph's size — measured on the PADDED deslanted array for italics and on the render CANVAS for synthetic glyphs, so an italic dilated to 3.8x its ink and lost its counter, and every render was normalized to a fatter stroke than the tight page crop it must match. Cropping to ink before the normalizer fixes both: TNR Italic 'e' 52 → 68 px, one counter (was 148 px, none). Measurement had to be done carefully: the harvest files store FEATURE VECTORS from the old extractor (137k exemplars, a third of the live pool), so a rebuild that merged them was confounded and measured negative (modern 70.5 → 69.2 word). Isolating the features — old against new, both built from renders alone, no harvests — is decisive: dev-8 73.0 → 83.5 word, broad-30 64.6 → 73.8, within 1.5 points of the live model WITH its harvests. So the harvests had largely been compensating for the render/crop stroke mismatch: real crops matched real crops while the synthetic prototypes stood systematically off. The harvests were regenerated under the new extractor (letters 200 pages → 86,730 exemplars, legal 45,594, digits 1,919 — twice the old digit count) and the truth file re-featured from its stored crops; the live prototypes, pool and MLP rebuilt. Four sets, old live → new live: dev-8 94.3/85.2 → 94.3/86.0 (recall 89.6 → 90.4, precision 88.8 → 89.5), broad-30 89.0/75.3 → 89.6/76.5 (82.1 → 82.9, 83.6 → 84.6) — the best headline yet — modern 83.6/70.5 → 83.8/70.5, legal-8 86.9/69.8 → 86.4/69.3 with recall 82.1 → 83.4 and precision 81.5 → 83.4. Also learned: the 'o'-hole shape gate was right to reject TeX Gyre Termes Italic — its 'e' renders as junk at every size — so stocked names get ordering priority but no gate bypass. |
| ✅ Page x-height anchor from mixed-case lines | Case decision by size (this project's height prior); x-height estimation from bimodal ascent distributions (cf. Tesseract's per-line x-height fitting with a page-level fallback) | `decode/beam.py` `_low_mode`, `_page_x_height`, `_line_x_height` | The rebuilt live model lost 4.3 word on one legal-8 page while gaining precision everywhere else; that page read 'CONSENT' as 'consENT' with the classifier putting every letter upper at rank 1 by wide margins. The decoder's size twins (c/C, o/O, s/S) go by the line's x-height, and an all-caps line's own median IS its cap height; the per-line caps rule falls back to a PAGE anchor — which was the median of per-line median ascents. On a pleading, mostly capitals and typewriter lowercase full of ascenders, that median is a cap height too (27.5, caps at 29–31, x-height 20): the rule could never fire. The anchor is now the median of the true x-heights of the page's MIXED-CASE lines (each line's low 2-means mode, exposed by `_low_mode`; caps lines contribute nothing). Page 9462: recall 67.2 → 73.3, precision 70.3 → 76.6; dev-8 letter 8530 also up 0.8 recall. Sets (anchor alone): legal-8 86.4/69.3 → 88.4/72.6, broad-30 89.6/76.5 → 89.7/76.6, dev-8 flat, modern 70.5 → 69.7 word (invoices/payslips: their numeric lines now anchor on the page x-height too, and the smaller scale exposed the calibration defect below — fixed there, not here). Two companions tried and reverted: gap thresholds scaled by the line's median glyph height, and a tall reward for capitals in the height prior — together dev-8 and broad-30 −1.5 word with legal-8 flat. |
| ✅ Pixel-evidence temperature from the top-1 distance | Softmax calibration of distance-based classifiers (Platt-style scaling; Tesseract converts its rating to a confidence with a fixed scale, not a per-list one) | `decode/beam.py` `_glyph_logprobs`, `evidence_temp_frac` | Chasing 'Elm' → 'blm' on an invoice (the 'E' at distance 8, 'b' at 25.5, a 3x ratio): the per-glyph softmax used the candidate list's STANDARD DEVIATION as its temperature, and a top-14 list always carries far junk at its tail (125, 140…), so the std was ~50 and the two leaders came out 0.3 nats apart — a margin the language model overturns at will. Every pixel decision in the system had been flattened this way. Temperature = 0.5 x the top-1 distance instead (floor 1.0): the same pair is 4.4 nats apart. Three-page probe: invoice-helvetica-0 recall 85.8 → 91.5, legal 9462 74.1 → 87.1 (precision 78.2 → 91.8), letter 8530 87.7 → 90.0; the 'the' page 8726 and the logotype page 8557 also up. Sets, together with the x-height anchor above: dev-8 94.3/86.0 → 94.7/87.2, legal-8 86.4/69.3 → 90.3/78.5 (recall 83.4 → 88.1, precision 83.4 → 87.7), broad-30 89.6/76.5 → 90.8/79.8 (85.1 / 87.1), modern 83.8/70.5 → 84.2/70.7 (88.4 / 89.0). The largest single-change gain in the project's record. Sweeps at the new scale: temperature fraction 0.7 worse everywhere, 0.35 better than 0.5 on all four sets (dev-8 87.2 → 87.3 word, legal-8 78.5 → 79.2, broad-30 79.8 → 80.3, modern 70.7 → 70.8), 0.25 no better than 0.35 — adopted 0.35; lm_weight 0.35 and 0.7 both within 0.3 word of 0.5 in either direction across three sets — kept at 0.5. split_char_bonus 1.5 / 3.0 against 2.2: 1.5 dev-8 +0.2 word (precision +0.4), legal-8 and broad-30 flat; 3.0 slightly worse — kept at 2.2 (a lean, not a win). pin_bonus 1.5 / 4.0 against 2.5: 4.0 dev-8 +0.2, broad-30 +0.2, legal-8 −0.8; 1.5 worse everywhere — kept. digit_mode_boost 0.7 / 2.0 against 1.2: 0.7 leans +0.1..+0.2 word on three sets, 2.0 leans down; merge_char_bonus −2.0 / −0.5 against −1.0: inert to 0.1. All kept. The weights tuned on the flattened scale sit near their optima on the sharp one: the temperature was the lever, not the weights around it. |
| 🔬 Serif italics from the TeX Gyre stock — mixed, not stocked | Stock widening (this project's font-stock rows); the open TeX Gyre faces are metric clones of Century Schoolbook, Times, Palatino and Bookman with true italics | `factory/stock.py` BODY_NAMES (+6), `factory/fonts.py` (.otf discovery, stocked names first), `~/Library/Fonts/mlws-ocr-stock/` | Motivated by the bills' italic clause; first measured on the OLD features and did nothing, because the extractor was the problem. Re-measured on the fixed features: renders-only variant dev-8 83.5 → 84.3 word, modern 68.8 → 69.7, broad-30 −0.2; on the live recipe with regenerated harvests: dev-8 86.0 → 86.2, legal-8 69.3 → 69.1, broad-30 76.5 → 76.7, modern 70.5 → 70.8 (char mixed within 0.4). Small and consistent on word accuracy under the OLD evidence calibration; rebuilt into the live models after the temperature fix it turned char-positive and word-negative — dev-8 94.7/87.2 → 94.8/87.6, legal-8 90.3/78.5 → 90.8/78.1, broad-30 90.8/79.8 → 91.0/79.3, modern 84.2/70.7 → 84.0/70.2 (recall +0.4). Word accuracy is the headline, so the six faces are not stocked; the pre-italic models are restored and the fonts stay installed for a later `--add-fonts` try (the bills' italic clause is now a decoder matter, since the same glyphs read 'Senate' and 'assembled' with the fixed features). Two of the eight fetched faces were out from the start: TeX Gyre Termes Italic renders junk 'e' at every size and Schola Bold Italic fails the same 'o'-hole gate — the gate was right. |
| ✅ Cut candidates one per valley | Chop-point selection at profile minima (classic projection-profile segmentation; Smith 2007 §4.1 ranks candidate chops) | `glyph/components.py` `_cut_candidates` | The census after the calibration fix still showed 'the' → 'he' on page 8531 (8 of 14). Its 41-px 'th' blob's ink profile: the h's arch is a plateau of four columns at 4 px, the t|h kiss two columns at 5 px. Ranking raw columns put BOTH offered cuts in the plateau (25 and 28, three columns apart) and never reached the kiss. Candidates are now one per valley — a maximal run of equal profile values no higher than its flanks, represented by its centre — ranked by depth. Page 8531: 'the' 5 → 7 of 14, 'he' 5 → 2, recall +0.5; page 8726 keeps 14 of 14. Unit-tested on a synthetic profile. Sets (at temperature 0.5): modern 70.7 → 71.1 word (recall +0.6, precision +0.5), broad-30 79.8 → 79.9, dev-8 flat, legal-8 word flat with recall −0.5 — adopted. |
| ✅ Spacing scales with the line's own size, case with the page's x-height | Word-gap thresholds relative to type size (classic inter-word gap statistics, e.g. Wong, Casey & Wahl 1982) kept apart from the x-height that decides case | `decode/beam.py` `space_scale` ("line") | After the page x-height anchor (correct for CASE), the modern payslips lost 6 word: their digits-only lines now took the page's x-height as their scale and the gap thresholds shrank, splitting '$150.90' into '$1 50.90' and '11/09/2025' into '1 1/09/2025'. Two scales tried: the line's median glyph height ('size') restores the payslip and invoice but wrecks prose (letter recall 93.7 → 77.6: a line rich in ascenders puts the median on the tall mode); the line's own x-height estimate WITHOUT the page-anchor substitution ('line': low mode when bimodal, glyph height when uniform) restores both — payslip-avenir-1 recall 84.9 → 89.0, precision 78.5 → 85.5; invoice-helvetica-0 85.8 → 91.5 / 82.0 → 92.4; the letter unchanged. Lesson: one estimate served two consumers with different needs; the anchor fix helped case and silently broke spacing until the census-by-kind showed the payslips falling. Sets: modern 70.8 → 71.7 word (recall 88.6 → 89.1, precision 89.2 → 90.1), broad-30 80.3 → 80.4, dev-8 87.3 → 87.2, legal-8 79.2 → 78.7 — the typewriter pages' uniform caps lines take their glyph height and the wider thresholds fuse words, so fixed-pitch pages keep the anchored scale: legal-8 back to 90.5/79.2 exactly, dev-8 unchanged at 94.4/87.2. Adopted. |
| 📊 Reading order re-measured: closed | Zone-order scoring (UNLV .uzn zones) as the upper bound on ordering | `scripts/eval_unlv.py --zone-order` | The roadmap carried 1.4 word points on letters as the reading-order gap. On the adopted model, dev-8 scored in the truth's zone order is 94.5 / 86.4 / 91.2 / 89.1 against 94.4 / 87.2 / 91.1 / 90.6 in our own order — the truth ordering is no better (slightly worse on word: zone boundaries split words the layout keeps whole). Column-first XY-cut and row alignment of column blocks closed the gap; item retired. |
| ✅ Bullets named by geometry | Position/size-based naming of untrained marks (this project's '\|' rule); UNLV's '~' bullet convention | `decode/beam.py` `_is_bullet` (standalone post-pass), `scripts/eval_unlv.py` fold '•' → '~' | Census after the calibration fix: the top spurious token was '.' (23) — on page 8522 a column of 15×15-px squares, the list bullets of a business letter, each read as a confident period (distance 6). UNLV writes bullets '~' (559 in bus.3B). A lone glyph with '.' at rank 1 whose box is roughly square and 0.6–1.3 x-heights tall is a bullet, emitted U+2022 and folded to '~' by the scorer. Page 8522: 5 of 10 bullets, recall 82.9 → 84.3, precision 83.4 → 84.8; the other five '~' are UNLV's em-dash convention ('-~' for a long dash we read '-' or '--'), not glyphs, and are left to the scorer's judgement. Same page, same census class: two 9×2-px slivers at the page's top edge read '-' on 'lines' whose x-height was 2 px; the output stage now drops any line whose x-height is under 5 px (`min_line_xheight_px`), never text at any dpi — three scrap lines gone, unit-tested. Sets, measured together: dev-8 87.2 → 87.5 word (precision 90.6 → 90.9), broad-30 80.4 → 80.6, legal-8 and modern flat — adopted. |
| ✅ Absolute glyph quality in path choices | Tesseract sums absolute ratings across a word's segmentation (Smith 2007 §4.2: chop/associate compared by rating, not by normalized probability) | `decode/beam.py` `abs_quality_weight` / `abs_quality_scale` (off by default until measured) | Business census: '(8.25%)' read '(8.2596)' on all eight invoices and 'payroll' read 'payro"' on all eight payslips. Both are path choices the per-glyph softmax cannot make: it is relative to each glyph's own list, so a '%' at distance 14.7 (excellent) and its two halves at 38 and 44 (poor) all score ~0 for their top candidate, and only the split bonus and digit boosts decide — for the split. Likewise a merged 'll' read '"' at 16.7 beats two singles at 0.0 and 9.1. A per-glyph cost of top-1 distance / 20 units per nat, constant within the list, restores absolute quality for split-vs-whole and merge-vs-singles: invoice-helvetica-0 recall 91.5 → 94.3, precision 92.4 → 95.2, 'Payroll' back, the 'the' page's splits intact. Also found on the way: a merged pseudo-glyph counted its pieces as 'parts', so any two merged letters earned the multi-part dot prior meant for quote ticks; only tick-sized pieces count now (unit-tested). Sets at weight 1.0: dev-8 87.5 → 88.9 word (precision 90.9 → 92.9), legal-8 79.2 → 79.6, broad-30 80.6 → 81.6 (precision 87.9 → 89.2), modern 71.7 → 72.7 (recall 89.1 → 90.0, precision 90.1 → 90.9) — up on every figure of every set; adopted. |
| ✅ Slant kept per glyph; the slash judged against the line's lean | Slant normalization loses slant (a known cost of deslanting, cf. Bozinovic & Srihari 1989); per-line skew/slant estimation | `glyph/features.py` `glyph_shear`, `recognize/stage.py` (`g["shear"]`), `decode/beam.py` `slant_prior` / `slant_min`, `SLANTED` | Census: 'social' read 'socia/', 'kowalski' 'kowla/ski' on the payslips. Probe: a clean Helvetica 'l', an 'I' and a '/' produce IDENTICAL feature vectors (distance 0.0 to one another) — the deslant shears the slash upright and no feature records the lean it removed; in a sans face the lean IS the difference. The recognize stage now keeps each glyph's second-moment shear (before deslant) and the decoder compares it with the line's median shear (italic lines lean as a whole): a glyph leaning right against its line by 0.15 favours '/' by 1.5 nats, one that does not is penalized the same. Payslip-avenir-1: every false slash gone, 'social' and 'kowalski' back; the invoice's dates keep their slashes. Set level it is within rounding of nothing (the false slashes are a few words per page on two faces); kept for the pages it fixes, at no measured cost. |
| 📊 Legacy-gap decomposition after the calibration fixes (2026-09-08) | Paired error-type analysis vs Tesseract legacy (`compare_legacy_errors.py`), as on 2026-09-01 | broad-30, 44,668 truth chars | Ours 91.4 char, legacy 95.5; excess errors over legacy 1,951. Shares of the excess: deleted characters 37% (1,180 vs 467), shape substitutions 28% (881 vs 340), deleted spaces 16% (369 vs 66), punctuation substitutions 11%, case 9%, digits 6%; we make FEWER inserted characters and spaces than legacy (−5% each). Reading: over half the remaining gap is characters that vanish — touching pairs read as one letter and words fused — the chopper's territory; the shape substitutions are the classifier's. The paired table: we win 3 of 30 pages and are within 5 char points on 18; the worst pages (8588: 89.9 char but 56.2 word) read 90 char with one error per word. |
| 🔬 Word-level chop ranked by aspect deviation — NEGATIVE, stays opt-in | Smith 2007 §4.1 word-level trigger, with a different choice of blob; this project's class-aspect table | `recognize/chop.py` `rank_by` ("aspect"), `require_better` off; `scripts/eval_pages.py` PIPELINE | The legacy-gap decomposition puts 37% of the excess in deleted characters and 16% in deleted spaces — touching pairs read as one letter. The word-level chop stage existed and measured inert. Probing 'Depatment' on a payslip: the stage chopped the word's two WORST-matching blobs, 'D' and 'a', and never the 'rt' blob, which reads 't' at distance 10 — a touching pair matches its single-letter reading well, so distance cannot point at it; its BOX can. Ranking the blobs by deviation from the top-1 class's trained aspect finds the 'rt' first; with Smith's undo rule off (the decoder now judges with absolute glyph quality and the lexicon), 'Department' reads. In the evaluation pipeline for the first time, with the undo rule off: dev-8 88.8 → 88.7 word, legal-8 79.6 → 79.4, broad-30 81.4 → 81.3, modern 72.3 → 71.8 — negative everywhere. With the undo rule ON the 'rt' cut is undone (its pieces, each carrying a serif stub, read worse than the whole 't') and the stage is inert, as before. Diagnosis holds from the lattice experiment: the pieces of a touching pair are not glyphs the whole-glyph classifier knows, so neither ranking nor the decoder's absolute quality can tell a right cut from a wrong one by their distances. What would work is a PIECE-AWARE scorer trained on labelled touching pairs — the print-and-scan loop's harvest — not a class per pair (a pair class multiplies confusions in nearest-prototype space, as the ligature classes did). The aspect ranking stays as the stage's default; the stage stays out of the pipeline. |
| 🔬 Short lines take the page's x-height — NEGATIVE, reverted | Per-line x-height estimation with a page-level prior (Tesseract fits x-height per line and falls back to the block's) | `decode/beam.py` `_line_x_height` (short-line branch) | Business census: the invoices' 'Qty' header read 'ots' on all eight, with 'Q', 't', 'y' at rank 1 by wide margins. A three-glyph line cannot be bimodal on its own glyphs ('y' alone in the low cluster fails the two-member rule) and its median ascent, 26 px against the page's 21.6, just misses the caps rule's 1.25 — so the line kept its cap height as x-height and the height prior turned 'Q' into 'o' and 'y' into 's'. A line of fewer than five glyphs, unimodal, whose median ascent lies within 0.8–1.6 of the page's x-height now takes the page's: table headers and labels are the page's type, not their own. 'Qty' reads on all eight invoices — and the sets say no: dev-8 88.8 → 88.7 word, legal-8 79.6 → 79.5, broad-30 flat, modern 72.3 → 72.1 (precision −0.3). Other short lines are NOT the page's type often enough (amounts, codes, captions) and the mis-scaled priors cost more than the headers gain. Reverted; the header case waits for a per-block type-size estimate. |
| 🔬 '@' named by geometry — NEGATIVE, reverted | Untrained-mark naming by shape (this project's '\|' and bullet rules); the '@' class itself measured negative (2026-09-03: it stole from a/e/0/O) | `decode/beam.py` `_at_sign_index` (standalone post-pass); `recognize/stage.py` publishes `g["holes"]` | Business census: 'billing@example.com' missed on all eight invoices, read 'biling(bexample.com': the '@' is a 32×31-px blob no class matches (best 'œ' at 80) that the chopper splits into '(' and junk. Inside a word, a roughly square glyph 1.3–1.6 x-heights tall with a hole whose best class distance is over 50 is an '@' — judged on the glyph's group whether the decoder read it whole or in pieces (the pieces' union box finds the group). Invoice-helvetica-0 reads the address — and the sets say no, everywhere: dev-8 88.8 → 88.5 word, legal-8 79.6 → 79.4, broad-30 81.4 → 81.2, modern 72.3 → 71.8. A square holed glyph that no class matches well is, on real pages, a degraded 'e', 'a', 'o' or 'g' far more often than an '@'; the geometric signature is not specific enough. Reverted (the second '@' negative; the first was a trained class). The address stays a known miss. |
| 🔬 A parenthesised label joins its number — NEGATIVE, reverted | Numeric token grammar (this project's numeric-join rules; forms-OCR field patterns) | `decode/beam.py` `_segment_line` (`paren_label`) | Business census: '401(k)' read '401 (k)' on six of eight payslips — an 11-px gap before the '(' at x-height 29 is 0.38 x-heights, just over the definite-space threshold, and the numeric-join rule never saw it: '(' is not a separator, and the context gate admits only data lines while '401(k) contribution' is prose. A digit followed by '(' joined regardless of the line's context: the payslips read '401(k)' — and the scan sets lost, dev-8 88.8 → 88.7 word, legal-8 79.6 → 79.3, broad-30 81.4 → 81.3 (footnote markers and citations that ARE spaced). Reverted: the fourth business-template fix of the day to fail the general sets. |
| 🔬 A gap band with one outlying gap — NEGATIVE, reverted | Word-gap estimation from a line's own gap distribution (2-means, this project's gap band) and its failure mode on lines with one outlying gap | `decode/beam.py` `_segment_line` (`band_max_join_frac` 0.55) | The paired table's worst page (8588, an order form: 89.9 char but 56.2 word against legacy's 95.1) read 'x' glued to its digits on every one of twenty '3 x 5 (lined)' lines. The line's gaps are 5–22 px plus one 276-px gap to the price column; the 2-means band clustered everything under 22 px as 'joined' on a 34-px x-height, and the x-height ratios never got a say. The band's lower edge may not exceed 0.55 x-heights — no intra-word gap is half a letter high — else the ratios rule. Six variants later the rule is: a band whose word-gap cluster has fewer than two members is degenerate and yields to the x-height ratios. Page 8588: recall 57 → 79.9, precision 76 → 86.5, the largest single-page gain of the week. The cost: a two-word line has one word gap too, and on a tightly kerned prose page (8528) the ratios call 12–14-px kerning gaps spaces (precision 84 → 71). Variants tried and dropped: a cap on the band's lower edge (0.55 x-heights — 8528's kerning and 8588's word gaps sit at the same ratio), reclustering without the outlying gap (shredded 8528's caps line), trusting narrow bands with several gaps above (shredded prose elsewhere), and a wider uncertain zone for degenerate bands ('x' is not lexicon-endorsed, so '3 x 5' stayed joined). Sets: dev-8 88.8 → 86.9 word (precision 92.8 → 91.2), legal-8 79.6 → 79.2 (recall +1.6, precision −0.9), broad-30 81.4 → 81.5 (recall 86.2 → 87.7, precision 89.1 → 88.3), modern 72.3 → 72.1. Recall up, precision down more; reverted. The order-form page stays a known miss: separating a lone column gap from a two-word line needs layout knowledge (the column) that the segmenter does not have. |
| 🔬 Wider legal harvest (300 pages) — neutral, not adopted | Training-set widening (the harvest flywheel rows) | `scripts/harvest_glyphs.py data/unlv/legal.3B --pages 300`, variant build in the scratchpad | The live legal harvest is 100 pages (45,594 exemplars); legal.3B has 300, of which the evaluation draws exclude 60. Harvesting all 240 gives 140,186 legal exemplars against 86,730 letters. Rebuilt and measured: legal-8 79.6 → 79.8 word but char 91.3 → 90.9, dev-8 88.8 → 88.5, broad-30 81.4 → 81.2, modern 72.3 → 72.1. Reading: the condensation keeps 90 prototypes per class from the whole pool, so tripling one source's share moves the k-means centres toward typewriter shapes and the proportional sets pay. More data of one kind is not more coverage; a per-source quota in condensation would be the way to add it (next). |
| 🔬 Condensation with per-source quotas — mixed, kept as an option | Stratified prototype selection; Hart's condensation applied per stratum rather than to the pooled set | `recognize/condense.py` `tag_quotas`, `condense(by_tag=True)`; `build_prototypes.py --condense-by-tag` | Motivated by the 300-page legal harvest: plain k-means over the pooled exemplars let the tripled source move every class's centres. With quotas, each class's 90 centres are split across its exemplar sources (font families, the harvests, the truth set) in proportion to the square root of their counts — nine times the exemplars earns three times the centres — with a floor of one, then k-means runs within each source. Two variants measured. Live harvests by tag: dev-8 flat, legal-8 flat, broad-30 81.4 → 81.1 word, modern 72.3 → 71.2 — the quotas take centres from the harvest sources that carry the modern pages. 300-page legal harvest by tag: legal-8 91.3/79.6 → 91.7/80.1 (the best legal figure yet), dev-8 and modern flat, broad-30 81.4 → 81.1. So the quotas do what they should — the wider legal harvest now helps legal without hurting modern — but the headline set pays 0.3. Not adopted; `--condense-by-tag` stays for the day a legal-heavy deployment wants it. Lesson: the pooled k-means is tuned to the current source mix by accident, and any change of mix, quota or not, moves some set. |
| ✅ Small type wants resolution: Federal Register at 400 dpi | Resolution requirements of feature OCR (Tesseract's guidance of ≥ 20 px x-height; Rice, Nagy & Nartker's UNLV studies on dpi vs accuracy) | `scripts/make_modern_set.py` (`DPI`), sets `data/modern_fr400`, `data/modern_fr_up400` | Three quarters of the modern set's missed words (2,468 of 3,331) are the twelve Federal Register pages: 8-pt three-column text whose x-height is 12 px at 300 dpi. The same pages rendered at 400 dpi through the same print model: 64.2 / 53.3 / 82.0 / 83.3 → 65.2 / 57.0 / 87.5 / 88.8 (char/word/recall/precision) — recall and precision +5.5 each; char accuracy barely moves because it is order-sensitive and these are three-column pages. Control: the same 300-dpi scans bicubically upscaled to 400 and tagged 400 dpi (no new information): 63.3 / 52.6 / 79.5 / 83.9 — WORSE than the originals (recall −2.5). The gain is information, not processing resolution: at 12 px x-height the hairlines are already gone from the image, and interpolation cannot restore them. Consequence: no internal magnify stage; the remedy for small type is at the scanner (400 dpi for 8-pt text), and the roadmap's 'FR small-type recall' item closes as a capture requirement rather than an algorithm. Legacy Tesseract's recall lead on these pages (92.0) is consistent: its classifier was trained on far more degraded small-type samples than our 29-face stock. |

| ✅ Outline evidence in float32 with precomputed segment geometry | Numerical-kernel practice; Tesseract's `intmatcher` keeps its prototype geometry pre-tabulated in integers for the same reason | `recognize/outline.py` `_SegmentBank` (`evidence()` kept as the readable reference; tested equal to 1e-6) | Profile of a business letter (2026-09-05): 20.6 s, recognize 12.8 s, of which `outline.evidence` 9.5 s over 2,820 calls — 70 features × 7,440 segments per gated glyph, all in float64 with (N, M, 2) intermediates and a complex exponential for the angle wrap. Batching classes into one call bought 7%. The same arithmetic on flat 2-D float32 arrays with per-segment geometry (A, AB, |AB|², θ) precomputed once per class: 5.8× on the kernel; page 20.6 → 14.1 s, recognize 12.8 → 5.9 s, output text byte-identical. Tried and dropped: partial partition instead of a full column sort in the rating (no gain; the sort was not the cost). Two more exact rewrites the same day: the rulings stage's line openings as run-length tests (identical to scipy's opening, 17×: 1.69 → 0.34 s) and deskew's profile variance from projected ink coordinates instead of 43 image rotations (same angle on 16/16 pages: 0.82 → 0.22 s); language models kept cached across passes. The letter: 20.6 → 11.6 s, text byte-identical throughout. Remaining recognizer profile: evidence 2.4 s (the pair count itself), rating 1.9 s, matcher load 0.3 s. Lever (b), configurations per class 31 → 12 by greedy max-min coverage over the class's own renders (`OutlineMatcher.condense`, `build_outline_protos.py --condense=12 --min-cover=0.85`): recognizer 7.2 → 4.0 s, the letter under 10 s (9.9); accuracy dev-8 identical, legal-8 +0.2 char / +0.2 word, broad-30 +0.1 / +0.1, modern flat (recall +0.1). Adopted: the live outline model is now the condensed one (1,225 configurations). |
| ✅ Engine profiles: classic / pure / neural (2026-09-09) | — | `configs/classic.toml`, `pure.toml`, `neural.toml`; `scripts/eval_pages.py` `load_pipeline`/`run_stages`/`--config`; `tests/test_profiles.py` | One codebase, three presets, because every network enters as a gated additive term and the other stages are shared. classic = the 2026-09-08 engine (MLP + char GRU); pure = classic with both off (`mlp_path=""`, `char_lm=""` — the GRU guard needed fixing: `Path("")` is the working directory and exists); neural = classic plus each heavier network as adopted. Every evaluation script takes `--config` and builds the same stage list as the CLI. Pure measured: dev-8 94.8/87.6, broad-30 90.9/78.3 (recall 82.9, precision 86.1), legal-8 90.3/75.8, modern 83.8/69.6 (85.8/87.0) against classic 95.1/88.8, 91.7/81.4, 91.3/79.6, 84.7/72.3 — the two light networks are worth about three word points on the headline set and four on the typewriter set. classic never gains a network term; after every neural adoption a classic four-set run must reproduce its row — first run 2026-09-09 after the sequence scorer landed: broad-30 91.7/81.4 (86.2/89.1) to the decimal; dev-8 95.1/88.7, legal-8 91.3/79.4 and modern 84.6/72.1 (89.4/90.4) against scoreboard entries of 88.8, 79.6 and 84.7/72.3 (89.7/90.7), which the previous commit reproduces exactly in a clean worktree (and two Python hash seeds agree), so the entries were stale by a tenth or two and are corrected, not the engine. |
| 📊 Offline rerank harness for the sequence scorer (2026-09-09) | n-best rescoring evaluation as in speech recognition (oracle vs rerank) | `scripts/seq_rerank_eval.py` (`dump` / `score`), `decode/beam.py` `_nbest_sink` | One pipeline pass dumps, per output word that aligns to a truth word, every segmentation variant with its decoder score, the truth, and the word's strip; any model and weight is then scored in seconds. dev-8 dump: 1,466 aligned words, 75 decoded wrong, 2.2 variants per word. The ORACLE (truth among the variants) is 95.4% against the decoder's 94.6% on all aligned words — 0.8 points of headroom for per-word rescoring; on split/merge readings 86.1% vs 72.2% (n=72), on length-mismatched words 56% vs 8% (n=25). The rest of dev-8's errors are words whose boundaries never aligned (fused or shredded words) and variants the enumeration never offered — the next two levers: rescoring the word-gap variants (deleted spaces, 16% of the excess) and widening the split suspects once a scorer can rank them (`split_width_factor` 1.3, `max_split_variants` 8). |
| ✅ Sequence scorer, synthetic-only model: rerank and gated string injection (2026-09-09) | As above; string injection is n-best augmentation by a second recognizer (ROVER-style combination, Fiscus 1997, reduced to one gated candidate) | `decode/beam.py` `seq_inject`, `seq_inject_margin`, `seq_inject_endorsed`, `_seq_rescore` | A 4-epoch model trained on the 250k synthetic windows ALONE (26 min on MPS, no real strips, Verdana/Tahoma held out) — the first pipeline numbers, at seq_weight 0.5. Offline harness on dev-8 first: rerank lifts aligned words 94.6 → 95.2 (oracle 95.4), split/merge readings 72.2 → 83.3, length-mismatched words 8 → 52%; the model's own greedy read is 93.3% right on aligned words and 93.0% on multi-variant words where the decoder's variants cap at 92.6% — the model can beat the enumeration, not only rank it, which is why injection was implemented. Pipeline, rescoring only (Phase A): dev-8 95.1/88.8 → 95.6/89.3, broad-30 91.7/81.4 → 92.1/82.0 (weight 1.0: 92.2/82.0, flat), legal-8 91.3/79.6 → 90.9/79.6 (the synthetic stock is thin on typewriter faces). Injection, ungated (the greedy string joins the variants with the decoder's best score when its CTC likelihood beats the best text by 3 nats): dev-8 95.5/90.4, broad-30 92.4/83.3, but legal-8 89.4/78.3 — wrong readings that out-score the decoder by 3 nats on typewriter pages. Injection gated to strings the lexicon or a numeric format endorses (`seq_inject_endorsed`): dev-8 **95.8/90.5**, broad-30 **92.9/84.3** (recall 88.7, precision 91.9), legal-8 90.8/79.3 — the gate helps on BOTH sets, so an endorsed string the image likes is a safe hypothesis and an unendorsed one is not. Synthetic page with injection: 99.8/99.0 · 99.8/99.0 · 99.5/98.1 (from 98.7/93.2 · 98.9/94.7 · 98.9/95.6). Modern (rescoring only, epoch-1 model): 84.7/72.3 → 85.3/73.2 (recall 90.7, precision 91.6). CAVEAT, found the same night: every pipeline number in this row was measured with ONE scorer forward per LINE and per-word slices of that posterior; the model is trained on windows of one to three words (≤ 512 columns) and its recurrent state does not survive a 980-column line — the greedy read of a whole line came out 'gNan', and the stronger full model, whose posteriors are sharper, injected junk ('of HMO National' → 'Nan', 'meeting,' → 'ng') and measured BELOW this weaker one (dev-8 95.2/89.1). The decoder now runs the scorer per word window, exactly as trained and as the offline harness always did; the numbers above are therefore a lower bound and the full-model row is the measurement that counts. Cost: about +3 s per dense page. Kept OFF in classic; the neural profile adopts the full model if the four sets agree. |
| ✅ Sequence scorer, full model (synthetic + real strips), per-word windows — ADOPTED in the neural profile (2026-09-09) | As above | `data/seq_en.npz` (from `scripts/train_seq.py --backend torch --epochs 12 --batch 256 --lr 3e-3`), `configs/neural.toml` | Trained on 245k synthetic windows + 31k bus.3B and 37k legal.3B truth-labeled real strips (×2), 12 epochs, 97 min on MPS at 483 s/epoch; page-disjoint held-out REAL word accuracy by greedy read 96.8% (88.3% on words the pipeline had read at the wrong length), synthetic 86.8% (75.9% with a touching pair). Harness on dev-8: the model's own greedy read is 97.5% on aligned words against the classic decoder's 94.6% and the enumeration's 95.4% oracle, 92% on length-mismatched words (decoder 8%), 96.8% on multi-variant words (decoder 90.1%). Pipeline, scorer run per WORD window (the line-posterior defect fixed), weight 0.5, injection gated to endorsed strings: dev-8 95.1/88.8 → **95.8/90.6**, broad-30 91.7/81.4 → **92.8/85.0** (recall 86.2 → 89.6, precision 89.1 → 92.6), legal-8 91.3/79.6 → **91.3/81.0**, modern 84.7/72.3 → **86.0/76.3** (recall 89.7 → 93.6, precision 90.7 → 94.7; by kind: letters 99.4/96.3, invoices 92.3/87.7, payslips 74.2/70.4, bills 93.0/74.6, Federal Register 67.3/62.4). Up on every figure of every set — the largest word-accuracy gain in the project's record, and it lands where the decomposition said the gap was (touching pairs, fused words). Ungated injection: dev-8 96.0/91.0, broad-30 92.6/85.3, legal-8 91.8/81.2, modern 85.7/76.0 — two or three tenths better on the UNLV word figures, worse on modern and on broad-30 char; the gate is adopted as the rule that wins the set with real modern faces. Two lessons: (1) what the model sees at inference must be what it saw in training — a line is not a window; (2) the offline harness predicted the pipeline gain correctly only once the pipeline cut windows the way the harness did. Classic regression guard re-run after adoption (see DESIGN §8). |
| 📊 Legacy-gap decomposition and census under the neural profile (2026-09-09) | As the 2026-09-08 decomposition and the missing-word census | `scripts/compare_legacy_errors.py --config configs/neural.toml`, `scripts/missing_words.py --config` | broad-30, 44,668 truth chars: ours 92.8 char, legacy 95.5; excess 1,303 (from 1,951). Shape substitutions 881 → 596 (share 28% → 20%), case 152, punctuation 124, digits 94; we now make FEWER inserted characters (275 vs 484), spaces and punctuation than legacy. What did NOT move: deleted characters 1,180 → 1,141 (52% of the excess) and deleted spaces 369 → 362 (23%) — three quarters of the remaining gap is characters and spaces that vanish. Census on the same pages: missing words 1,300 → 802, spurious words 1,072 → 507 (the scorer's largest visible effect is junk it no longer emits), suppressed lines 113 → 135. Top missing words are unchanged: '5', 'x', '3', '(lined)' from the order form's "3 x 5 (lined)" rows (template page 8588), then short function words ('and', 'a', 'the', 'to', 'of', 11 each) and proper nouns the lexicon cannot endorse ('usaa', 'rice', 'las'). Reading: the scorer fixed what it can see — a word window it is offered — and cannot restore a word that never became a window: fused words, words in suppressed or unsegmented lines, and short words lost at line boundaries. |
| ✅ Sequence scorer, Phase B: the beam's n-best rescored by the word window — ADOPTED; Phase C: gap variants — inert, opt-in (2026-09-09) | n-best rescoring, as above | `decode/beam.py` `seq_beam_weight`, `seq_beam_top`, `seq_gap_weight`, `_seq_window`, `_seq_costs` | Phase B: inside one segmentation, the top-8 beam strings are re-scored by −seq_beam_weight × (CTC nll − min) BEFORE the lexicon pass, so a lexicon-endorsed near-tie the image rejects cannot displace the pixel-best reading, and a shape substitution the language model liked pays for its pixels. dev-8 95.8/90.6 → 96.0/91.2 at weight 0.5, **96.1/91.3** at 1.0; at 1.0 on the four sets: broad-30 92.8/85.0 → **93.3/86.2** (recall 89.6 → 90.1, precision 92.6 → 93.4), legal-8 91.3/81.0 → **92.0/83.7**, modern 86.0/76.3 → **86.3/77.3** (94.3/95.5). Up on every figure of every set; adopted at 1.0. Phase C: the word-gap variants (which uncertain gaps are spaces) scored by one likelihood over the whole segment's window with the space class, entering the PRIMARY key at 0.1 and 0.2 lexicon units per nat: dev-8 90.5 word at both (−0.1) — inert, because the admissibility rule (every part endorsed) already decides these cases and a line fused into one string has no UNCERTAIN gaps to vote on; that case is the segment re-read's (next row). Kept opt-in at 0. |
| 🔬 Federal Register training strips by truth alignment — NEGATIVE yield, stopped; small-type synthetic set instead (2026-09-09) | Truth-aligned harvest (above); synthetic small type per `factory/words.py` | `scripts/make_modern_train.py` (pages 41–160 of the issue, far from the test's 9–20, through the modern set's print model), `scripts/harvest_lines.py --no-guard`, `scripts/make_seq_data.py --x-heights --x-weights` | The modern set's largest loss is the Federal Register (12 pages at 67 char / 62 word), so 120 other pages of the same issue were rendered for training. The line harvest labels a word only when both of its boundaries align to truth spaces, and on 8-pt three-column pages the classic pipeline reads at 67% char, so the harvest yielded five or six words per page (about 700 in all) at a minute a page — stopped after 40 pages as not worth the machine time; the pages stay under `data/modern_train` for a harvest that can align by the PDF's own word boxes later. In its place, 100k synthetic windows at x-heights 9–13 px (weights 1:2:2:2:1) from `data/corpus_en_modern` through the same renderer, 44% with a touching pair, 4 min — `data/seq_synth_small_v1.npz`, folded into the second model (`seq_en_v2`: both synthetic sets + the two UNLV harvests, hidden 128, 12 epochs). |
| ✅ Segment re-read by the sequence scorer — ADOPTED (2026-09-09) | Recognizer-driven re-segmentation: the reading of a whole segment, with spaces, placed back on the image by the emission frames of its characters (CTC alignment as a segmentation, Graves 2006) | `decode/beam.py` `seq_reread`, `_seq_reread`; `recognize/ctc.py` `greedy_decode_frames` | Motivated by the deletion probe: of broad-30's deleted characters, 218 sit inside matched lines and 2,177 in truth lines with no matching output line — lines that EXIST but were fused into one string ('wherevouwantl.t'completer.ththeblankshoryoutoR.lli') or set in a letterhead face outside the stock, so that no word window was ever offered at the right span. When a segment's decoded words are mostly unendorsed (endorsed fraction < 0.5) and long enough (≥ 8 chars), the scorer reads the whole segment window greedily; the reading is split at its space emissions, each word's page span comes from the frames of its first and last character (2 px a frame through the strip scale), every group joins the word whose span holds its centre, and the reading replaces the decoder's words only if it endorses MORE of them. Page 8753: 'andseloct56operators' → 'and select 56 operators', 'itspowe"leafle-boicongine' → 'its powerful Maple symbolic engine', two dropped lines back, +58 words right on the page. Over the profile before Phase B: broad-30 92.8/85.0 → 93.0/85.4 (recall 89.6 → 90.3), modern 86.0/76.3 → 86.0/76.5, legal-8 91.3/81.0 → 91.2/81.0, dev-8 95.8/90.6 → 96.0/90.6. On top of the adopted Phase B: dev-8 96.1/91.3 → **96.2/91.3** (recall +0.3), broad-30 93.3/86.2 → **93.4/86.5** (recall 90.1 → 90.7), legal-8 92.0/83.7 → **91.9/83.8**, modern 86.3/77.3 → **86.3/77.5** (94.5/95.6). Word, recall and precision up on every set; char flat or up on three and one tenth down on the typewriter set — adopted, with that tenth recorded. The remaining unmatched lines are the ones the re-read cannot endorse: foreign text (the French letterhead of 8559), proper nouns and addresses, and faces the model has never seen. |
| 🔬 Second sequence model, wider and longer with small type (`seq_en_v2`) — MIXED, not adopted (2026-09-09) | As above | `data/seq_en_v2.npz` (`train_seq.py --synth data/seq_synth_v1.npz data/seq_synth_small_v1.npz --hidden 128 --epochs 12`) | 479k windows (245k general + 100k small-type synthetic + the two UNLV harvests ×2), hidden 128 (about 480k parameters), 129 min on MPS. Held-out real words 96.9% (v1 96.8), hard subset 93.8% (88.3), synthetic 87.0% (86.8). Harness on dev-8: greedy read 98.0% of aligned words (v1 97.5), 96% of length-mismatched (92), 97.9% of multi-variant (96.8). Pipeline under the adopted profile (rerank + gated injection + Phase B + re-read): dev-8 96.2/91.3 → 96.5/91.4, broad-30 93.4/86.5 → 93.6/86.7, but legal-8 91.9/83.8 → 92.3/83.0 (char +0.4, word −0.8) and modern 86.3/77.5 → 86.2/77.1 (bills 76.8 → 75.8). Loses on two sets; stays a variant file. The small-type windows did nothing for the Federal Register pages (67.5 → 67.6 char): whatever limits them is not the model's reading of 8-pt type in isolation — the three-column layout, the line x-height on mixed footnote lines, and the re-read gate's dependence on lexicon endorsement (agency names, CFR citations) are the suspects for a census of those twelve pages. |
| 📊 Sequence scorer runtime: numpy for one window at a time (2026-09-09) | — | `recognize/seq.py` `load_scorer`, `decode/beam.py` `seq_backend` | A letter page: classic 12.8 s, neural 17.9 s on the idle machine. Profile: 542 word-window forwards through torch/MPS at 10.9 ms each (5.9 s) and 0.8 s of CTC arithmetic; the same page through the numpy reference 17.2 s against torch's 22.0 s under equal load. A single 32 x 180 window is launch-bound on the GPU, so `auto` now means numpy for inference; `torch` stays selectable and pays for training and for batched offline scoring. With numpy as the default the same page reads in 14.2 s against classic's 12.7 s on the idle machine: the three terms cost 1.5 s, and batching a line's windows before decoding would recover a second at most — not worth a mechanism; recorded instead of built. |
| 🔬 Unendorsed string injection at a 6-nat margin — MIXED, gate kept (2026-09-09) | As the injection row | `decode/beam.py` `seq_inject_endorsed=false`, `seq_inject_margin=6.0` | On the adopted profile (rerank, gated injection, Phase B, re-read): dev-8 96.2/91.3 → 96.2/91.2, legal-8 91.9/83.8 → 92.5/83.9, broad-30 93.4/86.5 → 93.1/86.6, modern 86.3/77.5 → 85.9/77.2. A wider margin than the 3 nats measured earlier does not change the picture: an unendorsed string the image likes wins on the typewriter set and loses on the modern set and on headline char. The lexicon gate stays; the words it withholds (proper nouns, addresses, foreign text) are the re-read's remaining gap too, and want a mechanism that can vouch for a string without the lexicon — a per-document word list built from confident reads, the classic adaptation idea at word level. |
| 📊 The Federal Register "loss" is the truth's reading order, not recognition (2026-09-09) | Evaluation ground truth from a PDF text layer: content-stream order vs visual reading order (poppler's pdftotext implements the latter) | `scripts/make_modern_set.py` `pdf_page_text`, `--truth-only` | Census of the twelve Federal Register pages under the neural profile: bag-of-words recall 91–95% and precision 93–97% on every page, sequence word accuracy 47–85%. Legacy Tesseract, run on the same twelve pages, shows the SAME pattern page for page (p011 50.9/50.3 vs ours 50.5/46.8; p016 90.8/87.8 vs 90.5/85.1; recall 95–98%). A gap that size between bag and sequence with two independent engines is the truth's order: pypdf's `extract_text` returns the content stream's order, which on three-column pages is not the order a reader follows. Scored against poppler's reading-order extraction, our page 11 output reads **91.5%** word accuracy (46.8% before), page 16 86.2% (85.1%); `-layout` mode interleaves the columns row by row (7.5%) and `-raw` is the stream order again (45.5%). The bills gain too (p3 of HR 500: 78.1 → 85.1; p2 of HR 1500: 73.5 → 79.0). Consequences: (1) the Federal Register was never the modern set's largest loss, and the small-type training data's null result on it was expected; (2) every PDF page's truth is rebuilt in visual order (`--truth-only`), which re-baselines the modern figures of classic, neural and legacy alike — re-measured below; (3) what the census did find on those pages that is recognition: '§' is not in the class set and comes out as a stray 's' (120 missing, 69 spurious), the running header's slashes fuse three tokens on every page ('register/vol.', '52/friday,', '2024/rule5'), hyphenated words break at the hyphen in 8-pt type ('third-party' → 'third-py'), and 'FMU' in small caps loses its F. |
| ✅ Per-document word list from the sequence scorer — ADOPTED (2026-09-09) | Per-document adaptation at word level: the cipher-solving argument (repetition on one page is evidence the lexicon cannot give) applied to the scorer's readings; cf. the cluster-refit glyph adaptation | `decode/beam.py` `doc_words`, `doc_words_min_len`, `doc_words_min_count`, `_collect_doc_words`, `_endorsed`; word records carry `seq_greedy` and `seq_agree` | Every gate that admits a scorer reading (injection, the re-read, the gap admissibility rule) asks the lexicon or a numeric format; proper nouns, addresses and foreign text fail all three and were most of what the re-read could not fix. In pass 1 every word window is read by the scorer and the reading stored on the word; pass 2 builds the page's list: readings that recur (two distinct words) or agree with the decoder, at least three letters, alphabetic, unknown to the lexicon — and treats them as endorsed. Two false starts recorded: agreement alone is circular (the words that need vouching are the ones the decoder never reads right — a page with 'usaa' six times listed only 'danise'); and the reading was stored only for multi-variant words, so most words never counted. Fixed: dev-8 96.2/91.3 → **96.2/91.4**, legal-8 91.9/83.8 → **92.0/84.3**, broad-30 93.4/86.5 → **93.4/86.8** (recall 90.7 → 91.0), modern 86.3/77.5 → **86.3/77.7** (old truth). Up on word on every set, char flat or up; adopted. The agreement-only version measured a tenth or two behind on each set. The re-read now also applies to each run of unendorsed words inside a segment (a junk 'SAHCAAVE' between an endorsed street number and state), measured flat on dev-8 on its own. |
| 📊 Seed variance of the sequence scorer (2026-09-10) | Run-to-run variance as the yardstick for model comparisons | `data/seq_en_v1s1.npz`, `data/seq_en_v1s2.npz` (`train_seq.py --seed 1/2`, same recipe as the adopted model) | Two more seeds of the adopted recipe, 97 min each, under the full neural profile (char / word): dev-8 96.2/91.4 · 96.5/91.8 · 96.5/91.7; broad-30 93.4/86.8 · 93.5/87.0 · 93.5/86.8; legal-8 92.0/84.3 · 91.9/83.9 · 92.1/83.3; modern 90.1/84.6 · 90.0/83.9 · 90.0/84.5. Word-accuracy range across seeds: 0.2 on broad-30, 0.4 on dev-8, 0.7 on modern, 1.0 on legal-8 (eight typewriter pages). On the dev-8 harness the three greedy reads are 97.5 / 97.4 / 97.4 on aligned words. Consequences: the second model's "mixed" result (broad-30 +0.2, legal-8 −0.8, modern −0.4) was inside this noise, neither better nor worse; a NEW MODEL is adopted only when it clears about 0.5 word on broad-30 and 1.0 on legal-8 and modern, or wins with two seeds. Changes to the decoder's TERMS with one fixed model are paired comparisons on identical pages and are not subject to this noise, but their tenths generalize with the same uncertainty. The adopted seed stays live; the two others are kept as variant files. |
| ✅ Calibrated word confidence and a reject path (2026-09-10) | Platt (1999) calibration by logistic regression; Niculescu-Mizil & Caruana (2005) on margins vs probabilities; coverage/accuracy curves as in OCR reject-option work (Chow 1970) | `decode/wordconf.py`, `scripts/harvest_word_conf.py`, `scripts/train_wordconf.py`, `decode/beam.py` `conf_path`/`review_below`, `decode/output.py` `review_words` | A production engine must say which words to route to a person. The beam's margin orders words but is not a probability (held-out Brier 0.064). Evidence per word, all already computed by the decoder: beam margin, lexicon / numeric / document-list endorsement, the scorer's likelihood of the chosen text per character and its margin over the next variant, agreement with the scorer's own read, injected and re-read flags, length and character mix. Labels: 24,822 words from 120 non-evaluation UNLV pages under the neural profile, aligned to truth by word span; an output word in a matched line that no truth span covers is labelled wrong — the first fit, on aligned words only, saw a 98%-right population and could not learn the reject case (recorded). Page-disjoint holdout (24 pages): Brier **0.039** (beam margin 0.064, constant 0.084); reliability within a few points in every bin with enough words; coverage: keep p ≥ 0.9 → 89% of words kept at 96.6% right (92.5% base), p ≥ 0.98 → 58% kept at 98.1%. On dev-8 end to end: keep p ≥ 0.9 → 91.4% kept at **98.0%** (base 94.4%), the flagged 8.6% right 57% of the time. Largest weight: an INJECTED reading (−2.18 z-scored) — the scorer's own strings, net positive on the four sets, are the least reliable words individually, which is exactly what a reject path should know. Adopted in the neural profile (`p_correct` on every word, `review_words` in the run summary); the garbage gate keeps the beam-margin `confidence` it was tuned on. Not seen by the calibrator: words in lines that match no truth line at all (whole junk lines), which stay the garbage gate's. |
| 🔬 Long-window sequence model (`seq_en_v3`, fine-tuned on 2–6-word windows up to 1,400 columns) — within seed noise, not adopted (2026-09-11) | Fine-tuning from the adopted weights; longer CTC windows so the scorer can read a whole line | `data/seq_en_v3.npz` (`train_seq.py --init data/seq_en.npz --synth data/seq_synth_long_v1.npz data/seq_synth_v1.npz --epochs 4 --lr 1e-3`), `make_seq_data.py --words 3 8 --take 2 6 --max-width 1400` | 150k long windows (mean 338 columns, 27 characters) plus the original sets, 528k windows an epoch at 18 min, four epochs from the adopted weights; held-out real words 96.7% (adopted 96.8%), synthetic 86.5% with the long windows now in the holdout. Under the full neural profile: dev-8 96.2/91.4 → 96.1/91.5, broad-30 93.4/86.8 → 93.6/87.1, legal-8 92.0/84.3 → 91.8/83.7, modern 90.1/84.6 → 90.0/84.5 — every difference inside the seed range measured the day before (0.2 / 0.4 / 1.0 / 0.7). The epoch-1 checkpoint read broad-30 at 93.8/87.1, also noise. The model now reads whole lines without collapsing (its purpose), but reading whole lines did not by itself move the four sets: kept as the model the line read (next row) requires; the adopted seed stays live. |
| 🔬 Line-level read by the sequence scorer — inert on the letters, negative on typewriter; opt-in (2026-09-11) | The scorer as a hypothesis source for whole lines: CTC likelihood of the decoder's line text against the scorer's own reading, placed back on the groups by emission frame | `decode/beam.py` `seq_line_read`, `seq_line_margin`, `_seq_reread_span(mode="line")` | With the long-window model: the scorer reads the whole line window; its reading replaces the line's words when its likelihood beats the decoder's line text by seq_line_margin nats per character AND it endorses strictly more words (with "not fewer" it swapped right proper nouns for wrong ones at equal count: broad-30 char −0.4..−0.6 at every margin 0.5/1.0/2.0 for word +0.1..+0.2). Strict, margin 1.0, on the four sets against the same model without it: dev-8 96.1/91.5 → 96.1/91.5, broad-30 93.6/87.1 → 93.6/87.2, legal-8 91.8/83.7 → **90.9/84.0**, modern 90.0/84.5 → 90.0/84.5. Why it did not pay: the lines it was built for are letterheads in faces the scorer has never seen and foreign text, and the scorer's reading of those is not better than the decoder's — the deletion probe's 2,177 unmatched-line characters are not recoverable by a better READER of the same pixels; they want faces in the training stock (letterhead grotesques) or real scans of such documents. On typewriter pages the whole-line reading loses char where fixed-pitch spacing had been right. Kept opt-in at 0 with the long-window model as its variant file. |
| ✅ Unruled tables of one-line cells read row by row (2026-09-11) | T-Recs (Kieninger & Dengel 1998) column clustering, applied to one-line blocks; extends the row alignment of column blocks (`layout/rows.py`, 2026-09-07) | `layout/rows.py` `_single_cell_tables`, `row_groups` | The payslips read at 74 / 72 with recall and precision at 98.6%: every cell of the pay table was its own one-line block, the reading order emitted the table column by column, and the row mechanism needed blocks of three lines to align, so nothing fired. Now cells that share a baseline form a row, rows whose cells fall into left-edge columns recurring in three or more rows form a table, and a table is a CONTIGUOUS run of such rows. The first version, without the contiguity rule and at two rows, paired a bill's margin line numbers with its short headings into a "table" that swallowed the page: classic modern 88.2/78.9 → 76.9/66.7 (bills −12 to −56 word a page, invoices −30) while the payslip gained 33 — recorded, and the reason the shared-stage guard exists. Tightened: invoice and bill pages unchanged to the decimal, the Arial Narrow payslip 35.6 → 79.5 word (what remains there is the template's column-major header against a row-wise read, and a five-word line too long for a cell). Modern: neural 90.1/84.6 → **91.6/86.0**, classic 88.2/78.9 → **89.7/80.2**, payslips under neural 74.4/71.6 → 86.2/82.5; dev-8, broad-30 and legal-8 identical to the decimal under both profiles (the eager version was already identical there, and the tightened rule fires on a subset). |
| 🔬 '§' and '•' as glyph classes — NEGATIVE, not stocked; and what a model rebuild reproduces (2026-09-11) | Class-set extension as in the ligature and '@' experiments; a control rebuild to separate the class from the build | `factory/stock.py` `CHARSET` (unchanged), variant builds `build_prototypes.py` / `train_mlp.py` / `build_outline_protos.py` | Every stock face has both glyphs; '§' comes out as a stray 's' 120 times across the Federal Register pages. Both classes: neural dev-8 96.2/91.4 → 96.3/91.9, broad-30 93.4/86.8 → 93.4/86.5, legal-8 92.0/84.3 → 92.2/83.9, modern 91.6/86.0 → 91.1/85.5 (Federal Register 90.6 → 91.3 word); classic broad-30 81.4 → 81.1, modern 89.7/80.2 → 89.3/79.6. '§' alone: neural broad-30 86.5, legal-8 84.2, modern 85.5; classic legal-8 91.3/79.4 → 90.3/78.5, modern → 89.1/78.9. CONTROL: the four models rebuilt with the ORIGINAL charset by the same commands — pool, prototypes and MLP come back bit-identical to the live files (seeded k-means and Adam), the outline set does not (52,302 segments against the live 53,087, coverage-greedy condensation from the same fonts and script; cause not settled) and that alone moves classic legal-8 91.3 → 90.8 char with word flat, broad-30 −0.1 word, modern 89.7/80.3 (flat). So half the '§' loss on legal-8 is the rebuild and none of the modern loss is: the classes cost 0.5–1.3 word on modern in both profiles for +0.7 on the twelve pages that use '§' — a rare class is a confusion magnet for the common ones ('§' against 'S', '5', 'g'; '•' against every speck), as the ligature and '@' classes were. Not stocked; the charset stays. Two consequences recorded: a class-set change must be judged against a control rebuild, and the live outline set is a specific condensation the current script does not reproduce — RESEARCH keeps its provenance, and any future outline rebuild is re-measured. |
| 🔬 Ten letterhead faces added to the stock — NEGATIVE, not stocked (2026-09-11) | Stock widening as on 2026-09-03 (modern sans, positive) and 2026-09-06 (TeX Gyre italics, mixed) | `build_prototypes.py --add-fonts`, `build_outline_protos.py --add-fonts` (variant files only) | The deletion probe put the remaining broad-30 gap in letterhead lines set in faces outside the stock, so Futura, Optima, Gill Sans, Palatino, Baskerville, Charter, Rockwell, Lucida Grande, PT Sans and Bodoni 72 were added as variants (39 faces; Helvetica, Helvetica Neue and Avenir left out because the modern set is rendered in them). Under neural, against 96.2/91.4 · 93.4/86.8 · 92.0/84.3 · 91.6/86.0: dev-8 96.2/91.4, broad-30 **93.3/86.3**, legal-8 **92.1/83.8**, modern 91.5/85.9 — and identical to a tenth with the control outline set in place of the rebuilt one, so it is the faces, not the rebuild. Ten more faces competing for the fixed 90 prototypes a class dilute the body classes, the effect the display faces had before they were routed per block (2026-09-03). The letterhead lines want the same treatment: a "letterhead" family routed to blocks that vote for it, not more exemplars in the body pool — the next mechanism for that gap, not built here. Side finding: the build warns that BigCaslon, a pinned stock face, is missing from this machine's fonts; the bit-identical control rebuild shows the live models were built without it too. |
| 🔬 A routed letterhead font family — inert to negative, plumbing kept, not stocked (2026-09-11) | Per-block font-family routing (2026-09-03, the display family) applied to letterhead faces | `factory/fonts.py` `FAMILY_HINTS["letterhead"]`, `build_prototypes.py --letterhead`, `build_outline_protos.py --letterhead` | The answer to the dilution above: letterhead faces as their own routed family, matched only by blocks that vote for them. Of the seven named, only Futura and Gill Sans passed the print gate into the 80-face pool. Under neural, against 96.2/91.4 · 93.4/86.8 · 92.0/84.3 · 91.6/86.0: dev-8 96.1/91.4, broad-30 93.4/86.7, legal-8 **91.8/83.6**, modern **91.1/85.6**, identical to a tenth with the control outline set. A block that votes for a two-face family is then matched against two faces, and the misrouted blocks on typewriter and modern pages cost more than the letterhead lines gain. The gap the deletion probe found in letterhead lines has now resisted a better reader (the line read), more faces in the body pool, and a routed family: what those lines need is not in the fonts on this machine or in the decoder. Family and flags stay for a stock that has the faces. |
| 📊 Legacy-gap decomposition at the end of the production leg (2026-09-11) | As before | `compare_legacy_errors.py --config configs/neural.toml` | broad-30, 44,668 truth chars: ours 93.4 char, legacy 95.5; excess **926** (1,951 on 2026-09-08, 1,303 on 2026-09-09 morning). Shape substitutions 466 vs legacy 340 (excess 126, 14%, from 881); deleted characters 1,054 vs 467 (**+587, 63%**); deleted spaces 320 vs 66 (+254, 27%); case 149 vs 14 (+135, 15%); punctuation +82; digits +77. We make FEWER inserted characters (266 vs 484), spaces (99 vs 184) and punctuation (72 vs 123) than legacy. Reading: the classifier side of the gap is closed to within a third of a point; what remains is (a) characters and spaces that vanish in the letterhead lines the deletion probe located, which a better reader, more faces and a routed family have all failed to recover, and (b) case, untouched since the calibration week and now the third share. |
| 🔬 Case from the scorer's reading — inert, opt-in (2026-09-11) | Case decision from whole-word context (relative letter heights) rather than per-glyph size twins | `decode/beam.py` `seq_case`, `_seq_rescore`; probe in the session record | The case share of the legacy gap (149 vs 14) probed on broad-30: nearly all are SIZE TWINS ('s/S', 'c/C', 'o/O', 'u/U', 'v/V', 'w/W', 'p/P'); 38 sit in all-caps lines read entirely lower ('USAA INVESTMENT MANAGEMENT COMPANY'), 36 are capitalised words mid-sentence read lower ('Chief Operating', 'Washington', 'Congress'), 19 go the other way ('Of', 'With' capitalised). On three pages the scorer's greedy reading had the case right in 10 of 26 case-only errors, the decoder's case in 9, something else in 7. Rule: when the reading equals the chosen word up to case, take the reading's case. Four sets under neural: dev-8 96.2/91.3 (−0.1), broad-30 93.4/86.7 (−0.1), legal-8 92.1/84.4 (+0.1), modern 91.6/86.0 (flat) — the fixes are offset one for one by the scorer's own wrong case elsewhere. Kept at 0. The caps-line share (38) is a line-level question — a line whose every letter is a capital should not be judged glyph by glyph against the page x-height — and is the next mechanism for case, not this one. |
| ✅ Case of size twins decided line by line — ADOPTED as the decoder default; first measurement CORRECTED (2026-09-11, corrected 2026-09-12) | Line-local typographic measurement (x-height and cap height from the line's own unambiguous letters) instead of a page anchor; cf. the caps-line rule of 2026-09-07 | `decode/beam.py` `_line_case_pass`, `line_case`, `line_case_ratio` | The case probe: the 149 case errors on broad-30 are size twins (c o s u v w x z), 38 in all-caps lines read entirely lower and 36 in capitalised words mid-sentence; on letterhead-heavy pages the page x-height anchor itself lands at cap height (8562: 22.4 px), so every capital twin looks lowercase. The pass judges each twin by its LINE: the line's unambiguous lowercase letters (a e m n r z) give its x-height, its unambiguous capitals (A B D E F G H I J K L M N P Q R T Y) its cap height, and the twin's own ascent picks the nearer; only WHOLE glyphs vote or are judged (a piece cut from a touching pair carries the component's height: 'child' → 'Child' five times on one page before that rule). **Correction.** The measurement first recorded here (legal-8 92.0/84.3 → 97.1/92.3 under neural, 91.3/79.4 → 96.7/90.2 under classic) was FALSE: those runs hit a crash on words without per-glyph provenance (fixed afterwards) on 3 of legal-8's 8 pages, 6–7 of broad-30's 30 and 2–5 of modern's 59, and `eval_unlv.py` averaged over the surviving pages without saying so. The evaluator now counts a crashed page as fully wrong and names the count. Honest figures, on the fixed code, on against off, all pages present (char / word): neural dev-8 96.2/91.4 → 96.2/91.5, broad-30 93.4/86.8 → 93.4/86.8, legal-8 92.0/84.3 → 92.1/84.4, modern 91.6/86.0 → 91.7/86.2; classic dev-8 95.1/88.7 → 95.2/89.2, broad-30 91.7/81.4 → 91.8/81.9, legal-8 91.3/79.4 → 91.7/81.2, modern 89.7/80.2 → 89.9/81.3. Up or flat on every set under both profiles — a real gain of half a point to two word points under classic and a tenth or two under neural, and it stays the default; but not the record-setting gain first claimed. Lesson, now in the evaluator: a mean over N pages is only a measurement when N is the number of pages asked for. |
| 📊 Legacy-gap decomposition after the line-case pass (2026-09-11) | As before | `compare_legacy_errors.py --config configs/neural.toml` | broad-30: ours 93.4 char, legacy 95.5; excess **916** (926 before the pass; 1,951 on 2026-09-08). Case 149 → 141 (the pass's gain landed on legal-8, whose pleadings are set in capitals; broad-30's remaining case errors are capitalised words mid-sentence whose first letter is a twin, many of them words the scorer injected or re-read and so without per-glyph provenance for the pass to judge). Deleted characters +590 (64%) and deleted spaces +254 (28%) are 92% of what remains and sit in the letterhead lines that three mechanisms have failed to reach; shape +119 (13%), punctuation +81, digits +77. We make 215 fewer inserted characters than legacy. |
| ✅ Sequence scorer fine-tuned on 615 open faces (`seq_en_v4`) — ADOPTED on two seeds (2026-09-12) | Tesseract's LSTM recipe: synthetic lines over thousands of fonts (Smith 2016); the scorer has no prototype budget, so faces cannot dilute it | `make_seq_data.py --no-stock --font-dirs` (Google Fonts sparse checkout, 133 families, 681 files, 615 pass the shape gate), `factory/fonts.py` `find_fonts(dirs=)`, `words.extra_fonts`, `train_seq.py --init --seed`; live file `data/seq_en.npz` = seed 1, previous scorer kept as `data/seq_en_v1_live_backup.npz` | 250k windows from the 615 faces (41.7% with a touching pair, 4.5 min), fine-tuned from the previous scorer's weights with the original windows and both real harvests, six epochs, 84 min a seed; held-out real words 96.7% / 96.3% (different page splits per seed), synthetic 82.8% / 83.5% on a holdout twenty times broader in faces. Harness on dev-8: greedy read 97.5%, identical to the previous model. Four sets under neural against the previous scorer on the same code, all pages present (char / word): seed 0 — dev-8 96.2/91.5 → 96.2/91.5, broad-30 93.4/86.8 → **93.6/87.1**, legal-8 92.1/84.4 → 92.0/84.4, modern 91.7/86.2 → 91.7/86.3; seed 1 — dev-8 96.1/91.5, broad-30 **93.5/87.3** (precision 93.6 → 94.0), legal-8 92.2/84.3, modern 91.7/86.3. Both seeds win the headline set by more than its seed range and lose nothing elsewhere: adopted through the two-seed door of the model rule. What the faces did not change: the letterhead lines, which are on the classic segmentation's side of the fence, not the scorer's. |
| 📊 Legacy Tesseract on dev-8 and legal-8 (2026-09-12) | Same harness (`eval_tesseract.py --oem 0`, `TESSDATA_PREFIX` to a legacy-capable `eng.traineddata`) | `scripts/eval_tesseract.py` | dev-8 (bus.3B seed 1): legacy 95.0 / 91.7 (recall 95.4, precision 95.2); neural 96.1 / 91.5 (95.3 / 95.2) — parity on the tuning set, a char point ahead. legal-8: legacy 90.4 / 88.7 (recall 98.3, precision 91.7); neural 92.2 / 84.3 (91.2 / 91.7) — two char points AHEAD of legacy but 4.4 word points behind, and the recall column says why: legacy reads 98.3% of the truth words somewhere on the page, we read 91.2%. Our typewriter errors are spread one to a word (a dropped or fused character costs the word) while legacy's are concentrated in junk it emits beside the text (its precision equals ours). A first attempt at these runs returned 0.0% on every page: the machine's temp cleaner had emptied the tessdata directory at midnight and Tesseract read nothing; re-fetched and re-run, both MEAN lines over 8 pages. |
| 📊 New domains: UNLV magazine (mag.3B) and newspaper (news.3B) sets (2026-09-12) | ISRI annual tests read these sets alongside the business letters; `data/unlv` holds bus.3A, mag.3B, news.3B and news.4B (200 pages each) beside bus.3B and legal.3B | `eval_unlv.py data/unlv/mag.3B --pages 8 --seed 1 --doc-type magazine` (and news.3B / newspaper); `--zone-order` for recognition without reading order | Eight pages each, seed 1 (mag-8, news-8), after the deskew fix and the layout prior (layout row): neural news-8 92.8 / 83.1 (recall 86.7, precision 90.9; zone-ordered 92.9 / 82.7), mag-8 68.3 / 50.7 (71.2 / 80.6; zoned 74.9 / 53.4); classic news-8 92.1 / 79.0, mag-8 65.3 / 40.6; legacy Tesseract news-8 96.4 / 93.6 (recall 97.6, precision 94.8), mag-8 87.4 / 85.0 (96.0 / 90.7). On the day's first run neural read 62.3 / 52.1 and 59.2 / 44.2. What remains, by page: newspapers lose recall on dense small type (a 511-word page at recall 93 but precision 81 emits junk beside its text; a 253-word page reads 80 of its words) — a recognition gap the scorer's small-type training should reach; magazines lose recall to image zones that swallow text set over tints and to three-column pages whose columns still fuse (a 1,543-word page at recall 48), and a third of their remaining gap is reading order. Neither set is harvested yet; when the mag/news line harvests run, the seed-1 and seed-2 draws are excluded by the standard guard. |
| 🔬 Gated mechanisms re-measured under the many-font scorer — ungated injection NEGATIVE again, scorer case inert (2026-09-12) | The gates were set under the stock-face scorer; a scorer that reads foreign faces might have changed what an unendorsed string is worth | `decode/beam.py` `seq_inject_endorsed=false`, `seq_case=true`, live `seq_en_v4s1` | Four sets, all pages present, against the adopted row dev-8 96.1/91.5, broad-30 93.5/87.3, legal-8 92.2/84.3, modern 91.7/86.3. Injection ungated (3-nat margin): dev-8 96.2/91.1, broad-30 92.9/86.8 (precision 94.0 → 93.3), legal-8 90.8/83.7, modern 91.2/85.6 — negative on every set, worse than under the old scorer (which at least won legal-8): the many-font model likes more strings the image does not say, so the lexicon gate stays. Case from the scorer's reading: dev-8 96.1/91.5, broad-30 93.5/87.3, legal-8 92.3/84.5, modern 91.7/86.4 — a tenth or two inside the seed bar everywhere; kept opt-in. The line read (`seq_line_read`) is not re-measured here: it wants a model trained on long windows, which `seq_en_v5` (in training on 150k windows of two to six words, up to 1,400 columns) will be. |
| ✅ Service concurrency test (2026-09-12) | Process pool behind a threading HTTP server: throughput scales with workers while a page stays the single-threaded pipeline (DESIGN §9) | `scripts/service_load.py`; `mlws_ocr/service.py` | Four workers, the eight dev-8 letters posted at once while a training job and two harvests ran on the same machine: 8 of 8 returned 200; one page alone 18.0 s, the burst 39.4 s wall (mean latency 24.8 s) — 3.7 pages a minute over 4 workers against 3.3 for one; mean character accuracy 0.961 against the truth, equal to the dev-8 neural row, so a worker reads exactly what the harness reads; two requests for one page read identically. The first request after start pays the model load in the worker (the process pool's initializer). |
| ✅ Scoring convention: line-end hyphenation folded to the joined word on BOTH sides (2026-09-12) | ISRI's truth is 'as printed' (Rice, Jenkins & Nartker 1995–96), so a wrapped word is two tokens, 'de-' and 'bates'; a reader's OCR joins them, and the decoder's dehyphenation pass does when its lexicon endorses the word; scoring one convention against the other is not scoring recognition | `scripts/eval_unlv.py` `join_line_hyphens`, `normalize(hyphens=)`, `--dump DIR` (per-page output text for offline re-scoring); `tests/test_eval_normalize.py` | Share of truth words that are wrapped halves: news-8 6.7%, mag-8 4.7%, broad-30 0.2%, dev-8, legal-8 and the modern set none by the naive count — the modern bills DO wrap, with the line number on its own line between the halves ('com-' / '7' / 'pany'), which the fold steps over. Each join the decoder made against an as-printed truth cost two word errors. Three iterations, each caught by re-running legacy Tesseract (which never joins, so the fold must leave it nearly unchanged): (1) the join must skip blank lines; (2) Tesseract writes a wrapped word's hyphen as U+2014, so dashes fold to '-' before the join looks for one (a 99.9/99.3 newspaper page had dropped to 99.6/97.8); (3) the line-number rule above (bill pages had dropped four word points for legacy); (4) the number written at the head of the continuation line instead ('Representa-' / '2 tives'), which is how pdftotext lays out the numbered bills while our output puts the number on its own line — without the rule the pure row lost 0.9 word on modern, all of it on bills. Legacy under the final fold against raw: broad-30 95.5/91.7 → 95.5/91.7, dev-8 95.0/91.7 → same, legal-8 90.4/88.7 → same, modern 75.3/70.9 → 75.2/70.4, news-8 96.4/93.6 → 96.3/93.1, mag-8 87.4/85.0 → 87.3/84.7 — the residual is a wrapped half at a column foot joined with a different partner in legacy's block order, and an em-dash before a lowercase continuation. Neural under the fold: news-8 92.8/83.1 → **93.5/87.3**, mag-8 68.3/50.7 → 68.4/51.1, dev-8 and legal-8 unchanged (96.1/91.5, 92.2/84.3); broad-30, modern and the classic and pure rows re-measured under the fold in the scoreboard (DESIGN §8). Every scoreboard row from here on is under this convention; the rows before 2026-09-12 are comparable on dev-8, legal-8 and modern and within 0.3 word on broad-30. |
| ✅ Beam: no language-model replay at zero weight (2026-09-12) | Engineering (profile-driven) | `decode/beam.py` `_beam_word_mode` | cProfile of one neural decode pass on a 256-word letter: 5.6 s, of which 2.7 s was the char-GRU `step` — 23k stateless `score` replays of up to 40 characters each, all from the DIGIT-mode beam, which runs with `lm_w = 0` and multiplied the result by zero. Skipped at zero weight; dev-8 output identical page for page (the `--dump` texts diff clean), so no measurement moves. The sequence scorer itself is 0.5 s of that pass; the remaining cost is the beam in Python, and a dense Federal Register page (11,652 glyphs, 211 lines) still spends 195 s a decode pass under neural — the throughput item for the service. |
| 🔬 Sequence scorer from scratch on all 3,179 gated Google Fonts faces with long windows (`seq_en_v5s1`) — NOT adopted (2026-09-12) | Tesseract's LSTM recipe taken further: the whole open-font corpus, and windows of two to six words so the model has seen a line | `make_seq_data.py --no-stock --font-dirs` on the full checkout (2,005 families, 3.9 GB, 3,179 faces pass the gate; one face crashed the renderer on a negative glyph origin, fixed in `words.render_line`); `data/seq_synth_gfonts_all.npz` 400k windows, `seq_synth_gfonts_long.npz` 150k windows of 2–6 words up to 1,400 columns; `train_seq.py` from scratch, 12 epochs, 921k windows an epoch (v1 synth + both new sets + the two real harvests at 2×), 373 min on MPS | Held-out real words 95.7% (best epoch 11; synthetic holdout 78.5%, touching 65.4%, still rising at the end). Harness on dev-8: greedy 97.5%, identical to the live scorer; split/merge readings 90.3 → 91.7%. A legal-8 harness dump was added the same evening (`data/nbest_legal8.npz`, 1,263 aligned typewriter words; `seq_rerank_eval.py dump ... --doc-type legal`) and it names the loss before any pipeline run: greedy 98.2% live → 97.1%, and on the multi-variant words 93.8% → 86.6% — a two-second typewriter gate for every scorer from here on. Four sets against the live scorer (fold convention): dev-8 96.1/91.5 → 96.2/91.8, broad-30 93.5/87.4 → **93.9/87.8** (recall 91.5 → 92.5, precision 94.1 → 93.5), legal-8 92.2/84.3 → **91.6/83.0**, modern 91.5/85.7 → 91.2/85.4. It wins the headline set by about its seed range and loses the typewriter set by more than its bar: the real strips are a smaller share of a 921k-window epoch than of the live recipe's, and the typewriter faces live only in the real strips. The second seed was cancelled (a model that loses a set cannot be carried by two seeds) and the machine went to two fine-tunes with the three new real harvests (`seq_en_v6a` from the live scorer, `v6b` from this one; RESEARCH row to follow). Line read (`seq_line_read`, the mechanism the long windows were rendered for), with this model: dev-8 96.3/92.2 (+0.1/+0.4 over the same model without it), legal-8 91.3/83.2 (−0.3 char, +0.2 word), broad-30 93.9/87.9 (+0.1 word, precision 93.5 → 93.7) — a tenth or two everywhere, inside the seed range, so the line read stays opt-in even with a model that has seen lines: the letterhead lines it was built for are not endorsed by the lexicon, and the strict 'more endorsed' rule is what keeps it from swapping proper nouns. |
| 🔬 Live scorer fine-tuned with the three new real harvests (`seq_en_v6a`): flat on the standard sets, +1.1 word on newspapers, +3.7 on magazines (2026-09-12) | More real strips from more kinds of page (the harvest is richest where the pipeline fails) | `harvest_lines.py` on bus.3A (170 pages → 33,268 words, 146 pages matched), news.3B (60 → 16,523 words, 8.6% decoded wrong) and mag.3B (60 → 20,612 words, 9.9% decoded wrong), all under the standard contamination guard; `train_seq.py --init data/seq_en.npz` with the v1 synth, the 400k all-faces set, the 150k long-window set and all five harvests at 2×, six epochs, 1.05M windows an epoch, 196 min | Held-out real words 95.9% (a different, larger holdout than the live model's). Harness: dev-8 greedy 97.6% (live 97.5), split/merge readings 94.4% (live 90.3); legal-8 greedy 97.2% (live 98.2), multi-variant 90.7% (live 93.8). Six sets against the live scorer (fold convention): dev-8 96.1/91.5 → 95.9/91.4, broad-30 93.5/87.4 → 93.5/87.2, legal-8 92.2/84.3 → 91.7/83.8, modern 91.5/85.7 → 91.5/86.0, news-8 93.5/87.3 → **93.1/88.4**, mag-8 68.4/51.1 → **68.2/54.8** (recall 72.1 → 77.0). Every standard-set move is inside its seed range (0.4 / 0.5 / 1.0 / 0.7 word) and the two new domains move by more than any range, so the new strips buy the new domains without costing the old ones measurably — but they do not lift the old ones either: the scorer is at its ceiling there (harness greedy 97.5%, rerank oracle-bound at 95.4%), and the remaining letter and typewriter errors are the segmentation's, not the scorer's. Not adopted on one seed; a second seed of the better of the two fine-tune recipes (this one, or `v6c` = the live recipe plus the harvests at real weight 3, under training) decides through the two-seed door. `v6b` (the from-scratch v5s1 plus the harvests) was cancelled: its base had already lost legal-8. |
| 🔬 Live scorer fine-tuned on the live recipe plus the harvests at real weight 3 (`seq_en_v6c`) — mixed (2026-09-13) | As `v6a`, with the real strips kept a large share of the epoch (the from-scratch model's typewriter loss was a dilution effect) | `train_seq.py --init data/seq_en.npz --synth seq_synth_v1 seq_synth_gfonts_v1 --lines <all five> --real-weight 3`, six epochs, 888k windows an epoch, 130 min | Held-out real words 96.6%. Harness: dev-8 greedy **98.1%** (live 97.5), multi-variant 98.1% (live 96.8); legal-8 greedy 98.3% (live 98.2), multi-variant 92.8% (live 93.8) — the best offline numbers of any scorer so far, on both harnesses. Pipeline against the live scorer (fold convention): dev-8 96.1/91.5 → **96.5/92.1**, broad-30 93.5/87.4 → 93.5/87.5, legal-8 92.2/84.3 → **91.0/83.7** (precision 91.7 → 91.3), modern 91.5/85.7 → 91.5/85.9, news-8 93.5/87.3 → 92.9/**88.6**, mag-8 68.4/51.1 → 66.4/51.9. Word accuracy up on five of six sets and dev-8 past its seed bar, but character accuracy DOWN on legal-8 (1.2), news-8 (0.6) and mag-8 (2.0) while their word accuracy rises: the sharper scorer's injected and re-read strings win more whole words and lose more characters in the words they get wrong — a precision effect the greedy harness, which scores aligned words only, does not see. Not adopted on one seed (a character loss on the typewriter set is a loss); the overnight second seeds of both fine-tune recipes decide, and the decomposition of the legal-8 character loss (`compare_legacy_errors.py`) is the first task after. |
| 🔬 Injection length guard — NEGATIVE, reverted (2026-09-13) | A reading must be a reading of the window it came from | `_seq_rescore` (a `seq_inject_min_frac` of 0.6: the injected string keeps 60–167% of the decoder's letters), removed again | Prompted by a word-level diff on the worst legal page under `v6c`: a fused forty-letter segment ('schedulesandstatementsmaybegrantedonly') had become the single injected word 'schedules', because the decoder's own text has no finite likelihood on such a window and the margin test is skipped when it is NaN. Six sets with the live scorer: dev-8 96.1/91.5 → 96.3/91.7, broad-30 93.5/87.4 → 93.4/87.2, legal-8 92.2/84.3 → 92.2/84.0, modern 91.5/85.7 → 91.4/85.8, news-8 93.5/87.3 → 93.6/86.7, mag-8 68.4/51.1 → 69.3/49.8; with `v6c` legal-8 91.0/83.7 → 90.8/83.4, modern 91.5/85.9 → 91.5/85.9 — the guard blocks more good injections (a reading that drops junk marks legitimately) than bad ones, and does not touch the `v6c` character loss, which is therefore not the truncated injection. Reverted. |
| 📊 Harvest fine-tune, second seed (`seq_en_v6a_s2`) — two seeds agree: a small systematic loss on the UNLV standard sets, a clear gain on newspapers and magazines; NOT adopted, kept as the press variant (2026-09-13) | Two-seed door of the model rule | `train_seq.py --init data/seq_en.npz ... --seed 2` (same recipe as `v6a`, 197 min); `data/seq_en_v6a.npz`, `data/seq_en_v6a_s2.npz` kept as variants | Held-out real 95.5%; harness dev-8 greedy 97.7%, legal-8 97.0%. Six sets against the live scorer (fold convention), seed 1 / seed 2: dev-8 91.5 → 91.4 / 91.3 word, broad-30 87.4 → 87.2 / 87.2, legal-8 84.3 → 83.8 / 83.8, modern 85.7 → 86.0 / 85.8, news-8 87.3 → **88.4 / 88.2**, mag-8 51.1 → **54.8 / 54.2**. Each standard-set move is inside its seed range, but both seeds move the three UNLV sets the SAME way by 0.1–0.5 word, which is a small systematic cost, not noise; the two new domains gain about one and three word points on both seeds. The rule says a model that costs the standard sets is not adopted, and it is not: the live scorer stays. What the pair of seeds establishes is the trade — real strips from newspapers and magazines teach the scorer those pages at a small price on letters and typewriter pages — and the reason the standard sets do not move up: the scorer's rerank is oracle-bound there (95.4% on dev-8, 96.1% on legal-8; its greedy read is already above the oracle), so the next point on those sets comes from the segmentation that produces the variants, not from a better judge of them. `v6c` seed 2 (the real-weight-3 recipe) is under training for the record. |
| 🔬 Scorer-posterior candidates for the beam — negative, opt-in at 0 (2026-09-13) | The oracle-miss census: the truth is outside every variant mostly because no candidate list held the right letter; a second recognizer's per-position posterior is the classic source of extra candidates (ROVER-style lattice combination, Fiscus 1997) | `decode/beam.py` `_seq_glyph_candidates`, `seq_cand_thresh` (0 = off), `seq_cand_penalty` | For each glyph, the scorer's characters with at least the threshold probability on the glyph's own frames of the word window join its candidate list just under the best prototype candidate; the beam and the CTC rescoring judge them. Live scorer, threshold 0.3 / 0.5 against off: dev-8 96.3/91.9 → 96.3/91.4 / 96.3/91.4, broad-30 93.7/87.5 → 93.7/87.5 / 93.8/87.5, legal-8 92.0/84.4 → 91.9/84.1 / 91.8/84.0. Half a word point lost on the tuning set, the rest flat: the extra letters reach the truth in some words and let the language model and the CTC term prefer a plausible wrong word in others, and the second effect is the larger. The census's same-length misses are real but few (54 of 1,466 words), and this is the second mechanism (after the lower injection margin) that fails to convert them without collateral; the candidate list is not the cheap lever it looked. Kept at 0. |
| 📊 BUSINESS set: invoices, payslips, receipts, statements, purchase orders (2026-09-13) | Templated documents with truth by construction, as the modern set's business half; receipts on a monospace thermal roll (Courier New, Menlo); statements and purchase orders ruled | `scripts/make_business_set.py` → `data/business/sev0..2` (60 pages, 12 per kind); `eval_unlv.py --by-kind`, `eval_tesseract.py --by-kind` | Char / word, recall / precision, sev0, under the fold (character rule lines such as '------' dropped from both sides as layout, not words): **neural 91.7 / 84.9 (93.9 / 94.0)**, classic 90.3 / 79.7 (88.2 / 88.8), legacy Tesseract 70.7 / 68.0 (**97.5 / 97.8**), Tesseract LSTM about the same. The edit-distance columns and the bag-of-words columns disagree by thirty points for Tesseract because it reads a table by COLUMN blocks while the truth (and we) read it by row, so on tabular pages recall and precision are the recognition comparison and the edit distance is the reading-order comparison: we read rows in order and lose 6.1 of the words; Tesseract finds 97.5 of the words in another order. By kind, our recall against legacy's: invoices 96.2 / 94.9, payslips 96.8 / 98.4, purchase orders 93.0 / 97.8, receipts 88.3 / 97.0, statements 91.9 / 99.4. The missing words, from the per-page dumps: capital-letter cells fused ('ATM WITHDRAWAL' → 'ATMWITHDRAWAL', 'UBER TRIP'; the line's x-height estimate is its cap height, so the word gaps of a caps cell sit in the letter-gap range) or split ('WHITEBOARD' → 'white board'); a leading '1' cut from a date or amount by its kerning gap ('1' + '1/28/2025', '$1' + '14.73'); '(7%)' read '(796)' in bold on every purchase order; and on receipts the '@' of '2 @ 2.72' read 'e' 35 times — there is no '@' in the class set (`factory/stock.py` CHARSET), the one hole a receipt exposes at once. |
| 📊 BLOCK metric: a text block on its own (2026-09-13) | Recognition with no layout question: the ISRI zone convention (`.uzn`) gives the blocks, a caller-style crop gives the input | `scripts/eval_blocks.py` (`truth` derives and caches each Text zone's truth by aligning our zone-ordered reading to the page truth, `data/blocks/*.json`; `score` reads each crop with a profile, or with Tesseract `--psm 6`) | Pooled char / word over the Text zones (per-block means in brackets): dev-8, 9 blocks, 1,312 words — neural **97.4 / 94.7** (96.6 / 93.2), classic 97.2 / 93.3, legacy **98.9 / 97.2** (96.9 / 93.5); legal-8, 13 blocks, 1,416 words — neural 93.1 / 88.4 (87.1 / 83.1), classic 92.7 / 86.7, legacy 96.7 / 95.3 (89.8 / 87.1); broad-30, 43 blocks, 6,435 words — neural 94.6 / 89.9 (94.3 / 89.9), classic 93.4 / 84.3, legacy 98.2 / 96.6 (97.0 / 94.7). This is the recognition gap with the layout taken out: 3.6 character points on the letters and on the typewriter pages, 1.5 on dev-8 — wider than the page-level gap on broad-30 (1.8), because Tesseract reads a paragraph handed to it alone better than it reads the page (98.2 against 95.5) and we read it about as well as the page (94.6 against 93.7). Found by the metric on its first run: a block on its own lost 98% of its ink to the image-zone stage (row below). |
| ✅ Image-zone size fractions referenced to a letter-size page (2026-09-13) | A fraction 'of the page' must mean a page | `layout/imagezones.py` (`page_area = max(image area, 8.5 x 11 in at the input's dpi)`); `tests/test_imagezones.py` fixture moved onto a page-size canvas | On a 273 x 1,056 px crop of a court caption the stage found 28 image zones holding 98.6% of the ink and the block read 'I' (legacy on the same crop: every word). With the reference never smaller than a page, the legal-8 blocks went from 67.4 / 56.8 to 87.1 / 83.1 per block and the full-page sets are unchanged to the decimal (dev-8, legal-8, broad-30, modern, business re-run). |
| ✅ `doc_type = "block"`: the caller says the input is one block (2026-09-13) | The block metric's own finding; a layout prior in the pattern of the letter/legal and newspaper/magazine ones | `layout/blocks.py` `XYCutBlocks` (block: a column cut needs a region at least 40% of the block tall; row cuts unchanged); `eval_blocks.py score --doc-type block` (default); the service and CLI take it as any other `doc_type` | On the cropped paragraphs a whitespace river through two or three lines let a column cut split them left from right and the lines came out of order (a 104-word paragraph read 59.6 word). Three variants on the broad-30 blocks (pooled char / word; no hint 94.6 / 89.9): every cut forbidden — 94.9 / 89.3, the paragraphs fixed (+30, +16, +15 word on three blocks) but an order form's table columns fused into one line per row (94.1 → 37.1 word on that block); row cuts forbidden, full-height gutters kept — 94.9 / 89.2, the table still fused (its gutters stop at the prose above and below); **row cuts kept, column cuts only through a region at least 40% of the block tall — 95.4 / 91.1**, the paragraphs and the table both right. dev-8 blocks 97.4 / 94.7 → **98.8 / 96.0**, legal-8 blocks 93.1 / 88.4 → **94.1 / 89.4** (the no-cuts variant read legal-8 best, 95.7 / 91.5: a pleading's line-number column beside a short block wants no cut at all, but it lost the headline). Against legacy on the same crops: dev-8 98.9 / 97.2, legal-8 96.7 / 95.3, broad-30 98.2 / 96.6 — the recognition gap is now 0.1 char on dev-8, 2.6 on legal-8, 2.8 on broad-30. A re-read guard (a reading must keep half the segment's characters) was tried beside it and measured identical on every set; removed. |
| ✅ The LINE READER — a second decoder, `impl = "hybrid"`, ADOPTED into the neural profile (2026-09-13) | Tesseract 4's line recognizer (Smith 2016): read the line end to end, no segmentation; CTC prefix beam search (Graves 2012 §7.3) with a word-level lexicon prior (Hannun et al. 2014); kept beside the classic reading and decided per line | `decode/lineread.py` `HybridDecode(BeamDecode)`, `recognize/ctc.py prefix_beam_search`, `harvest_lines.py --line-out` (real whole lines), `configs/neural_line.toml` (the experiment's profile), `configs/neural.toml` `impl = "hybrid"` (the adoption) | Motivation measured first: the scorer's greedy read (98.2 / 98.3 on the harnesses) above the oracle of the classic variants (95.4 / 96.1), and the block gap concentrated in tightly set lines the segmenter fuses. Reader alone (`line_mode = pure`) with the long-window model `seq_en_v5s1` read in 1,400-column chunks: broad-30 blocks 70.6 / 50.0 pooled — weak overall, but 93.3 char on the tight block 8531 where the classic decoder reads 75.6. Three choice rules, v5s1 as the reader, against the neural profile (dev-8 96.3/91.9, broad-30 93.7/87.5, legal-8 92.0/84.4, modern 91.6/86.1, business 91.7/84.9; blocks broad-30 95.4/91.1, legal-8 94.1/89.4): (1) more endorsed words, or as many at a higher mean emission probability — dev-8 96.3/92.5, broad-30 93.5/87.9, legal-8 91.5/85.0, modern 91.3/85.5, **business 89.0/80.2** (the reader took table rows it cannot read), blocks broad-30 **97.0/93.1**; (2) likelihood of both texts under the reader's own posterior, 0.3 nats a character margin — dev-8 96.0/92.3, legal-8 91.4/85.1, business 88.6/80.6, blocks 96.6/92.1 — worse than (1) on every set: the reader prefers its own reading by construction, so its likelihood is not a judge; (3) **repair only** — a line with an unendorsed classic word that is not a data line, replaced when the reading endorses more words and keeps 80% of the characters — dev-8 **96.6/92.9**, broad-30 **93.9/88.2** (recall 92.1, precision 94.4), legal-8 92.0/84.4, modern 91.5/86.2, business 91.5/84.9 (recall 94.1), news-8 93.1/**88.8**, mag-8 67.7/**55.8**; blocks dev-8 98.8/96.0, legal-8 **94.8/90.4**, broad-30 **95.9/92.5**. Rule (3) wins the headline set past its bar, the tuning set by a word point, the newspapers and magazines, and loses nothing: adopted as the neural profile's decoder, with `seq_en_v5s1` as the line model and the word scorer unchanged as the judge of the classic variants. Classic guard: dev-8 95.2/89.2 exact (the classic profile never loads the stage). Cost: one forward per line in numpy, about +4 s on a letter. What the reader still lacks is a model trained on LINES: 15,549 real whole lines are harvested (`data/linesfull_*.npz`, five sets) and `seq_line_v7a` is training on them with the long synthetic windows; its verdict decides `line_model_path`. |
| 🔬 A fitted judge for the line choice (`decode/linechoice.py`) — mixed with the present reader; the repair rule stays (2026-09-13) | The word-confidence recipe at line level: label which reading is right on non-evaluation pages, fit a logistic regression over both readings' evidence | `harvest_line_choice.py` (hybrid decoder with `line_keep_alt`, lines matched to truth, label = the reading has fewer character errors), `train_line_choice.py`, `line_choose_rule = "calibrated"` / `"union"`; harvests `data/linechoice_{en,legal,bus3a,news,modern}.npz`, judges `data/linechoice_unlv.npz` (four UNLV harvests, 6,459 pairs) and `data/linechoice_all.npz` (with the Federal Register pages, 12,451 pairs) | With `seq_en_v5s1` as the reader the reading beats the classic text on only 2.9% of the lines where they differ (3.4% with the Register pages), so the judge learns mostly to refuse: at P ≥ 0.5 it takes 1.3–2.8% of lines with 81–86% of the taken lines better (the fixed 'endorses more' count takes 15–21% at 79–84% decision accuracy). Its heaviest weights are the length ratio of the two texts (+15: a reading that keeps the characters), the reader's likelihood of its own text (−8: an unsure reading) and the likelihood gap (+4). Against the adopted repair rule (dev-8 96.6/92.9, broad-30 93.9/88.2, legal-8 92.0/84.4, business 91.5/84.9, blocks broad-30 95.9/92.5): the UNLV-fitted judge — dev-8 96.2/92.0, broad-30 **94.0/88.5**, legal-8 92.1/**85.1**, business 91.4/**86.6** (recall 95.6), blocks **97.0/93.2**; the judge with the Register pairs — dev-8 96.4/93.0, broad-30 93.7/88.3, legal-8 91.7/85.2, business **90.3/83.2**, blocks 96.9/93.3; the union of repair and the UNLV judge — dev-8 96.5/93.0, broad-30 94.0/88.3, legal-8 92.0/84.6, business 91.6/85.0, blocks 96.9/93.2. The judge wins the headline set, the typewriter set and the blocks by a few tenths to a point, and its business result swings by three word points with the training mix — a judge fitted on 190–430 positive examples is not stable enough to adopt over a rule that loses nothing. The machinery is in place for the line-trained reader (`seq_line_v7a`), whose readings should be right far more often than 3% and give the judge something to learn; the harvest must be re-run whenever the reader model changes, since every feature is the reader's. |
| ✅ The LINE MODEL (`seq_line_v7a` = `data/seq_line_en.npz`) and the refitted judge — ADOPTED (2026-09-13) | Tesseract 4's recipe made ours: a CRNN trained on whole lines, real and synthetic, read end to end; the judge is the word-confidence recipe at line level | `train_seq.py --init data/seq_en_v5s1.npz --synth seq_synth_v1 seq_synth_gfonts_v1 seq_synth_gfonts_long seq_synth_long2 --lines <five word harvests> <five whole-line harvests> --real-weight 3 --epochs 8 --batch 32` (1.22M windows an epoch, 531 min); `harvest_line_choice.py` re-run against this reader (2,456 differing lines on 247 pages, the reading better on 68% of them; with the old reader 3%), `train_line_choice.py` → `data/linechoice.npz` (P ≥ 0.5 takes 83% of the differing lines at 81% precision on a page-disjoint holdout) | Held-out real words 94.8% (the holdout now includes whole lines). The reader ALONE on the blocks (`line_mode = pure`): dev-8 **99.0 / 97.3** (legacy 98.9 / 97.2), legal-8 **96.2 / 95.9** (legacy 96.7 / 95.3), broad-30 97.8 / 94.1 (legacy 98.2 / 96.6) — from 70.6 / 50.0 with the long-window word model that morning, and above the classic-based decoder everywhere. Pages, this reader with the repair rule / with the refitted judge, against the morning's neural row: dev-8 96.6/92.9 → 96.7/93.2 / **96.6/93.2**; broad-30 93.9/88.2 → 94.0/89.2 / **94.3/89.8** (recall 94.0); legal-8 92.0/84.4 → 93.4/87.7 / **93.6/88.5** (legacy 90.4/88.7); modern 91.5/86.2 → 91.8/86.7 / **91.9/86.7**; business 91.5/84.9 → 91.9/86.2 / 91.6/85.9 (recall 95.2 / 95.5; receipts 76.1 → 94.8 char); news-8 93.1/88.8 → 92.8/89.1 / **93.9/90.2**; mag-8 67.7/55.8 → 68.4/57.8 / **76.1/66.6** (recall 90.7); blocks broad-30 95.9/92.5 → 96.3/93.5 / **97.9/95.1**, legal-8 94.8/90.4 → 95.8/93.6 / 95.8/93.9, dev-8 98.8/96.0 → — / 99.0/96.3. The judge beats the repair rule on every set but business (−0.3 word, recall up) and is adopted with the model: `configs/neural.toml` `line_model_path = data/seq_line_en.npz`, `line_choose_rule = calibrated`, `line_choice_path = data/linechoice.npz`. Classic guard: dev-8 95.2/89.2 exact. Against legacy Tesseract now: ahead on dev-8, legal-8 (both metrics), modern and business; behind on broad-30 by 1.2 char / 1.9 word (was 1.8 / 4.2 at the start of the day), on news-8 by 2.4 / 2.9, on mag-8 by 11 / 18. Judge and reader are one pair: the judge's features are the reader's, so a new line model means a new harvest and fit. |
| 🔬 Line model continued six epochs at a lower rate, second seed (`seq_line_v7b`) — flat, not adopted (2026-09-14) | The held-out curve was still rising at epoch 8 | `train_seq.py --init data/seq_line_v7a.npz --lr 1e-3 --epochs 6 --seed 2` (398 min); judge re-harvested and refitted against it (`data/linechoice7b_unlv.npz`, 2,489 pairs, 65.7% reading-better) | Held-out real words 94.8 → 95.3%. Reader alone on the blocks: dev-8 99.0/97.3 → 99.1/97.0, legal-8 96.2/95.9 → 96.0/95.4, broad-30 97.8/94.1 → 97.7/93.8. Pages with the judge, against the adopted v7a row: dev-8 96.6/93.2 → 96.6/93.3, broad-30 94.3/89.8 → 94.3/89.8, legal-8 93.6/88.5 → 93.6/88.4, modern 91.9/86.7 → 91.7/86.5, business 91.6/85.9 → 91.4/85.4, news-8 93.9/90.2 → 93.7/90.0, mag-8 76.1/66.6 → 75.8/65.5; blocks broad-30 97.9/95.1 → 97.9/95.1, legal-8 95.8/93.9 → 95.7/93.6. The held-out gain did not reach the pipeline: every set inside its range, most a tenth or two down. The line model's data route is saturated at this size; the remaining headline gap to legacy (1.2 char / 1.9 word on broad-30) is not in the reader's accuracy on the lines it reads but in the lines the judge does not give it and in the layout. `v7a` stays live. |
| 📊 Where the residual sits: page errors by ISRI zone type (2026-09-14) | The zone files name what each region of a letter is; pooling errors by type says where the gap to legacy lives | `eval_blocks.py zones` (full-page read, truth split among the zones by alignment, output words assigned to zones, errors pooled by type; also the output/truth length ratio and how many of the zone's lines the classic decoder flagged as graphics) | broad-30, neural: 2,840 character errors — **Letterhead 47%** (48 zones, 3,518 truth characters read at 61.8% char / 22.3% word; we emit 1.14 characters per truth character, and 118 of the zones' 246 lines are flagged `graphic_suspect`), Text 28% (37,976 characters at 97.9 / 93.9), Signer 8% (73.5%, 1.16 output ratio: signature ink read as text), Salutation 4.5%, Closing 4% (1.40 ratio), Dateline 3.5%, Inside address 2%. Classic for comparison: 4,789 errors, Text 39% (95.1 / 86.2) and Letterhead 38% (50.8 / 8.2). So the line reader closed most of the body-text gap (Text errors 1,867 → 786) and left the letterheads, which it never reads: half their lines carry the graphic flag and the reader skips flagged lines. The judge's threshold, swept at 0.3 / 0.5 / 0.7 on dev-8, broad-30, legal-8 and business, gives identical output at all three: the fitted probabilities are saturated, so the judge is a classifier, not a dial. |
| ✅ The reader reads the graphic-flagged lines too (`line_read_graphic`) — ADOPTED (2026-09-14) | The zone census above: half the letterhead lines carry the classic decoder's graphic flag, set from prototype distances that say nothing about the reader | `decode/lineread.py` (`line_read_graphic`, `line_graphic_conf` 0.6): a flagged line's reading is kept, and the flag cleared, when its mean emission probability is 0.6 or more and the lexicon or a numeric format vouches for a word of it | Against the adopted row: dev-8 96.6/93.2 → **97.0**/93.3, broad-30 94.3/89.8 → **94.8**/89.7 (recall 94.0 → 94.5), legal-8 93.6/88.5 → **94.8/89.1** (past legacy's 88.7 word), modern 91.9/86.7 → 92.0/86.8, business 91.6/85.9 → 91.9/86.2. Zone census after: letterhead errors 1,344 → 1,268 and flagged letterhead lines 118 → 69; the residual 2,840 → 2,584. What the letterhead zones still hold, read off the worst ones: logo art decoded as text and never suppressed (a 168-character run of punctuation where the truth is empty; 1.12 output characters per truth character), letter-spaced display capitals read as single letters ('FORTY F O U R H U N D R E D'), and case on capital lines ('mESA RIM ROAD san diego'). |
| 📊 Zone census corrected: the output stage's suppressed lines excluded (2026-09-14) | The census had counted words on graphic-flagged lines the output never prints | `eval_blocks.py zones` | broad-30, neural, graphic-flagged lines dropped as the output drops them: 1,948 character errors (not 2,584) — Letterhead 48% (43 zones, 73.7% char / 51.7% word, **0.94** output characters per truth character: deletions now, not junk; 63 of 240 lines still flagged), Text 33% (98.3 / 95.0), Signer 6% (86.4), Salutation 5%, Closing 2%. The 'logo art decoded as text' of the first census was largely lines the output already suppresses; what the letterheads lose is text the flag removes or the reader declines (a 41-px-tall 'The Corporate Card' the reader read at confidence 0.20 and so did not offer). |
| ✅ Letter-spaced display type re-joined (`line_join_spaced`) — ADOPTED (2026-09-14) | Letter-spacing is a typographic convention, not word spacing: a run of one-letter tokens is one or more words; the lexicon segments it (the classic word-break problem, dynamic programming over endorsed words) | `decode/lineread.py` `_join_spaced`: a run of three or more single alphanumeric tokens becomes the fewest lexicon-endorsed words that cover it, or one token | 'FORTY F O U R H U N D R E D' → 'FORTY FOUR HUNDRED'. dev-8 97.0/93.3 → same, broad-30 94.8/89.7 → 94.8/**90.2** (precision 94.1 → 94.5), legal-8 94.8/89.1 → same, modern 92.0/86.8 → same. The headline set moves by its bar and nothing else moves: adopted. |
| 🔬 Display faces for the reader (`seq_line_v7c`) — NEGATIVE, not adopted (2026-09-14) | Letterheads are set in display faces at large sizes; give the reader such windows | `make_seq_data.py --caps-frac 0.6 --x-heights 20 26 32 40 --words 1 4` over all 3,211 faces → `data/seq_synth_display.npz` (150k windows); `train_seq.py --init data/seq_line_v7a.npz` with it beside the standard mix, 4 epochs at 1e-3, seed 3 (290 min); judge re-harvested against the new reader (2,893 pairs, 61% reading-better) and refitted | Held-out real words 93.1% on a holdout that now includes the display windows. Reader alone on the blocks: broad-30 97.8/94.1 → 98.0/94.4, legal-8 96.2/95.9 → 96.0/93.5. Pages with its judge, against the adopted row: dev-8 97.0/93.3 → 97.0/93.7, broad-30 94.8/90.2 → **94.7/89.7**, legal-8 94.8/89.1 → 94.5/88.5, modern 92.0/86.8 → 91.7/86.4, business 91.9/86.2 → **91.1/84.6**; broad-30 letterhead errors 1012 (71.9% char) (adopted reader: 941, 73.7%). The display windows buy the letter blocks a few tenths and cost the typewriter, business and modern pages more; the judge fitted against this reader also takes fewer lines (78% at P ≥ 0.5 against 83%). A model that moves 4 of 5 sets down is not adopted; `v7a` stays live. The letterhead residual wants something other than more synthetic capitals — the lines still flagged are logos and letter-spaced names the lexicon does not know, and the census's 0.94 output ratio says they are dropped, not misread. The renderer bug found on the way (the capitals option landed in `max_width`'s slot and every window was rejected; forty minutes of eight cores at 100%) is fixed. |
| 📊 Word errors on the letter blocks, by kind (2026-09-14) | Character parity with legacy on the broad-30 blocks (98.2 both) but 1.7 word points behind: what kind of word errors are they? | ad-hoc census over the 43 broad-30 Text blocks under the neural profile: every differing word aligned (difflib) and classed | 330 word errors: **split/merge 46%** ('topics :', 'a valuable' → 'avaluable', 'TurboCAD' → 'Turbo CAD', "you'll" → 'you ll'), extra tokens 14%, one-character substitutions 15% (apostrophes dropped, "We'll" → 'Well'; '*' → 'k'; '~' bullets), case 9% (a capitalized word lowercased: 'Congress', 'Washington', 'Operator' — the classic decoder's words, so the case decision, not the reader), other 7%, missing words 5% (every one a closing line, 'Sincerely,' — an artefact of the zone-truth derivation, which attached the closing's words to the Text zone because our page-level reading placed them there; the crop never contained the line), punctuation-only 3%. Two repairs follow from the list: a lone ',.;:!?' token attaches to the word before it, and a clitic split from its word by a lost apostrophe is rejoined when the lexicon knows the contraction (`line_fix_tokens`, measured below). The case share wants a capitalization prior the lexicon does not have yet (it is lowercased). |
| 🔬 Punctuation attach and contraction rejoin (`line_fix_tokens`) — inert, removed (2026-09-14) | The two repairs the block census suggested | a lone ',.;:!?' token joined to the word before it; 'you ll' → "you'll" when the lexicon endorses the contraction | dev-8 97.0/93.3 → 96.9/93.2, broad-30 94.8/90.2 → same, legal-8 94.8/89.1 → 94.8/89.0, business same; blocks broad-30 98.2/94.9 → 98.2/95.0. The census's examples are real but few, and the rules also fire where the truth itself has the space or lacks the apostrophe. Removed; the split/merge share of the block word errors is the reader's and the classic segmenter's space decisions on ordinary words, which no token rule reaches. |
| 🔬 Capitalization prior from the corpus — NEGATIVE, removed (2026-09-14) | The case share of the residual (9% of block word errors, 14% on the letters): a word the corpus writes with a capital nine times in ten ('Washington', 'Congress', 'Mr', 'ABC') takes that form when read up to case | a `build_case_forms.py` over the raw (un-lowercased) corpus, 1,531 entries at share ≥ 0.9 and count ≥ 20; a decoder pass restoring the dominant form (`case_forms_path`); both removed | dev-8 97.0/93.3 → 96.7/93.2, broad-30 94.8/90.2 → **94.5/89.8**, legal-8 94.8/89.1 → **94.1/88.1**, business 91.9/86.2 → 91.6/85.8, blocks broad-30 98.2/94.9 → 98.2/94.8. The corpus (nineteenth-century books and the Federal Register) is the wrong case reference for business letters: its capitalized entries include acronyms that the letters use as words and words that happen to open sentences in a small sample, and the truth itself writes 'congress' lowercase where the prior insists on the capital. The case share is real but needs page-local evidence (the word's own glyph heights, which the line-case pass already uses for size twins) rather than a corpus prior. |
| 🔬 Lexicon re-segmentation of the reader's lines — NEGATIVE, removed (2026-09-14) | The split/merge share (46% of the block word errors): word boundaries by dynamic programming over the reader's letters, a corpus log-frequency per endorsed word, a per-character penalty per unknown word, 2 nats per boundary that disagrees with the reader's own space emissions | `line_resegment` in `decode/lineread.py`, removed | Reader alone on the broad-30 blocks 97.8/94.1 → **93.2/41.6**; with the judge 98.2/94.9 → 97.7/91.9; pages dev-8 93.3 → 91.8 word, broad-30 90.2 → 88.4, legal-8 89.1 → 84.2, business 86.2 → 84.0. The DP prefers runs of short frequent words and long unknown spans over the reader's boundaries: a corpus unigram is no match for the reader's own spacing evidence, which is right on 98 words in 100. The split/merge residual on the blocks is decided at the frame level (a space emitted or not under a tight gap) and the fix, if any, is in the reader's training on tightly set lines, not in a post-pass. |
| 🔬 The '@' class, with a control rebuild — no gain on the receipts, not adopted (2026-09-14) | The receipts read '@' as 'e' 35 times on twelve pages because the class set has no '@'; the rare-class lesson says a new class is judged against a rebuild without it | `MLWS_EXTRA_CLASSES="@"` (`factory/stock.py`): control rebuild of the condensed prototypes, the uncondensed pool, the MLP and the outline bank (`*_ctl.npz`) beside the same rebuild with '@' (`*_at.npz`); the neural profile with the paths swapped | The control reproduces the live rows (dev-8 97.0/93.3, broad-30 94.8/90.2, legal-8 94.7/89.1, modern 92.0/86.8, business 92.0/86.2), so the classic tables rebuild faithfully. With '@': dev-8 97.1/93.8, broad-30 94.9/90.2, legal-8 94.3/**88.5**, modern 92.0/86.9, business 91.9/85.9 — and the receipts themselves 87.0 → **84.9** word. The class reached the classic channels only: the sequence models' class lists are fixed at training (`default_classes()` from CHARSET at the time), so the line reader still cannot emit '@', and under the neural profile the receipt lines that carry it are the reader's; where the classic decoder did read an '@', the judge's features saw a word the lexicon does not know. Giving the receipts their '@' means retraining both sequence models on a 113-class list, a day of GPU for 3% of the receipt words; recorded and deferred. The control-versus-variant machinery (an environment knob, variant files, paths swapped by `--set`) is the way to run any class experiment from here. |
| 🔬 Standout gap on short cell lines — NEGATIVE, removed (2026-09-14) | The fused capital-letter cells of the business statements ('ATMWITHDRAWAL'): on a line too short for a gap population, the widest gap that stands twice above the next is offered as an uncertain word gap for the lexicon to decide | `cell_gap_ratio` 2.0 in `_segment_line`, removed | business 91.9/86.2 → 91.9/**85.2** (precision 95.2 → 94.2), the four standard sets identical. The rule fires on short lines that are not cells — a kerned pair inside a word on a two-word line — and the lexicon variant search, offered the split, takes it often enough to cost a word point. The fused cells themselves turn out not to sit under the fallback threshold at all (space_lo is 0.35 x-heights, their gap is 0.5): they reach the variant search and lose there, because the second word is misread ('W/THDRAWAL') and so unendorsed. The cell problem is the reader's, not the segmenter's, and the reader already reads those cells when the judge gives them to it. |
| 🔬 A fitted judge for the graphic-flagged lines — negative on the typewriter set, the fixed gate stays (2026-09-14) | The 69 letterhead lines still flagged: let a judge fitted on flagged lines decide whether the reading beats dropping the line | `harvest_line_choice.py --graphic` (flagged lines, label = the reading is closer to its truth line than nothing; an unmatched flagged line is logo art, label 0; 1,162 pairs on 244 pages, 26% worth keeping), `train_line_choice.py` → `data/graphicchoice.npz` (P ≥ 0.5 takes 19% at 83% precision on a page-disjoint holdout); the decoder rule removed again | Against the fixed gate (confidence ≥ 0.6 and one endorsed word): dev-8 97.0/93.3 → 97.0/93.5, broad-30 94.8/90.2 → 94.7/90.1, legal-8 94.8/89.1 → **93.5/88.3**, modern and business same. The judge takes flagged typewriter lines the gate refuses — the pleadings' line-number columns and stamps, on which the reader's confidence is high enough and a numeral counts as endorsement — and the letters gain nothing measurable. The gate stays; the harvest is kept as the data for a better judge when the flagged letterhead lines have a reader that reads them. |
| 📊 Line model, second seed (`seq_line_v7s2`) — the adopted model is not a lucky seed; the line recipe's seed range measured (2026-09-15) | The two-seed door of the model rule, applied to the adopted `seq_line_v7a` after the fact | The adopted recipe with `--seed 2` (528 min), its judge re-harvested (2,844 pairs, 63% reading-better) and refitted; every set under the neural profile with that judge | Held-out real words 94.7% (v7a 94.8%). Reader alone on the blocks: dev-8 98.9/96.6 (v7a 99.0/97.3), legal-8 96.5/94.4 (96.2/95.9), broad-30 98.1/94.5 (97.8/94.1). Pages: dev-8 96.9/93.4 (v7a 97.0/93.3), broad-30 94.8/89.7 (94.8/90.2), legal-8 94.5/88.8 (94.8/89.1), modern 92.0/86.8 (same), business 91.6/85.5 (91.9/86.2), news-8 94.0/90.2 (94.1/90.4), mag-8 76.0/66.3 (76.4/67.1); blocks broad-30 98.2/94.9 (same), legal-8 96.4/93.4 (96.5/94.0). Seed 1 is ahead by a tenth to half a word point on every set but dev-8 and modern, all inside the ranges the model rule uses (broad-30 0.5, legal-8 1.0, modern 0.7, dev-8 0.4), so the adoption stands and the line recipe's own seed range is now measured: 0.1 dev-8, 0.5 broad-30, 0.3 legal-8, 0.0 modern, 0.7 business word. `v7a` stays live. |
| 🔬 Lower confidence gate for flagged lines (0.45) — flat to negative (2026-09-14) | Rescue more of the 63 letterhead lines still flagged | `line_graphic_conf` 0.45 against 0.6 | dev-8 and broad-30 identical, legal-8 89.1 → 88.6 word. The lines the gate still withholds are ones the reader itself is unsure of; the gate is not the lever, the reader's training on display faces is. |
| 🔬 Reader-confidence junk test for unendorsable lines — inert, removed (2026-09-14) | Use the reader's uncertainty to flag logo art | `line_junk_conf` 0.3: a line neither reading can endorse a word of, with a reading under 0.3 mean emission probability, flagged graphic | Identical output on dev-8, broad-30, legal-8 and business: the lines it would catch are already flagged by the classic decoder's distance test. Removed. |
| 🔬 Capitals x-height scale for the reader's strip — negative, removed (2026-09-14) | A caps line's x-height estimate is its cap height, so its strip is normalized 40% too small for a reader trained on lowercase-anchored strips | `line_caps_scale` 0.72 applied to the strip cut of a line whose classic letters are 80% capitals | dev-8 97.0/93.3 → 97.0/93.4, broad-30 94.8/89.7 → 94.8/89.6, legal-8 94.8/89.1 → **94.0/88.0**, business 91.9/86.2 → 92.3/85.7. The typewriter pages, whose captions are capitals, lose a word point: the reader's real training lines include those caps lines at the estimate the harvest gave them, so the 'correction' moves them away from what it learned. Removed; the display-face letterheads want display faces in the reader's training, not a rescale. |
| 🔬 Word spacing on a line of capitals judged at a scaled x-height — NEGATIVE, removed (2026-09-13) | The cap-height-as-x-height diagnosis from the business statements | `_segment_line` (0.72 x the measured height on lines whose top-1 readings are 80% capitals or digits), removed | business 91.7 / 84.9 → 91.2 / **79.8**, dev-8 96.3 / 91.9 → 96.5 / 92.2, legal-8 92.0 / 84.4 → 92.0 / 84.2: the lower thresholds split the letter gaps of every caps line on the statements and purchase orders, five word points, against a gain of a few fused cells. The fused-caps-cell problem is real (the gap band cannot form on a two-word cell) but the x-height is not the lever; a cell-level rule wants the column structure, which the row reader has and the line segmenter does not. |
| 🔬 Numeric join with a look-ahead for the separator — inert, removed (2026-09-13) | The leading-'1' split on dates and amounts | `_segment_line` `numeric_join` (four glyphs ahead searched for a numeric separator) | Identical output on business, modern, dev-8, legal-8 and broad-30: the gap after a leading '1' never reaches the join rule (the data-line gate or the tiny-mark branch decides it first). The split stays on the list for the row reader. |
| 🔬 Endorsed-injection margin lowered (1 and 2 nats) — negative, 3 stays (2026-09-13) | Prompted by the oracle-miss census on the harness dumps: of the dev-8 words whose truth is outside every classic variant (67 of 1,466, 4.6%), 54 are same-length substitutions the scorer reads right ('t0' → 'to', '0f' → 'of', 'vou' → 'you', 'CCO' → 'CEO') and 19 are case-only; on legal-8 (49 of 1,263) 25 are case-only. The scorer's greedy read is above the classic oracle on both sets, so the question was whether the 3-nat margin withholds endorsed corrections | `decode/beam.py` `seq_inject_margin` | Live scorer, margin 1.0 / 2.0 against 3.0: dev-8 91.5 → 91.4 / 91.4 word, broad-30 87.4 → 87.2 / 87.4, legal-8 84.3 → 84.2 / 84.0. Each lower margin lets in as many wrong endorsed strings as right ones (a short endorsed word is easy to like by a nat or two); the margin stays at 3. The same-length misses are therefore not an injection-threshold problem but a variant-list one: the character the truth needs is missing from the beam's candidates for that glyph ('0' for 'o', 'l' for 'I', '.' for ','), which points at the classifier's candidate list and the punctuation priors, not at the scorer. |
| ✅ Live-recipe fine-tune on five real harvests at real weight 3 (`seq_en_v6c`), second seed — ADOPTED; live scorer = seed 2 (2026-09-13) | Two-seed door of the model rule | `train_seq.py --init data/seq_en_v4s1.npz --synth seq_synth_v1 seq_synth_gfonts_v1 --lines <five harvests> --real-weight 3 --epochs 6 --seed 2` (131 min); live `data/seq_en.npz` = `seq_en_v6c_s2.npz`, previous live kept as `data/seq_en_v4s1.npz` | Seed 2: held-out real 96.5%; harness dev-8 greedy 98.2% / legal-8 98.3% (seed 1: 98.1 / 98.3; the previous live 97.5 / 98.2). Six sets against the previous live scorer (fold convention), seed 1 / seed 2, char/word: dev-8 96.1/91.5 → 96.5/**92.1** / 96.3/**91.9**; broad-30 93.5/87.4 → 93.5/87.5 / 93.7/87.5; legal-8 92.2/84.3 → 91.0/83.7 / 92.0/84.4; modern 91.5/85.7 → 91.5/85.9 / 91.6/86.1; news-8 93.5/87.3 → 92.9/**88.6** / 93.1/**88.2**; mag-8 68.4/51.1 → 66.4/51.9 / 65.9/51.8. Both seeds clear dev-8's seed range (0.4 word) and lift news-8 by about a word point; broad-30, legal-8 and modern move inside their ranges on both seeds and never down on seed 2 (seed 1's legal-8 character loss of 1.2 is seed-specific: seed 2 reads 92.0 there). The headline set is flat, not up — this is not a headline win, and the adoption rests on two seeds agreeing on the tuning set, the newspapers and the offline harnesses, with nothing lost at the bar on the standard sets. Recorded against it: mag-8 loses two character points on both seeds while gaining a word point (its worst pages are read with fewer characters; the same word-up/char-down shape as the newspapers' first day), and it is a fine-tune of the previous live scorer, so its faces are the 615-face set, not the 3,179. Seed 2 is the live file because seed 1 lost legal-8 characters; a choice between seeds on the evaluation sets is a small selection bias and is recorded as such. Classic regression guard: the classic profile does not load the scorer, and its dev-8 row re-reads 95.2 / 89.2 exactly. |
| 🔬 Per-character length bonus in the CTC variant comparison — inert, opt-in (2026-09-13) | Insertion penalty / length normalization, the standard question of any likelihood-based rescoring (Graves 2012 §7) | `decode/beam.py` `seq_len_bonus` (nats per character credited before the comparison; default 0) | Hypothesis: a likelihood summed over characters favours the shorter variant where the image is ambiguous, and the `v6c` scorer's extra character deletions on legal-8 (its `seq_flips` 13 → 24 on the worst page, 72 fewer output characters) are that bias sharpened. Live scorer at 0.5 and 1.0 nats: dev-8 96.1/91.5 → 96.1/91.5 and 96.2/91.6, broad-30 93.5/87.4 → identical, legal-8 92.2/84.3 → identical. The variants a word offers are mostly of one length, and where they differ the decoder's own terms already carry the decision. Kept at 0; the `v6c` loss stays unexplained until the second seeds say whether it is systematic. |

## External benchmark (2026-08-31)

Same 30-page UNLV bus.3B sample, same harness and metrics
(`scripts/eval_tesseract.py`):

| system | char acc | word acc |
|---|---|---|
| Tesseract 5.5 LSTM (`--oem 3`) | 95.9% | 92.2% |
| Tesseract legacy pre-neural (`--oem 0`) | 95.3% | 90.5% |
| **mlws-ocr** (feature+GRU stack, day 2) | **77.1%** | **47.3%** |
| 📊 Tesseract's LSTM engine measured on every set (5.5.3, default tessdata_fast English, `--oem 3`) — the second yardstick beside legacy (2026-09-16) | The question "can we equal Tesseract?" needs both engines on the board: legacy is the stated reference, the LSTM is what a default install reads with | `eval_tesseract.py --oem 3` and `eval_blocks.py score --legacy --oem 3` on the same pages and seeds as every other row; no `TESSDATA_PREFIX` (homebrew's bundled model) | Char / word (recall / precision): dev-8 95.5 / 93.1 (97.1 / 96.8), broad-30 96.0 / 92.7 (96.7 / 95.4), legal-8 90.3 / 86.9 (98.8 / 90.9), modern 74.2 / 66.6 (89.5 / 91.6), business 70.7 / 68.6 (98.0 / 98.6; by kind invoice 40.7 / 32.7, payslip 34.6 / 36.7, purchase order 89.5 / 89.4, receipt 93.8 / 91.2, statement 95.0 / 93.1), news-8 96.7 / 94.8 (98.6 / 95.7), mag-8 87.8 / 84.4 (97.4 / 90.5); blocks pooled dev-8 99.4 / 98.5, legal-8 97.1 / 96.0, broad-30 98.5 / 96.5. Against legacy: about a word point better on letters, newspapers and every block set; level on business, magazines and modern, where Tesseract's shared page analysis decides; WORSE on the typewriter pleadings (90.3 / 86.9 against legacy 90.4 / 88.7 and our 94.8 / 89.1) with precision 90.9 — it reads text into the ruled margins and hole punches. Against the neural profile: we lead legal-8, modern and business, tie dev-8 characters and lose 1.2 character / 2.5 word on the headline set, of which the bare-block comparison (98.2 / 94.9 against 98.5 / 96.5) says 0.3 character / 1.6 word is recognition and the rest is the letterhead deletions. On receipts it reads 93.8 / 91.2 against our 94.8 / 87.0: level in characters, four word points ahead, on the monospace roll where the '@' price token is one of our two misses. Recorded so the roadmap can name which gap is layout and which is the reader. The public `langdata_lstm` word list and training text are admissible inputs here (Apache 2.0, public data); the weights are not. |
| 🔬 The '@' class in BOTH sequence models, trained from scratch on the 113-class list (`seq_en_at`, `seq_line_at`, judge `linechoice_at`) — receipts DOWN, typewriter set down; not adopted (2026-09-16) | Receipts miss every '2 @ 2.72' price token because no channel has an '@' class (35 misses on twelve receipts, 2026-09-14); the classic channels alone measured negative, so the scorer's and the reader's class lists were the untried half | `MLWS_EXTRA_CLASSES="@"` for every step: `numeric_token` emits 'N @ price' and 'name@domain' tokens when '@' is a class (`factory/words.py`); `make_seq_data.py --n 100000` → `seq_synth_at` (42.7% touching); `train_seq.py` from scratch on the v6c recipe plus that set (12 epochs, 345 min, held real 96.4%; live 96.5%); the line model from it on the v7a recipe plus the set (8 epochs, 612 min, held real 94.9%; live 94.x); `harvest_line_choice.py` on the four harvest sets with the '@' classic tables and both models (2,796 pairs on 252 pages, reading better 64.8%; P ≥ 0.5 takes 80.6% at 75.3% precision on a page-disjoint holdout) → `linechoice_at`; six evaluations with every path swapped by `--set` | Against the live rows, char / word: dev-8 97.0 / 93.3 → 96.9 / 93.5, broad-30 94.8 / 90.2 → 94.5 / 89.8, legal-8 94.8 / 89.1 → **93.8 / 86.5**, modern 92.0 / 86.8 → 92.0 / 87.0, business 91.9 / 86.2 → 91.4 / 85.2, blocks broad-30 98.2 / 94.9 → 98.1 / 94.8. By kind against a same-day live run: invoice 92.6 / 87.2 → 92.7 / 87.6, payslip 87.2 / 84.0 → 87.2 / 84.2, purchase order 92.7 / 86.6 → 92.5 / 85.7, **receipt 94.8 / 87.0 → 92.3 / 83.2** (recall 92.3 → 91.6), statement 92.5 / 86.3 → 92.4 / 85.3. The class the experiment exists for LOSES on the pages it exists for: the receipts fall 2.5 characters and 3.8 words, so whatever the new class gains on the price tokens the from-scratch models lose elsewhere on the monospace roll, and the typewriter set loses 2.6 word points, the same from-scratch loss recorded for `v5s1` (legal −1.3) and every from-scratch recipe since — the live scorer's lineage is a fine-tune chain from `v4s1` and a class-count change forced a fresh start. Two confounds, one experiment: the class and the lineage. Not adopted; every file kept as a variant. The separation is under way: `SeqNet.with_classes` (recognize/seq.py) widens a trained model's output layer by class NAME with the new class starting rare, `train_seq.py --init` applies it when the class list differs, and a fine-tune of the live scorer (6 epochs) and live line model (4 epochs) on the same data plus the '@' set is training — the class alone, lineage kept. Whichever way it comes out, this row and that one together say whether '@' was ever the receipts' problem (the misses are two tokens a line; the LSTM yardstick reads receipts at 93.8 / 91.2 with an '@' class and does not beat our 94.8 characters). |

Newspapers (10pg): Tesseract LSTM 85.0/81.6; magazines (6pg): 73.3/63.6.
Reading: the legacy engine proves the pre-neural architecture reaches 95%+
on this corpus — the project's founding premise is validated by its own
benchmark rival. The 18-point gap is implementation maturity, chiefly
(a) classifier training data: legacy Tesseract's shape classifier was
trained on enormous real-glyph corpora versus our 26 synthetic fonts;
(b) its character chopper/associator (a full segmentation search we
approximate with single-cut variants); (c) two decades of accumulated
edge-case handling. Historical context: ISRI's 1996 annual test reported
top commercial engines around 97–99% char on these very sets.

## Design lineage (whole-system)

- The overall architecture — explicit features, prototype matching, adaptive
  per-document classification, LM-scored decoding — is the classic pre-neural
  OCR stack, best publicly documented in R. Smith's Tesseract papers (2007,
  2009 "Adapting the Tesseract open source OCR engine for multilingual OCR").
  This project is a from-scratch, legibility-first reimplementation of those
  ideas, not a port.
- Stage/registry/DebugBundle pipeline design and the "manufacture the labels"
  program (synthetic θ-degradation + printed calibration sheet) are original
  to this project's plan; the θ-degradation half follows Baird/Kanungo.
