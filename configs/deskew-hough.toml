# Default cleanup pipeline. Swap any stage's impl to compare algorithms;
# the inspector's diff view renders two runs side by side.

[pipeline]
stages = ["magnify", "deskew", "illumination", "binarize", "despeckle"]

[stage.magnify]
impl = "xheight"
target_px = 0                  # median glyph height to magnify small-type pages up to (0 = off; measured 2026-09-20)

[stage.deskew]
impl = "hough"
max_angle = 5.0

[stage.illumination]
impl = "median_background"

[stage.binarize]
impl = "sauvola"
window = 41
k = 0.2

[stage.despeckle]
impl = "components"
min_area_300dpi = 5
